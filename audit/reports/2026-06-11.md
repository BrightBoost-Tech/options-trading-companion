# AUDIT v5 — NIGHTLY — 2026-06-11

- **Run:** NIGHTLY, executed manually at 2026-06-11 06:17Z (01:17 CT) as a stand-in — **the scheduled midnight Task Scheduler run was blocked by the account spend limit and never executed** (audit-infrastructure miss, caught by the human, noted in §2 and Area 4). READ-ONLY: SELECT-only SQL, broker read-tools only; only writes are this report + ledger appends.
- **Baseline:** `audit/reports/2026-06-10.md` (FULL) + `audit/ledger.md`. Window covered: ~06-10 mid-run → 06-11 06:17Z (~15h, mostly the 06-10 afternoon session + a quiet overnight).
- **No ALERT file written** — nothing critical found this run. H11 sweep: zero CRITICAL `risk_alerts` since the prior run.

---

## 2. Ground-truth delta vs 2026-06-10

- **Code/worker (H8):** origin/main HEAD = `93d19c6` (#1052) = worker deploy `7f7d203f…` SUCCESS 06-10 20:39:39Z; RQ worker `e0e32541…` processing on `otc` from ~20:45Z. No merges since. PR #1053 HELD open by design (not a finding). Two deploys landed after the prior run's market hours: `756627e` (#1051, 20:27Z) and `93d19c6` (#1052, 20:39Z) — **neither has seen a market-hours cycle yet**; today 16:00Z/16:30Z is their first exercise.
- **Flags:** env-set controls verified on worker: `RISK_UTILIZATION_GATE_ENABLED=1`, `RISK_MAX_UTILIZATION_PCT=0.85`, `RISK_ENVELOPE_ENFORCE=1`, `EXECUTION_MODE=alpaca_live`, `LIVE_ENABLED=TRUE`, `POLICY_LAB_ENABLED=true`, `INTRADAY_TARGET_PROFIT_ENABLED=1`, `H7_PREFILTER_ENABLED=true`. The #1040–#1052 kill-switches (REENTRY_COOLDOWN, ENTRY_QUOTE_VALIDATION, LEARNING_HISTORICAL_QUARANTINE, CLOSE_REARM, INTRADAY_COHORT_STOP, CALIBRATION_STALENESS_TTL, ENTRY_QUOTE_SOURCE_ALIGNED) are absent-from-env = **default-ON, code-verified at `93d19c6`** (e.g. `paper_endpoints.py:936/946` empty/unset→ON, test-pinned). `CALIBRATION_EV_EPOCH` absent from env = code default `2026-06-11T00:00:00+00:00` (`calibration_service.py:82-83`, pinned by `test_honest_pop.py:158`) — **epoch ARMED, not a no-op**. `PRICE_CLASS_SPREAD_CUTOFF` absent = $60 default. Env diff vs CLAUDE.md: `REGIME_V4_ENABLED` now `0` (was `true`) — dead flag either way, no behavior change.
- **Broker (live 211900084):** equity **$2,207.85** at read time (last-trade option marks, pre-market) vs `last_equity` **$2,263.85** (06-10 EOD) — i.e. the book closed 06-10 UP ~$43 from the prior run's $2,220.85 read, current read is mark-method noise on an illiquid spread. Cash/OBP **$1,885.85** (unchanged). Same single open structure: NFLX 07-10 P86/P79 1ct, entry 3.65, mark 3.22 (P86 4.80 / P79 1.58), **unrealized −$43**, max loss $365. **Zero broker orders and zero fills of any kind since the prior run** (broker `get_orders` after 06-10: empty; `paper_orders` since 06-10: empty).
- **Jobs:** zero failed job_runs 06-08→06-11 (181/178/179 succeeded + 1 on 06-11 so far: `phase2_precheck` 05:00Z OK). `alpaca_order_sync` day 2 post-#1049: **1.43s avg / 3.05s max / 96 runs** (vs 6.52s on 06-09) — holding.
- **Funnel day-over-day:** 06-09 emitted 3 → staged 1 → blocked (old pre-#1044 code) ⇒ 06-10 emitted **14** → candidates 3 → staged **2** (XLE + NFLX, first 2-candidate day; #1047 effect, already ledger-verified) → executed **0**: 3× XLE `entry_quote_unpriceable` (feed divergence — #1052's exact target, fixed that evening) + 2 NFLX cohort forks silently skipped unstamped (see Area 6 note). Budget telemetry still `usage:0 / cap 754.34` on a 1-position book (ledgered A6, unchanged).
- **Rejections:** 06-10 total 143 vs 06-09 165; mix stable (spread_too_wide_real 39 vs 50; no_fallback 42 vs 48); 1× `insufficient_history` = SPCX pre-listing (designed; IPO monitor 16:45Z OK, lists 06-12).
- **reentry_cooldowns:** 0 rows — correct (no stop event since #1040 deploy).
- **H11 (unconditional):** zero CRITICAL since prior run. HIGH: 26 steady-state "NFLX is 100% of risk" concentration WARNs (known post-#1044 noise, ledgered under A4/A5) + **1× `loss_per_symbol_protection_degraded`** 06-10 13:30:02Z (NFLX unpriceable on the first monitor tick of the day; loud skip, no action on stale, repriced by 13:45Z) — **the #1035/#1036 fail-closed design working as built**, not a finding.
- **Calibration:** RAW MODE confirmed in DB — deploy-time empty-blob reset row at 06-10 **20:27:34Z** (`total_outcomes:0`, `{}`), 19s after the #1051 deploy. Designed, not a finding. Today's 10:00Z write will be the first post-epoch run (expect insufficient-data no-op at all rungs — also designed).

---

## 3. Pending-verifications table

| # | Item (ledger) | Status | Evidence / next check |
|---|---|---|---|
| 1 | 06-11 16:00Z FIRST HONEST-EV CYCLE (debit EV ~2× drop; entries may → 0 = correct; calibration ratio 1.0) | **STILL PENDING** — fires ~16:00Z today (run was 06:17Z) | Raw baseline locked for the diff: 06-10 NFLX `ev_raw 89.61 / pop_raw 0.6423`, XLE `84.08 / 0.5927`; blob empty (`{}` @20:27Z) so calibrated==raw today. Honest math (#1051 `756627e`) is on the running worker. |
| 2 | 06-11 16:30Z [ENTRY_QUOTE] FEED DIVERGENCE warnings + blocked_reason stamps | **STILL PENDING** — fires ~16:30Z today | Code-armed at `93d19c6`: `_aligned_leg_quote_fetch` truth-layer→Polygon + divergence WARNING (`paper_endpoints.py:984-1000`); stamp sites live (`paper_autopilot_service.py:850/860/872/894`). Note: the 06-10 16:30Z rows show NULL blocked_reason — **expected**, the stamping rider deployed 20:39Z, AFTER that run. Caveat for tonight's checker: dedup/filter skips will NOT stamp (see Area 6). |
| 3 | Nightly-audit queue: CLOSE-side Polygon-only quote validation | **DONE — exposure CONFIRMED in code** (new ledger entry) | Stage-time combo quote `paper_endpoints.py:645` and internal fill engine `:1179` are Polygon-only `_fetch_quote_with_retry`; #1052's aligned fetch is wired ONLY into `_validate_entry_quotes`. Live path insulated (monitor marks = truth layer `intraday_risk_monitor.py:462-487`; live fills = broker). Blast radius = shadow/internal close fills + TCM staging inputs. See Finding N1. |
| 4 | #1048 cohort stops first exercise | **STILL WATCH** | No stop event since deploy (book −$43; aggressive cohort stop ≈ −$109.50, envelope ≈ −$66 — neither crossed; zero close orders). Cohort configs loading without failure warnings. |
| 5 | reentry_cooldowns first write on next loss-envelope stop | **STILL WATCH** | 0 rows, flag default-ON — correct (no stop has occurred). |
| 6 | PR #908 next system close (live credit-mleg) | **STILL PENDING** | No system close since 06-08 (which already passed once — see 06-10 report §3 row 6). Candidate remains open NFLX. |
| 7 | First post-epoch calibration write behavior | **STILL PENDING** — fires 10:00Z today | Expect: 30/60/90d rungs all `insufficient_data` (epoch 06-11 excludes all pre-fix outcomes) → users_skipped, raw mode persists = DESIGNED. A `calibration_stale` alert is expected only after ~06-20 (TTL) — reminder, not defect. |

---

## 4. Areas 1–7 (NIGHTLY: one-liners unless signals moved)

**AREA 1 — PROFITS: UNCHANGED** (fix shipped, awaiting first exercise). The ledgered CRITICAL (raw-delta PoP) is fixed-and-live (#1051 on worker `93d19c6`) but NO market-hours cycle has run on the fixed code yet — first honest-EV evidence lands 16:00Z today (table row 1). No new profit-side signal in the 15h window (zero fills).

**AREA 2 — MINIMIZING LOSSES: UNCHANGED.** No envelope/stop/close events. The one HIGH alert (06-10 13:30Z `loss_per_symbol_protection_degraded`, one tick, recovered 13:45Z) is the #1035/#1036 fail-closed design operating correctly. Daily-loss realized-blind finding stands as ledgered (no new data — no realized losses in window).

**AREA 3 — SELF-LEARNING: UNCHANGED.** Zero new `learning_feedback_loops` rows since 06-09; store untouched. Epoch + dedup + live-dimension (#1051) live but unexercised (no closes). Empty-blob RAW MODE confirmed in DB (20:27:34Z row) — designed.

**AREA 4 — SELF-SUSTAINING: UNCHANGED** on the ledgered ops-health dead-tail (no code change). **Meta-note (not a system finding, but squarely this area's theme):** the audit loop itself silently failed tonight — the midnight Task Scheduler run was blocked by the account spend limit, nothing alerted, and a human caught it. The "who watches the watcher" question now demonstrably includes this audit's own scheduler. Recommend (operator-side, outside the repo): a failure/no-run notification on the Task Scheduler job or a morning "did the report file appear?" check.

**AREA 5 — EFFICIENCY: UNCHANGED-IMPROVED.** #1049 holding day 2 (1.43s avg, −78% vs baseline). 06-10 16:00Z scan 35.0s; monitor tick at 16:00:41Z — the known grid-collision delay (ledgered A5), no >120s event in window.

**AREA 6 — TRADING VOLUME: signal MOVED (funnel widened; conversion still 0) + one small NEW observability finding.** Emitted 3→14, staged 1→2 (#1047 working, ledger-verified). Conversion to entries: 0/2 — 3 XLE forks died on the feed-divergence quote bug (#1052 fixes exactly this; re-test today), and the 2 NFLX forks (aggressive + neutral) were **silently skipped with no stamp, no error, no log above INFO**, left `status=pending`/`blocked_reason=NULL` to be swept by the morning sweep. Mechanism (CODE): per-cohort symbol-dedup (`paper_autopilot_service.py:746-770`) and the min-edge/min-score filters (`:397/:409-427`) `continue` without calling `_stamp_blocked_reason` — only cooldown/utilization/quote stamp (`:850/:860/:872/:894`). The cohort books both hold NFLX (live `7f604f7a…`-class + neutral shadow fork), so dedup-skip is the high-probability cause for those two specific rows (HYPOTHESIS at the per-event level — the [DEDUP] log line is INFO-invisible in Railway); the **stamping gap itself is code-certain**. This is a refinement of the ledgered "dismissed-status funnel gap"/#1051-rider arc that CHANGES the action (extend stamps to dedup/edge/score skip sites) → appended to ledger as Finding N2. Severity LOW (observability only; no trading behavior implicated).

**AREA 7 — TIME IN TRADE: UNCHANGED.** No closes; zero new hold-time data. Open NFLX in day 3 of the documented stall corridor (−$43; nothing in reach between −$66 envelope and +$182.50 flat TP until the 7-DTE dump 07-03) — the ledgered A7 finding's live illustration, not new.

---

## 5. AREA 8 — SELF-EXTENSION

**Spec KEPT:** NEGATIVE-DECISION EFFICACY (counterfactual lens), `audit/area8.md` 2026-06-10 — NIGHTLY does not re-derive the slot. **Status: UNCHANGED, with corroborating drip:** the window added 5 more outcome-unmeasured negatives (3 XLE quote-rejects + 2 unstamped NFLX skips) and 0 measured positives — the ~400:1 ratio worsens. Finding N2 (stamping gap) is upstream feedstock for this lens: unstamped skips are unmeasurable even in principle.

---

## 6. New findings this run

### N1 — CLOSE/internal-fill path still reads quotes Polygon-only (the #1052 divergence class, close side) — severity LOW-MEDIUM — **FINDING** (code-certain exposure; impact prospective)

- **WHAT:** #1052 aligned the ENTRY validator to the scanner's source set, but the two other stage/fill quote reads remain legacy Polygon-only: (a) stage-time combo quote feeding TCM staging values for ALL orders incl. closes — `paper_endpoints.py:645`; (b) the internal fill engine's fresh-quote read that prices every internal/shadow fill — `paper_endpoints.py:1179`. A Polygon-dark-but-OPRA-real leg (exactly the 06-10 XLE event: Polygon 0/0 vs OPRA 2.15×428/2.39×565) on a SHADOW CLOSE → fill falls to the TCM `missing_quote_fallback` (stale staging-derived price, `:1219-1230`) or stalls below the $1.01 floor — biasing shadow learning outcomes and D6 live-vs-shadow comparisons.
- **WHY it matters / WHY not higher:** LIVE path is insulated both ways — close limits come from monitor marks via `MarketDataTruthLayer` (`intraday_risk_monitor.py:462-487`) and live fills execute at the broker. The harm channel is shadow-fill integrity (the #1017 lesson's surface) + TCM cost-model inputs.
- **EVIDENCE:** CODE at `93d19c6` (call sites above; `_aligned_leg_quote_fetch` referenced only by `_make_entry_quote_fetch_fn`) + SUPABASE/ledger (the divergence class demonstrably fires on this exact feed — 06-10 16:30Z XLE, 3 cohort rejects) + RAILWAY (worker running `93d19c6`). No close-side incident observed yet (zero close orders in window) — impact is prospective, hence LOW-MEDIUM.
- **HOW:** reuse `_aligned_leg_quote_fetch` at `:645` and `:1179` (observe-first acceptable: log divergence WARNING before switching the fill source); never weakens a gate — widens the evidence basis exactly as #1052 did for entries.
- **Exclusions checked:** #1052 is ledgered ENTRY-side only; this surface was explicitly queued by the ledger's nightly-audit item (a check, whose outcome is this finding). Not in #1038–#1049, not backlog-ticketed.

### N2 — Stage-time skip stamping gap: symbol-dedup + min-edge/min-score skips bypass `_stamp_blocked_reason` — severity LOW — **FINDING** (mechanism code-certain; 06-10 NFLX attribution HYPOTHESIS)

- **WHAT/WHERE:** the #1051 rider stamps only `symbol_cooldown`/`entry_utilization_blocked`/`utilization_gate_error`/`entry_quote_unpriceable` (`paper_autopilot_service.py:850-894`). The per-cohort symbol-dedup skip (`:746-770`) and the user-level dedup/min-edge/min-score filters (`:390-427`) `continue` silently at logger.INFO — suggestions stay `pending`/`blocked_reason NULL` and are swept (the exact "swept-as-stale" gap the rider was built to close). Observed: both 06-10 NFLX forks (aggressive `ff1f65b7…`, neutral `2c1d7f79…`) unprocessed, unstamped; dedup is the high-probability cause (both cohort books hold NFLX) but the INFO line is unretrievable.
- **IMPACT:** on the first 2-candidate day, 2 of 5 cohort-fork negatives (40%) were invisible-by-design to the funnel, the stamps, AND the Area 8 counterfactual surface. Zero trading impact.
- **HOW:** add `_stamp_blocked_reason(sid, "symbol_already_held" | "edge_below_minimum_at_stage" | "below_min_score", …)` at the three skip sites (write-path only, fail-soft like the existing helper).
- **Exclusions checked:** refines the ledgered "dismissed-status funnel gap" + #1051 rider, and changes the recommended action (extend stamps to three new sites) — passes the refinement rule.

---

## 7. TOP-3 · conflicts · four-source disagreements

1. **Watch today's 16:00Z/16:30Z cycle** (not a code action): first honest-EV scan + first #1052/#1051-rider exercise — table rows 1, 2, 7. The single highest-information event since the FULL run.
2. **N1** — port the aligned quote fetch to `:645`/`:1179` (shadow-fill integrity; pairs with the ledgered A3 live/shadow work).
3. **N2** — three one-line stamp calls (closes the last silent stage-time skip class; feeds Area 8).

**Conflicts/synergies:** N1+N2 are both additive observability/integrity; neither touches live close/stop paths. N2 should land before any counterfactual-marker work (A8) so skips are attributable. No conflicts with held PR #1053 identified (not inspected — held by design).

**Four-source disagreements:** none new. Broker-vs-DB mark class (#1022) not re-checked this run (no DB mark reads taken; broker authoritative numbers used throughout). Equity read $2,207.85 vs last_equity $2,263.85 is pre-market mark-method noise, not a divergence finding.

---

## 8. Ledger updates made

- Appended **2 findings** to `audit/ledger.md` under new section `status:reported — 2026-06-11 NIGHTLY run`: N1 (close-side Polygon-only quote reads) and N2 (stage-time skip stamping gap, labeled refinement).
- Everything else in the ledger left untouched per tonight's instruction (pending-verification outcomes are recorded in §3 above for the human to fold in; rows 1, 2, 4, 5, 6, 7 remain open and fire later today: 10:00Z calibration, 16:00Z scan, 16:30Z executor, 16:45Z IPO monitor; SPCX lists 06-12).

**STOP.** Recommendations only; no production surface was touched. The only writes: this report + the ledger appends.

---

## AREA 8 SUPPLEMENT (full protocol, run 2026-06-11)

- **Run:** 2026-06-11 06:39Z (pre-market; supplements the 06:17Z nightly, whose §5 under-ran the Area-8 charter — per the v5 NIGHTLY exception, Area 8 runs its COMPLETE protocol every run). READ-ONLY honored: SELECT-only SQL, broker `get_*` only; writes = this section + an `audit/area8.md` footnote extension. No ledger append (no new finding — see §S7).
- **Code basis:** origin/main HEAD still `93d19c6` (git ls-remote, this run); working-tree `paper_autopilot_service.py` verified IDENTICAL to `93d19c6` (empty `git diff --stat 93d19c6 -- …`), so all file:line cites below are against deployed code. The 06-11 production cycles (10:00Z calibration, 16:00Z scan, 16:30Z executor) have NOT yet fired at run time (zero 06-11 `job_runs` rows for those jobs) — day-over-day below is 06-10 vs 06-09, complete days.

### S1 — Charter half 1: blind-spot re-evaluation → **LENS KEPT**

Negative-Decision Efficacy remains the single highest-value lens Areas 1–7 structurally cannot see, and the 06-09→06-10 ship wave *raised* its value rather than eroding it. Candidates considered honestly: (a) **execution-quality/slippage lens** — already inside Area 1's charter ("per-leg pricing vs combo quotes; fill-rate vs price aggression", `audit/v5-prompt.md:125-127`); fills, cancels, and staged limits are recorded data, so Areas 1/5 can reach it — thin (N≈5 live fills), not blind. (b) **Regime-shift detection lens** — regime labels are persisted per cycle (`cycle_metadata.regime`) and ex-post label-vs-market correctness is reconstructable from existing bars + labels; that is a *deeper* Area 1/2 question, not a missing lens, and at a 1-position book its dollar consequence is small. (c) **Capital-efficiency-over-time lens** — jointly owned by Areas 5/6/7; the 06-10 A6 finding (book-blind budget stack) proves that surface is already auditable. Against these, negative decisions remain the only evidence class with **zero outcome measurement anywhere** — and this week made the gap binding: #1051's honest EV makes the edge gate strictly stricter (more negatives, fewer survivors); the CALIBRATION_EV_EPOCH makes the learning restart **survivors-only from an empty blob** (survivorship bias now gates the relearn, §S5); and #1052 was itself a wrong-negative bug (3 false rejects) caught by code-reading, not by data — detection latency is still code-audit-bound, exactly the lens's thesis. Hard boundary honored: nothing below proposes loosening any gate, expanding audit write permissions, or touching the v5 contract.

### S2 — Look-at 1: negative-decision inventory, day-over-day (SUPABASE)

| Reason (suggestion_rejections) | 06-09 | 06-10 |
|---|---|---|
| spread_too_wide_real | 50 | 39 |
| no_fallback_strategies_available | 48 | 42 |
| strategy_hold_no_candidates | 25 | 25 |
| all_strategies_rejected | 23 | 19 |
| execution_cost_exceeds_ev | 7 | 9 |
| spread_too_wide | 5 | 1 |
| condor_* (no_viable/no_deltas/ev_not_computed) | 7 | 7 |
| insufficient_history (SPCX pre-listing, designed) | 0 | 1 |
| **Total** | **165** | **143** |

- `trade_suggestions` negatives: 06-09 → 3 `dismissed`; 06-10 → **5 `pending` / blocked_reason NULL** (3 XLE quote-rejects + 2 NFLX skips; still unswept at run time — morning sweep ~13:00Z will flip them `dismissed`, the ledgered funnel-gap behavior).
- Executor blocks in `job_runs.result`: 06-10 16:30Z `paper_auto_execute` → `status:partial`, `executed_count:0`, 3 errors `entry_quote_unpriceable: leg=O:XLE260717C00058000 quote={bid:0, ask:0}` with suggestion_ids (conservative `043d55ea`, neutral `f5f91d27`, aggressive `c9885b85`); `total_processed:0` — the 2 NFLX forks left **no trace at all** in the result payload (skipped pre-processing).
- 30d totals: **2,504** rejections + **23** negative-status suggestions = **2,527 unmeasured negatives vs 6 measured closes ≈ 421:1** (baseline 06-10: ~2,384 / ~400:1 — worsened, as the lens predicts under honest-EV).

### S3 — Look-at 2: reconstructability drift — **NO DRIFT (0% / 100% unchanged)**

- `capture_version` still **d8_v1** — located at the NESTED path `spread_debug.spread_debug.capture_version` (a top-level `->>'capture_version'` probe returns NULL for all rows; baseline text should be read with this path). 06-09: 55/68 debug rows d8_v1 with `per_leg`; 06-10: 40/55. Per-leg carries strike/side/premium/bid/ask, **expiry on 0 of 95 d8_v1 rows** → scanner rejects remain **0% repriceable**.
- The non-d8_v1 debug rows (13 + 15) are condor diagnostics (e.g. EEM 06-10 16:00:33Z) carrying `tried_expiries` lists but no chosen combo → also not repriceable. 88/143 of 06-10 rejections carry no structure at all (no-candidate classes: no_fallback, strategy_hold — nothing to reprice by nature).
- Dismissed/blocked `trade_suggestions`: **5/5 (100%) repriceable** via `order_json.legs` full OCC symbols + qty + side + limit — demonstrated by §S4, which repriced all five.

### S4 — Look-at 3: HAND-MARKED COUNTERFACTUALS (ALPACA, OPRA NBBO t=2026-06-10 19:59:58-59Z)

Quotes (run is pre-market, so "now" = last NBBO at the 06-10 close): XLE 26-07-17 C58 **2.09×2.17** (mid 2.13) / C62 **0.70×0.73** (mid 0.715); NFLX 26-07-10 P86 **4.75×5.38** (mid 5.065 — 63¢ wide) / P79 **1.50×1.60** (mid 1.55). Spread values: XLE bull call = **1.415 mid / 1.36 executable** (sell C58@bid, buy C62@ask) vs staged limit 1.53; NFLX bear put = **3.515 mid / 3.15 executable** vs staged limit 3.60.

| Fork (suggestion_id) | Cohort | Structure | Qty | Limit | Δ$/ct mid | Δ$/ct exec | Δ$ mid | Δ$ exec |
|---|---|---|---|---|---|---|---|---|
| `c9885b85` (#1038-rejected) | aggressive (LIVE) | XLE 7/17 C58/C62 | 2 | 1.53 | −11.50 | −17.00 | **−$23.00** | **−$34.00** |
| `043d55ea` (#1038-rejected) | conservative | XLE 7/17 C58/C62 | 7 | 1.53 | −11.50 | −17.00 | −$80.50 | −$119.00 |
| `f5f91d27` (#1038-rejected) | neutral | XLE 7/17 C58/C62 | 16 | 1.53 | −11.50 | −17.00 | −$184.00 | −$272.00 |
| `ff1f65b7` (dedup-skipped) | aggressive (LIVE) | NFLX 7/10 P86/P79 | 1 | 3.60 | −8.50 | −45.00 | **−$8.50** | **−$45.00** |
| `2c1d7f79` (dedup-skipped) | neutral | NFLX 7/10 P86/P79 | 6 | 3.60 | −8.50 | −45.00 | −$51.00 | −$270.00 |
| **TOTAL (5 forks)** | | | | | | | **−$347.00** | **−$740.00** |

**Every 06-10 negative decision was same-day-favorable**: had all five staged orders filled at their limits, the books would mark **−$347 (mid bound) to −$740 (liquidation bound)** worse at the 06-10 close. Live-capital share (aggressive forks only): −$31.50 mid / −$79 exec avoided. Comparison to what the book actually held: the live NFLX 1ct (the *identical* structure the dedup skip declined to double) moved ≈ **−$8.50/ct mid** over the same window (stage price 3.60 → close mid 3.515) and stands at **−$43 unrealized** from its 3.65 entry at broker last-trade marks (−$13.50 mid-basis / −$50 exec-basis) — the skip avoided doubling a position that itself lost over the window. Contrast 06-09's counterfactual (blocked XLF: **missed +$32..$66**): the first two-sided dataset — **counterfactual sign varies day to day; the net is unmeasurable without systematic marking**, which is precisely the ledgered A8 recommendation (unchanged).

**Fill-realism caveats (explicit):** (1) all five were DAY limits at modeled mid — live history is instant-or-never (06-04 filled in 32ms; 06-03 never filled, watchdog-cancelled), so the avoided losses are conditional on fills that may never have occurred; (2) at rejection time (16:30Z) OPRA quoted XLE C58 2.15×2.39 — a 1.53 combo fill required C62 ≈ 0.74 simultaneously (plausible, unverified; no 16:30Z C62 quote pulled); (3) marks are close-NBBO on thin contracts (P86 63¢ wide makes the exec bound punitive); (4) point-in-time: 37/29 DTE remain — an EOD mark is not an outcome verdict; per the spec's disqualifier, **no gate-quality verdict is issued from this N**.

### S5 — Look-at 5: survivorship exposure under raw-mode + post-epoch training

- Post-epoch (`CALIBRATION_EV_EPOCH=2026-06-11`) training rows: **0** (`learning_feedback_loops` trade_closed since 06-09: 0). Calibration blob empty (raw mode) since 06-10 20:27:34Z.
- **Reachable near-term training population:** NFLX LONG_PUT_DEBIT_SPREAD closes only — live 1ct + shadow forks (conservative 3ct, neutral 6ct). The first post-epoch blob therefore risks being **single-symbol, single-strategy, single-direction, and majority simulator-filled** (shadow closes feed learning; calibration is cohort-unscoped — ledgered A3).
- **Structurally absent classes (cannot reach training while their gates hold):** IRON_CONDOR (regime-gated; no CHOP since 03-17) and credit spreads (zero rows in the 60d training set — it is 100% LONG_CALL/LONG_PUT debit, n=13/5); the `spread_too_wide_real` class (~39-50 kills/day, cost-avoidance by design); the >$60 H7-dropped class (`h7_prefilter_dropped:1` on 06-10); symbol-held add-ons (dedup); and — new this week — **every debit candidate the honest-EV gate now kills** that the inflated PoP previously admitted. Net statement: the post-epoch learning feed is bounded by {3 open NFLX bear-put books} ∪ {future honest-EV survivors, possibly ∅}. If today's 16:00Z honest-EV cycle admits nothing, raw mode persists indefinitely — **designed** (the ~06-20 `calibration_stale` alert is the reminder, not a defect; explicitly not re-found).

### S6 — Look-at 6: surface integrity — stamp coverage delta + N2 size (do-not-re-find honored)

- **Stamping paths (code at `93d19c6`, re-verified this run):** `_stamp_blocked_reason` (def `paper_autopilot_service.py:591`) is called for exactly 4 reasons — `symbol_cooldown` (:850), `entry_utilization_blocked` (:860), `utilization_gate_error` (:872), `entry_quote_unpriceable` (:894). **Still-unstamped negative paths:** already-executed dedup (:390-392), symbol-already-held (:396-401), min-edge (:409-415), below-min-score (:425-428), per-cohort symbol dedup (:766-770ff) — all silent `continue` at INFO.
- **Coverage delta measured:** since the 06-10 baseline, **5/5 executor-stage negatives carry blocked_reason NULL**. Decomposition: 3 (XLE quote class) are a deploy-timing artifact — rejected 16:30Z, stamping rider deployed 20:39Z; that class WOULD stamp today (first live test = today's 16:30Z, pending-verifications row 2). **True N2-class silent rows since 06-10: 2 (the NFLX dedup skips) = 40% of the day's executor-stage negatives** — these would still be NULL under today's code. Size measured per charter; N2 remains the ledgered finding, not re-found.
- `outcomes_log`: **0 rows all-time** (unchanged; #67 deletion-consistent). `policy_decisions` 30d: rejected **2**, rejected-with-outcome **1** (baseline 1/30d, unchanged); accepted 25 / 5 with outcome.

### S7 — Look-at 4 + findings determination

- **Gate-efficacy distribution (look-at 4): STILL ABSENT** — no counterfactual marker exists, so per-gate reject-vs-accept P&L remains uncomputable; this run's §S4 is the manual stand-in. The ledgered A8 recommendation (expiry in capture + nightly read-only marker on the 100%-repriceable suggestion class + per-gate metric with ≥3-window info alert) is unchanged and is the prerequisite for ever running look-at 4 systematically.
- **No new ledger-grade finding this run.** Everything observed maps to ledgered items (A8 baseline, N1, N2, funnel gap, #67) or to designed behavior (raw mode, SPCX, deploy-timing NULLs); the counterfactual table is evidence under the existing lens, not a finding (N=1-day disqualifier). Per the v5 contract, an invented finding is worse than an empty slot.
- **Writes this supplement made:** this section; `audit/area8.md` first-audit footnote extended with 2026-06-11 numbers (ratio 421:1, two-sided counterfactual, nested capture_version path). Ledger untouched.
