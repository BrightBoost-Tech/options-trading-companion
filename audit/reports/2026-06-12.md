# AUDIT v5 — NIGHTLY — 2026-06-12

- **Run:** NIGHTLY, ~2026-06-12 06:00Z (01:00 CT). READ-ONLY honored: SELECT-only SQL, Railway/log reads only; writes = this report + `audit/ledger.md` appends + `audit/area8.md` footnote.
- **Baseline:** `audit/reports/2026-06-11.md` + `audit/ledger.md`. Window: 06-11 06:17Z → 06-12 06:00Z (covers the full 06-11 incident-arc session, the 5-PR ship wave, and a quiet overnight).
- **No ALERT file written.** The five CRITICAL `risk_alerts` in the window (QQQ broker reject 14:25Z; phantom-daily force_close NFLX/QQQ/SPY 16:30Z; QQQ cohort-stop force_close 19:00Z) are the operator-documented 06-11 incident arc, fully ledgered same-day (#1055–#1058). Zero criticals of any kind after 19:45Z; overnight clean.
- **⚠ TOOLING GAP THIS RUN: the Alpaca MCP servers (live + paper) never connected; broker snapshot (pre-audit step 3) could NOT be taken.** Repeated ToolSearch attempts returned nothing. Broker-truth statements below are reconstructed from DB rows-of-record, worker logs, and the ledger's same-day broker numbers, and are labeled as such. First item for the morning: eyeball Alpaca directly (equity, the two resting-or-not condors, NFLX).

---

## 2. Ground-truth delta vs 2026-06-11

- **Code/worker (H8): worker = `5681919` (#1059) = origin/main HEAD** (git ls-remote via subagent this run), deploy `9d6ee5c4` SUCCESS 21:52:35Z, container fresh since then. Ten worker deploys in the window — incl. TWO mid-RTH (16:13/16:16Z, `a3a7403` #1055, operator-sanctioned incident response) and the forced recycles that resolved the ledger's "Railway missed bcbfb0c" flag (#1057 deployed 20:14–20:18Z). `GATE_DEPLOY_PARITY=5681919-2026-06-11T21:25Z` set on worker env.
- **Deploy anomaly (resolved, hypothesis on cause):** Railway shows a deploy of commit `6a07c97d` at 21:31:53Z, replaced by a re-deploy of `5681919` at 21:52:35Z. `6a07c97d` does not exist in the local repo, is not on origin/main, and is on no branch — Railway ran a commit git no longer has, for ~21 minutes. The off-cadence `ops_health_check` 21:38:14Z (the successful synthetic delivery proof, below) executed ON that container. HYPOTHESIS: a temporary synthetic-proof-wiring commit was pushed, used for the proof, then rolled back; the clean re-do is the local unpushed branch `n2-synthetic-proof-wiring` (`9c957d9`, "v5-A4 proof was unreachable — 422 extra_forbidden + payload rebuild drop"). Final state is coherent (worker == origin/main); flagged as a four-source disagreement, not a finding.
- **Flags (worker env, read this run):** `RISK_UTILIZATION_GATE_ENABLED=1`, `RISK_MAX_UTILIZATION_PCT=0.85`, `RISK_ENVELOPE_ENFORCE=1`, `RISK_MAX_DAILY_LOSS=.08`, `RISK_MAX_SYMBOL_LOSS=.03`, `RISK_MAX_WEEKLY_LOSS=.1`, `EXECUTION_MODE=alpaca_live`, `LIVE_ENABLED=TRUE`, `POLICY_LAB_ENABLED=true`, `SCHEDULER_ENABLED=1`, `CALIBRATION_ENABLED=1`, `TASK_NONCE_PROTECTION=1`, `PDT_PROTECTION_ENABLED=0`, `MIN_EDGE_AFTER_COSTS=15`, `LIMIT_PRICE_AGGRESSION=.5`. The default-ON kill-switch set (#1038/#1040/#1042/#1045-TTL/#1046/#1048/#1052) absent from env = ON; `CALIBRATION_EV_EPOCH` absent = code default 06-11 (armed); `PRICE_CLASS_SPREAD_CUTOFF` absent = $60; `OPS_ALERT_WEBHOOK_URL` absent = webhook consciously deferred (backlog-allowed). New env vs prior run: `GATE_DEPLOY_PARITY`, `PR1055_DEPLOY_TIMESTAMP`, `PR1057_DEPLOY_TIMESTAMP`.
- **Broker (RECONSTRUCTED — Alpaca tools down):** live book per DB rows-of-record + 20:30Z truth-layer MTM: NFLX P86/P79 1ct (entry 3.65, mark 3.965, **+$31.50** — flipped positive), QQQ 07-10 iron condor 1ct (credit 1.61, close-cost mark 1.815, −$20.50), SPY 07-24 iron condor 1ct (credit 1.48, mark 1.705, −$22.50). **First-ever live condor fills** 16:15:49/51Z: QQQ −1.61 (limit 1.54, +$7 improvement), SPY −1.48 (limit 1.43, +$5) — 12/12 legs Polygon-dark, truth-layer priced (#1052 exercised, log-verified below). Equity: ledger same-day broker read 2075.42 vs last_equity 2263.85 (real −8.3% day); no fresher read available this run. Shadow: cons NFLX 3ct +$83.55, neutral NFLX 6ct +$167.10; neutral QQQ condor 7ct CLOSED −$234.78 (19:00Z cohort stop, value corrected pre-ingest).
- **Jobs:** 06-11: 182 succeeded, 0 failed, **2 stuck `running` forever** — `suggestions_open` 16:13:35Z (killed by the 16:16:53Z #1055 recycle) and `alpaca_order_sync` 20:20:01Z (killed by the #1057 recycle wave). Quantifies the backlogged deploy-drain item: 2 orphaned rows / 10 recycles / 1 day. `alpaca_order_sync` day 3 post-#1049: 1.43s avg / 4.77s max / 95 runs — holding. Overnight schedule normal (silence after 22:07Z; 06-12 `phase2_precheck` 05:00Z OK — same shape as yesterday).
- **Funnel day-over-day:** 06-10 one cycle: emitted 14 → staged 2 → executed 0 ⇒ 06-11 **three** scan cycles (14:19Z incident-session, 16:00Z scheduled, 16:13Z killed mid-run by deploy): emitted 6 + 9, suggestions 4 (all QQQ/SPY IRON_CONDORs — regime CHOP, first condor day since 03-17), executor shots **3** (14:21Z, 16:15Z, 16:30Z-blocked-by-breaker) → 6 order submissions → **3 fills** (2 live + 1 shadow), 2 broker rejects (pre-#1055 sign bug), 1 watchdog cancel (third condor, idle 249s > 90s threshold). First conversion>0 day; first multi-position live book (3 structures).
- **Rejections:** 143 → 317 (3 cycles vs 1); mix per-cycle stable; NEW reason `ev_non_positive` ×3 = condor EV on TLT/DIA (condor scanning newly active in CHOP — NOT the honest-EV debit signature; no debit candidate emitted all day).
- **reentry_cooldowns: first 2 rows EVER** (16:30:13/14Z): aggressive-cohort QQQ + SPY, reason `envelope_force_close`, until 06-12 13:30Z — written off the per-symbol envelope's `symbol_loss_stops` exactly per the documented key (NFLX, positive at the time, correctly got none; daily-breach force-closes alone don't write). Mechanically correct, but values are phantom — see Finding N3.
- **H11 (unconditional):** criticals = the 5 ledgered incident events only. HIGH: NFLX concentration warns all session (100% → 54% after the condor fills). One warn-class storm: `ghost_position` QQQ `85db73c8` ×21 (14:35→16:15Z) — the neutral SHADOW condor (internal fill, no broker legs by definition) tripping the live ghost reconciler for ~100 min until the live QQQ fill gave Alpaca matching legs; stopped on its own. Known shadow-scoping seam (memory/1b-prerequisite class), alert-noise only; folded into Area 5 note, not a finding.
- **Calibration:** 06-11 10:00Z run: 30/60/90d rungs ALL `insufficient_data` sample 0 → users_skipped 1, raw mode persists = DESIGNED (epoch bounds every window). Blob still the 06-10 20:27Z empty reset. First post-epoch outcome exists as of 21:20Z (n=1 < 8); today's 10:00Z run will still skip.
- **Learning:** `paper_learning_ingest` 21:20Z: closed_positions_found 4, **outcomes_skipped_duplicate 3, outcomes_created 1** — the #1051 position-level dedup's FIRST live exercise: PASS. The 1 created row is the corrected QQQ −234.78, `is_paper=true` (internal fill → correct), suggestion-linked. `post_trade_learning` 21:45Z consumed it (trades_processed 1). `daily_progression_eval`: no_closes/realized 0 — correct (Alpaca-fills-only rule; the only close was internal).

---

## 3. Pending-verifications table

| # | Item (ledger) | Status | Evidence |
|---|---|---|---|
| 1 | #1059 signature: ops delivery proof + synthetic row written-then-deleted | **VERIFIED (with one asterisk)** | Off-cadence `ops_health_check` 21:38:14Z result: `synthetic_delivery_test: {sent:true, severity:critical, fingerprint:4d5866f923b4e69d, risk_alert_written:TRUE, webhook_sent:false (unset=deferred), suppressed_reason:null}` — first ops delivery ever. Row absent from `risk_alerts` now (deleted as designed; zero rows of any severity post-20:00Z). Asterisk: the proof ran on the rolled-back `6a07c97d` container; the reproducible signed-endpoint trigger is the UNMERGED local branch `9c957d9` (422 extra_forbidden on main's path). Re-arm to fully-closed when that wiring (or equivalent) merges. |
| 2 | Railway missed-deploy of bcbfb0c (#1057) — H8 flag | **RESOLVED** | Forced deploys 20:14/20:15/20:18Z (bcbfb0c ×3), then 2ac071b 21:10Z, 5681919 21:20Z + 21:52Z SUCCESS. Worker SHA == origin/main HEAD (git-verified this run). |
| 3 | #1048 cohort stops first exercise | **VERIFIED** (already ✅ in ledger) | 19:00Z QQQ shadow 7-lot stop at monitor cadence; force_closes_submitted:1 in the 19:00Z monitor result. |
| 4 | reentry_cooldowns first write on next loss stop | **VERIFIED — with data caveat (Finding N3)** | 2 rows 16:30:13/14Z, aggressive QQQ/SPY, until 06-12 13:30Z. Writer keyed per spec (symbol stops only; no NFLX row, no daily-trigger row). realized_loss values are phantom-derived (−300.0/−282.5 from the #1056 signed-entry marks; actual realized $0, positions still open). |
| 5 | 06-11 16:00Z first honest-EV cycle (debit EV ~2× drop) | **STILL PENDING — not exercisable yet** | All 06-11 cycles ran on honest code, but regime=CHOP → every candidate was an IRON_CONDOR (credit math unchanged by #1051). Zero debit candidates emitted. Calibration-ratio-1.0 half VERIFIED: suggestion rows show ev==ev_raw (46.06/46.06, 35.63/35.63, 39.41/39.41 — raw mode passthrough). Debit-EV check fires on the next non-CHOP scan day. |
| 6 | 06-11 16:30Z FEED DIVERGENCE + blocked_reason stamps | **DIVERGENCE VERIFIED; stamps unexercised** | 12× `[ENTRY_QUOTE] FEED DIVERGENCE` WARNINGs in the 16:15:45–52Z worker logs (every QQQ/SPY condor leg: Polygon 0/0, truth-layer priced, "the 06-10 XLE class") — #1052 working exactly as designed on the first live test. No stage-time rejection occurred in the window → stamp paths still unexercised. |
| 7 | First post-epoch calibration write behavior | **VERIFIED** | 06-11 10:00Z `calibration_update`: attempts 30/60/90d all `insufficient_data sample_size 0`, users_skipped 1, last_write_age_days 0.6 — epoch excludes all pre-fix outcomes; raw mode persists; no false stale alert. |
| 8 | 06-12 first credit fill row: avg_entry_price/max_credit POSITIVE (#1056 write-side proof) | **PENDING — fires on the next credit fill** | Note: the 06-11 16:15Z fills happened on PRE-#1056 code (merged 20:03Z) and were hand-corrected (+1.61/+1.48, ledgered). DB now shows positive entries on both live condors. The unassisted write-side proof needs a NEW credit fill on ≥faa425d code. |
| 9 | 06-12 closes: positive close limits, no Sign-incoherent raise; double-submission persists until the single-submitter fix | **PENDING — today's session** | No close order since 19:00Z 06-11. Every live close until the queued 06-12 fix double-submits at the broker (documented trace, `docs/double_submit_close_trace.md`). |
| 10 | #1058 signature: `[EQUITY_STATE] realized-blind gap` WARNING | **PENDING — first armed RTH is today 13:30Z+** | #1058 live on worker since 21:10Z but every monitor tick since has been market_closed (checks:0). Today is the first session where the daily brake reads broker equity−last_equity. |
| 11 | PR #908 live credit-mleg-close validation | **STILL PENDING** | All three 06-11 live close attempts terminated unfilled (2 watchdog, 1 broker-cancel) on pre-#1056 signed limits. Candidates now: any of the 3 open live structures. |
| 12 | CLOSE-side Polygon-only reads (N1) / stamp-gap (N2) | **OPEN, unchanged** | No code change in window (the 5 merged PRs don't touch those sites). N2 class recurred silently: 16:00Z fork map `{cons:0, neutral:0, aggr:1}` — neutral skipped the third QQQ condor (holds QQQ — dedup, HYPOTHESIS) and conservative forked 0 in both cycles; all unstamped, INFO-invisible. |

---

## 4. Areas 1–7

**AREA 1 — PROFITS: signal MOVED (first fill-quality dataset); NO NEW FINDING.** First live multi-fill day: 2/3 live entry submissions filled, both with price improvement (QQQ +$7, SPY +$5 vs limit); the third (f49a50ba, QQQ 07-17 condor, limit 1.28 vs ~1.21 mid at submission) rested 249s → watchdog-cancelled — consistent with the documented instant-or-never live fill shape (06-03/06-04 precedent). Honest-EV debit verification still pending (row 5). Condor EV model (`CONDOR_EV_MODEL=tail`) produced its first staged-and-filled candidates; realized verdicts pend closes. Nothing actionable beyond ledgered items at N=3.

**AREA 2 — MINIMIZING LOSSES: signal MOVED (incident arc) — all root causes ledgered same-day (#1055/#1056/#1057/#1058); one NEW residue finding (N3, below).** Worth recording how the stack behaved given corrupted input: phantom −22.8% daily (signed-entry marks) → monitor force-closed the whole book AND the breaker blocked the 16:30Z executor (correct dual response); per-symbol stops fired only on the two phantom-loss symbols (QQQ/SPY → cooldown writes), not NFLX (actually positive) — selective exactly per spec; the failed (mis-signed) close orders were watchdog-cancelled and NOT blindly retried after the operator corrected the marks ~90s later. The control topology worked; the INPUT was the bug, and #1056/#1058 closed it. First armed RTH for the broker-true daily brake is today (row 10).

**AREA 3 — SELF-LEARNING: signal MOVED — two verifications PASSED, one small NEW finding (N4).** #1051 dedup first exercise PASS (3 dup-skips/1 create); first post-epoch training row is correct-valued, correctly `is_paper=true`, ingested 2h20m after the operator's value correction (sequencing held). Post-epoch training set composition (Area 8 §S5): n=1, IRON_CONDOR, shadow internal fill — the 06-11 supplement predicted NFLX-debit-first; the regime flip made it condor-shadow-first instead. Cohort-unscoped calibration (ledgered A3) means simulator fills will train the live predictor when n≥8 — unchanged, watch.

**AREA 4 — SELF-SUSTAINING: signal MOVED — the lifetime-0% ops delivery is BROKEN (proof landed); residual = reproducibility.** `risk_alert_written:true` at 21:38:14Z is the first delivered ops alert in 916+ runs (verification row 1). data_stale behavior now: detections at 21:07/22:07Z (post-close) produced NO alert = the #1059 market-hours gate working; the chronic "Last successful job > 30 min" daytime false positive (13:07–20:07Z, every run) was detected-but-unsent on pre-#1059 code — TODAY's :07 runs during RTH are the first real-delivery test, and if the 30-min-threshold false positive still fires it will now actually PAGE (the threshold fix was supposed to land with #1059 — worth the operator confirming which threshold is live before 14:07Z). Recycle-orphaned `running` job rows ×2 quantify the backlogged drain item. The audit's own scheduler ran tonight (this report exists) — last run's meta-miss did not recur.

**AREA 5 — EFFICIENCY: UNCHANGED-IMPROVED.** order_sync 1.43s day 3 (−78% vs #1049 baseline, holding). Scans 36.9s/29.9s; monitor 2–3s steady, 11.3s on the 3-force-close tick (acceptable). New noise source logged: 21 ghost_position warns/100min from the shadow-condor scoping seam — alert-noise feedstock for the ledgered A4/A5 noise item, goes away if order_sync ghost detection scopes to live-routed ids (`position_scope` pattern, same fix family as N1).

**AREA 6 — TRADING VOLUME: signal MOVED (conversion finally >0).** Emitted 14→15 across 2 days; staged forks 5→6; fills 0→3. Two structural notes, neither a new finding: (a) the one-shot/day executor bottleneck was operationally bypassed during the incident (3 shots), and the extra shots are precisely where the fills happened — live evidence for the backlogged cadence item, no new mechanism; (b) the entry watchdog now cancels at `threshold=90s` idle (order rows verbatim) while CLAUDE.md §6 still says "~5min" — doc drift to fix, and a tighter bound on fill-or-kill behavior than the docs imply (third condor lost to it). Cooldown benches (QQQ/SPY) expire 13:30Z today — before the 16:30Z executor, so they will never practically bind; consistent with #1040's next-session-open spec on a once-daily cadence.

**AREA 7 — TIME IN TRADE: signal MOVED (first same-day round trip).** Shadow QQQ condor: open 14:21Z → cohort-stop close 19:00Z = 4.6h hold, at monitor cadence (#1048 working as designed). NFLX (live) day 4: recovered from −$43 to +$31.50 — the documented stall-corridor position is now a winner-in-corridor (flat TP +$182.50 still far; 7-DTE dump 07-03). No new mechanism.

---

## 5. AREA 8 — SELF-EXTENSION (full protocol, every run)

**Spec re-evaluation → LENS KEPT** (Negative-Decision Efficacy, `audit/area8.md`). Candidates re-considered against this window: execution-quality (the new fill/cancel data is Area 1-reachable — it reached it tonight), incident-response/data-correction integrity (a process lens, not a system-data lens; N3 shows the existing areas can catch residue), regime-shift (cycle metadata exists; deeper-Area-1). Negative decisions remain the only zero-measurement evidence class, and tonight's sign-flip (below) is the strongest corroboration yet.

- **S2 inventory (day-over-day):** 06-10: 143 rejections + 5 neg-status suggestions. 06-11: **317** rejections (3 cycles) + 1 executor-stage negative (watchdog-cancelled third condor) + ≥2 silent cohort skips (neutral/conservative, unstamped). 30d: **2,821 rejections vs 7 measured closes ≈ 403:1** (prior 421:1 — closes 6→7 helped more than rejects grew).
- **S3 reconstructability: NO DRIFT.** 06-11: 176 debug-bearing rejection rows, 68 d8_v1, **0 with expiry** → scanner rejects still 0% repriceable. Condor diagnostics (incl. all 3 `ev_non_positive`) carry `tried_expiries` but no chosen combo — also not repriceable. The 4 suggestions: 4/4 repriceable via order_json (100%, unchanged).
- **S4 hand-marked counterfactuals — the 06-10 set SIGN-FLIPPED in 24h.** The two dedup-skipped NFLX forks, marked −$8.50/ct (mid) on 06-10 close, are at the 06-11 20:30Z truth-layer mark 3.965 vs limit 3.60 → **+$36.50/ct: aggressive +$36.50, neutral +$219 would-be-gains foregone** — yesterday's "avoided losses." Same instruments, one day apart, opposite verdicts: this is the cleanest demonstration yet that a single-day counterfactual mark is noise and only a systematic marker (the standing A8 recommendation) can integrate it. XLE forks: NOT remarkable tonight — no broker/quote tooling this session (limitation stated; first marker-design constraint: the marker must not depend on session-local tooling). 06-11's one entry-negative (third QQQ condor, credit 1.28): no quote available; its filled 07-10 sibling marked −$20.5/ct against by 19:45Z (cancel plausibly avoided a marked-against position — HYPOTHESIS, N=1, no verdict per spec).
- **S5 survivorship under the epoch:** post-epoch training set = 1 row: IRON_CONDOR, **shadow internal fill**, −234.78. If condor-CHOP days continue, the first post-epoch blob will be condor- and simulator-fill-weighted while live-fill debit classes stay absent — the inverse of the 06-11 prediction (NFLX-debit-first), proving the composition is regime-hostage. Cohort-unscoped calibration remains the ledgered exposure.
- **S6 surface integrity:** stamp coverage unexercised (no stage-time rejections); N2-class silent skips recurred (fork maps `{c:0,n:1,a:2}` / `{c:0,n:0,a:1}` — conservative 0 forks both cycles, neutral 0 in the second, no stamp, no WARNING). `outcomes_log` 0 all-time; `policy_decisions` unchanged.
- **S7 finding determination: NO new ledger-grade A8 finding** — everything maps to the standing A8 recommendation (expiry capture + systematic marker + per-gate metric), which tonight's sign-flip evidence strengthens but does not change. Hard boundary honored: nothing above proposes loosening a gate, expanding audit writes, or touching the contract.

---

## 6. New findings this run

### N3 — First-ever reentry_cooldowns rows carry phantom loss values from the 06-11 corrupted-mark event — severity LOW — **FINDING** (data residue; mechanism ledgered, rows are not)

- **WHAT:** The 2 inaugural #1040 cooldown rows (aggressive QQQ/SPY, 16:30:13/14Z) persist `realized_loss = −300.0 / −282.5` and `reason=envelope_force_close` for losses that never existed: the values are the #1056 signed-entry phantom unrealized marks (live QQQ unrealized was −$300 only because avg_entry was stored as −1.61; both positions are STILL OPEN, realized $0). The 06-11 data-correction sweep fixed `paper_positions`, `paper_orders` context, and the cohort cash balance — `reentry_cooldowns` was not touched.
- **WHERE:** `risk_envelope.py:481` (`"realized_loss": unrealized` — the field is trigger-time UNREALIZED by construction, despite the name; test `test_reentry_cooldown.py:262` confirms "trigger-time loss") → `intraday_risk_monitor.py:702` → `services/reentry_cooldown.py:223` (durable write).
- **IMPACT:** Quantified: 2 rows / 100% of all #1040 history to date are phantom-valued; benches expired-or-expiring (06-12 13:30Z, before the 16:30Z executor — zero trading impact). Risk is forward-looking: any future analytics/audit reading `realized_loss` from this table (e.g., a cooldown-efficacy study, exactly what Area 8 will eventually want) trains on fabricated dollars, and the misnomer invites it.
- **HOW (additive only):** operator data decision per the 06-11 correction precedent — annotate or correct the 2 rows (e.g., set realized_loss NULL with a note, or leave + record in the ledger that pre-06-12 rows are phantom-valued); optionally rename/comment the field at `risk_envelope.py:481` (`trigger_unrealized_loss`) next time that file is open for sanctioned work. NEVER changes gate behavior.
- **EVIDENCE:** SUPABASE (rows verbatim; positions open with realized_pl NULL) + CODE (cites above) + ledger (#1056 phantom mechanism, corrections list omitting this table). Sources agree; the finding is the uncorrected residue itself.
- **RISK:** zero blast radius on live paths (no production reader of the column found; gates read `cooldown_until` only). CONFIDENCE: high.

### N4 — `paper_positions.learning_ingested` is a dead column that the incident evidence trail treated as live — severity LOW — **FINDING** (Known-Liar class; evidentiary, not behavioral)

- **WHAT:** `learning_ingested` exists only in the migration that added it (`supabase/migrations/20260410000000_add_agent_tables.sql:101`, DEFAULT FALSE); zero code reads or writes it anywhere in the repo (repo-wide grep this run: 2 hits = migration + ledger prose). It is FALSE on every row forever — including the QQQ shadow close that HAS a learning_feedback_loops row (created 21:20Z). The 06-11 ledger entry cites "learning_ingested was false — zero contamination" as the proof the correction beat the ingest; the column was never going to be anything but false. (The conclusion was still right — the ingest creates outcomes fresh from position rows at run time, 21:20Z, after the ~19:05Z correction — but the cited mechanism is a phantom.)
- **WHERE:** migration line above; actual dedup/ingest-tracking = the suggestion-id-keyed duplicate check inside `jobs/handlers/paper_learning_ingest.py` (verified working tonight: 3 dup-skips).
- **IMPACT:** evidentiary only, but this exact pattern (trusting a column/print that doesn't compute through the real path) is the doctrine's Known-Liar class, and it reached the ledger. Anyone querying `learning_ingested` to find un-ingested closes gets "all of them, always."
- **HOW (additive):** add to CLAUDE.md §8 Known Liars + fold into the ledgered dead-instrumentation ticket (wire it in the ingest's post-write step, or drop the column in the next batched migration); amend the 06-11 ledger line so the recorded mechanism is the run-time-creation ordering, not the flag.
- **EVIDENCE:** CODE (grep, 2 hits) + SUPABASE (row false despite outcome existing) + ledger text. CONFIDENCE: high. RISK: none (no live path).

---

## 7. TOP-3 · conflicts · four-source disagreements

1. **Today 13:30Z–20:00Z is the highest-information session of the cycle** (watch, not build): first armed RTH for the #1058 broker-true daily brake (last_equity baseline = the −8.3% day) + #1057 margin-basis utilization on a 3-structure book + first unassisted #1056 write-side proof on any new credit fill + the first ops data_stale RTH delivery (confirm the 30-min threshold fix is actually in #1059 before 14:07Z, else the first delivered ops alert may be the known false positive) + SPCX lists today (gates decide; 16:45Z monitor). Also queued by the operator: the single-submitter close fix — until merged, any live close double-submits.
2. **Land the synthetic-proof wiring durably** (`n2-synthetic-proof-wiring` / `9c957d9` or equivalent): the v5-A4 proof currently exists only as a one-off on a deleted commit; merging makes the ops-delivery proof reproducible and closes verification row 1's asterisk. (Effort: small; the branch exists.)
3. **N3 + N4 cleanups** (operator decisions, minutes each): annotate/correct the 2 phantom cooldown rows; add `learning_ingested` to Known Liars and the dead-fields ticket. Both protect future evidence quality; neither touches a gate.

**Conflicts/synergies:** none conflicting. The ghost_position shadow-scoping noise (A5) and N1 (close-side aligned quotes) share the position_scope/live-routed pattern — natural same-PR pair. N3's field-rename rides whatever next touches risk_envelope.py under sanctioned work; do not open that file for this alone.

**Four-source disagreements encountered:** (1) **ALPACA source absent this run** (MCP servers never connected) — broker-truth items reconstructed and labeled; nothing asserted on broker authority tonight. (2) RAILWAY ran commit `6a07c97d` for 21 min that GIT no longer contains (resolved by rollback; hypothesis documented). (3) CLAUDE.md "watchdog cancels idle DAY limits ~5min" vs order-row truth `threshold=90s` (cancel lands at the next ~5-min sync pass) — doc drift, update §6 wording. (4) `learning_ingested` (N4): ledger prose vs code reality.

---

## 8. Ledger updates made

- Appended findings **N3** and **N4** under a new `status:reported — 2026-06-12 NIGHTLY run` section.
- Updated the 06-11-night pending-verifications list: #1059 proof → VERIFIED (asterisk: unmerged wiring); Railway/bcbfb0c → RESOLVED; #1048 + reentry first-write + first post-epoch calibration + FEED DIVERGENCE → VERIFIED (with N3/stamps caveats); honest-EV debit check, #1056 write-side proof, close-sign/double-submit, #1058 signature, PR #908 → carried (today's session).
- Extended `audit/area8.md` footnote with run-3 numbers (403:1; 06-10 counterfactual sign-flip +$36.50/ct; tooling-independence constraint for the future marker).

**STOP.** Recommendations only; no production surface touched. Writes: this report, `audit/ledger.md`, `audit/area8.md`.
