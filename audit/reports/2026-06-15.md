# AUDIT v5 — NIGHTLY — 2026-06-15

- **Run:** NIGHTLY (Monday; FULL is Sundays only). Started ~2026-06-15 05:03Z (00:03 CT), pre-market (market opens 13:30Z today). READ-ONLY honored: SELECT-only SQL, Railway/Alpaca read tools only; the only writes are this report + `audit/ledger.md` + `audit/area8.md`.
- **Baseline:** newest report file `audit/reports/2026-06-12.md` + the `audit/ledger.md` **2026-06-13 combined-run** section (which captured the 06-12 RTH session and week verdict) + `docs/backlog.md`. Effective prior run = the 06-13 combined run.
- **Window:** 06-13 (after the combined run) → 06-15 05:03Z. Spans a **quiet weekend** (markets closed Sat 06-13 / Sun 06-14); the last trading session was Fri 06-12, already covered by the 06-13 run. No new trading activity in-window.
- **No ALERT file written.** `risk_alerts` critical/high since 06-13 00:00Z = **ZERO of any severity** (H11 baseline clean). The ghost_position shadow-sweep seam did not fire — monitor/order_sync don't run on weekends.
- **All four sources reachable this run** (Alpaca MCP up, unlike the 06-12 tooling gap): broker snapshot taken directly.
- **Note on report cadence:** no automated report file exists for 06-13 (Sat) or 06-14 (Sun). 06-13 was an operator combined run (folded to the ledger/backlog/CLAUDE.md #1065); the Sunday FULL did not produce a file — most likely the local Windows Task Scheduler host was offline over the weekend (a known fragility of local scheduling, not a production-system defect). The loop self-recovered tonight. Logged as an Area-4 observation, not a finding.

---

## 2. Ground-truth delta vs prior run (06-13)

- **Code/worker (H8): both workers on `5778760` (#1065 CLAUDE.md refresh + backlog reorg).** worker deploy `f725b945` SUCCESS 2026-06-13 03:15:47Z; worker-background `6126ffe0` SUCCESS 2026-06-13 03:15:47Z — **dual-worker parity, recycled together.** This is the 06-13-pending CLAUDE.md-refresh merge, now landed (the prior SHA 776a363/#1064 is REMOVED). origin/main (last fetched) == deployed == local HEAD = `57787605…`. No deploys over the weekend — no-RTH-merge + weekend-silence doctrine respected. Worker demonstrably alive: RQ `otc` heartbeat every ~13min, no errors in deploy logs. (`git ls-remote` could not be run — Bash tool not enabled this context; H8 rests on the cached origin/main + the Railway dual-deploy listing + weekend merge-silence, which is conclusive.)
- **Flags (worker env, read this run): clean, no regressions, all match CLAUDE.md §4.** `EXECUTION_MODE=alpaca_live`, `LIVE_ENABLED=TRUE`, `PDT_PROTECTION_ENABLED=0`, `RISK_ENVELOPE_ENFORCE=1`, `RISK_UTILIZATION_GATE_ENABLED=1`, `RISK_MAX_UTILIZATION_PCT=0.85`, `SCHEDULER_ENABLED=1`, `CALIBRATION_ENABLED=1`, `TASK_NONCE_PROTECTION=1`, `MIN_EDGE_AFTER_COSTS=15`. New/notable: **`GTC_PROFIT_EXIT_ENABLED=1` + `GTC_PROFIT_EXIT_PILOT_POSITION_IDS=6798e58f…`** (= the live QQQ condor — #1064 resting-TP pilot live, scope correct); **`EXIT_MARK_SANITY_ENFORCE_ENABLED=1`** + observe=1 (#1034 Stage-2 armed). Default-ON kill switches (#1038/#1040/#1042/#1045-TTL/#1046/#1048/#1052/CLOSE_REARM) absent-from-env = ON. `PRICE_CLASS_SPREAD_CUTOFF` absent = $60; `CALIBRATION_EV_EPOCH` absent = code default 06-11. Stale informational var: `GATE_DEPLOY_PARITY=5681919-…` (last set #1059, not the running SHA — marker only).
- **Broker (Alpaca LIVE 211900084, direct read):** equity **$2,179.15**, last_equity $2,197.15 (pre-open drift −$18, balance_asof 06-12), cash $2,385.15, **OBP $1,804.15** (deployable). Open: a **single live structure — QQQ 07-10 iron condor** (1ct, credit 1.61; legs C750/C755 + P640/P645; net cost basis −$161). Broker unrealized **−$45** (market_value net −206). All 4 legs `qty_available:0` = committed to the resting closing order. NFLX and SPY (in the 06-12 report) are **gone** — closed during the 06-12 RTH. Utilization ~28-32% (initial_margin $581 / OBP $1,804) — well under the 85% cap.
- **Resting TP `550fccc5` ALIVE & correct:** GTC mleg, limit **0.81** buy-to-close (≈50% of the 1.61 credit = aggressive-cohort flat TP), status `accepted` since 06-13 00:28:44Z, untouched by the watchdog (intentional_resting_exit exempt). Condor close-cost is ~2.06 (broker) / 1.77 (DB) — far above the 0.81 target, so correctly unfilled. The broker-resting GTC is immune to DB-mark phantoms by construction.
- **Jobs (since 06-13 00:00Z): weekend silence as designed.** Only two: `paper_exit_evaluate` succeeded 06-13 00:28:38Z (5.7s — the off-schedule run that placed the resting TP), and `phase2_precheck` succeeded 06-15 05:00:02Z (4.15s, Monday 00:00 CT). **Zero failures, zero stuck `running` rows.** Monday 05:00-CT calibration (11:00Z) and the 16:30Z executor have not run yet.
- **Funnel day-over-day:** 06-11: 317 rejections / 4 suggestions (dismissed). 06-12: 298 rejections (11 reasons) / 2 suggestions (still `pending` — the ledgered dismissed-status funnel gap; today's morning sweep will relabel). 06-13/14/15: zero (weekend, no scans). 06-12 rejection mix is **all real**: no_fallback 83, spread_too_wide_real 78, all_strategies_rejected 42, execution_cost_exceeds_ev 42, condor_no_viable 30, condor_ev_not_computed 16 — no feed artifacts (#1052 holding).
- **reentry_cooldowns:** one NEW active row — **SPY** (aggressive cohort `3d289dca`), reason `cohort_stop_force_close_manual_bench_0612`, `realized_loss=−45.00` (the REAL SPY loss, correctly valued — contrast the N3 06-11 phantoms), `cooldown_until=2026-06-15 13:30Z` (expires at today's open). Operator-authored manual bench after the 06-12 SPY cohort-stop close (see Pending-Verif row 7 — the auto-cooldown path shipped same-day but post-this-event). The two 06-11 phantom rows (−300/−282.5) are expired.
- **Learning:** all 5 closes from the 06-11/06-12 sessions ingested to `learning_trade_outcomes_v3`; **NFLX LPD pair sums +181.35 = corrected 133.35 (conservative shadow) + 48.00 (live)** → confirms the corrected value (not the phantom +314.70) was consumed. Post-epoch closes **5/8** (unchanged — no weekend closes); calibration blob still the 06-10 20:27Z empty reset → raw mode, as designed. All 5 outcome rows `is_paper=true` including the 3 live closes (ledgered P2 is_paper-discriminator gap, unchanged).

---

## 3. Pending-verifications table

| # | Item (ledger) | Status | Evidence |
|---|---|---|---|
| 1 | CLAUDE.md refresh (#1065) merges; both workers recycle; DEGRADED + raw-mode-reset fire once | **PASS (H8) / DEGRADED line carried** | Both workers on `57787605`/#1065, deployed 06-13 03:15:47Z (dual parity). Worker healthy (RQ heartbeat, clean 05:00Z precheck, 0 errors). The DEGRADED line fires on the first CONVICTION read = first scan; no scan since recycle (weekend) → fires today 16:00Z. Not failed; **carried**. |
| 2 | PR #908 live credit-mleg-close validation | **VERIFIED** | SPY iron condor close `1f444239` (06-12 15:30:07Z): mleg **buy-to-close at POSITIVE limit 1.96, filled POSITIVE 1.93**, single order, instant clean fill, no Sign-incoherent raise → realized −45. A credit structure closed correctly; pending since the ledger's first list — now closed. |
| 3 | 06-12 closes: positive close limits; double-submission persists until single-submitter fix | **VERIFIED (both halves)** | Positive limits confirmed (SPY 1.93, NFLX −4.13 credit-close, MARA −1.11 credit-close — all coherent). Double-submit confirmed on pre-#1064 code: QQQ condor close attempts `fc1625f1` (13:30:07Z submit→cancel in 0.6s) + `0675f969` (13:30:08Z, watchdog-cancel 13:35Z) = the documented pre-cancel double-submit, on worker 5681919 (#1064 single-submitter deployed 06-13 00:20Z, after). SPY/NFLX/MARA were single orders (instant fills, no second stage). |
| 4 | #1056 write-side proof: avg_entry/max_credit POSITIVE on a NEW credit fill | **VERIFIED** | DB QQQ condor `6798e58f` carries `avg_entry_price=1.61` (positive); broker legs show coherent signed per-leg avg prices. No phantom-negative entry. (The 06-11 condors were hand-corrected; this is the standing positive state.) |
| 5 | #1034: any TP fire writes a price-normalized corroboration row | **VERIFIED** | `exit_mark_corroboration_observations`: NFLX TP 06-12 15:15:04Z (mark 4.7355/+314.70 → achievable 4.131/**+133.35**, divergence_frac 0.086, `corroborated_allow`); QQQ TP 06-12 13:30:04Z; SPY stop 15:30:06Z (`stop_loss_never_suppress`). Rows written with price-normalized divergence. |
| 6 | Learning ingest consumed realized_pl **133.35** (not 314.70) for `1e2dd73f` | **VERIFIED** | Closed position `1e2dd73f` realized_pl=133.35; outcomes_v3 LPD pair sums +181.35 = 133.35 + 48.00. Corrected value consumed; the +181.35 fiction class did not propagate. |
| 7 | reentry_cooldowns writes on a cohort stop (the manual_bench question) | **CODE-VERIFIED / runtime-PENDING** | The SPY `manual_bench` was a one-time PRE-FIX operator action (06-12 15:30Z on worker 5681919, before cohort-stop auto-cooldowns). The fix is **deployed**: `intraday_risk_monitor.py:355` → `_write_cohort_stop_cooldown` (`:757-772`, `reason="cohort_stop_force_close"`), shipped #1062. No cohort stop has fired on #1062+ code → auto-cooldown path **unexercised**; verify on the next cohort-stop force-close. |
| 8 | #1058 realized-blind daily brake first armed RTH | **PENDING (log)** | #1058 live; first armed RTH was 06-12. The 06-13 week verdict implies it functioned (MARA round-trip unblocked the 16:00Z funnel), but the `[EQUITY_STATE] realized-blind gap` line is log-only and 06-12 RTH logs have aged out of Railway retention. Carried; next clean check is today's session. |
| 9 | #1034 06-12 normalization fix (phantom re-scores 0.06→~0.91) + Stage-2 enforce suppress | **PENDING — first fire awaited** | **NEW this run:** every corroboration row in-window is PRE-fix (worker 5681919); the QQQ 13:30Z phantom (mark −0.65/+96 vs achievable −7.6/**−599**) scored divergence_frac **0.0604** with `would_suppress=false` — exactly the value CLAUDE.md says the 06-12 fix re-scores to ~0.91. The corrected normalization + `EXIT_MARK_SANITY_ENFORCE_ENABLED=1` are deployed but **never observed on a live fire**. First test = today's RTH. |
| 10 | #1064 resting-TP exit-evaluator deferral (`skipped_resting_tp_owns_profit_side`) | **PENDING — first RTH monitor awaited** | Resting TP placed 06-13 00:28Z (post-close); no RTH monitor cycle since. The single-owner deferral fires on today's first monitor tick (q15min from 13:30Z). |
| 11 | #1059 ops delivery — reproducible signed-endpoint trigger | **PENDING (asterisk carried)** | No ops_health_check fired the synthetic proof in-window (the reproducible wiring was the unmerged `9c957d9` branch). Webhook still deferred (`OPS_ALERT_WEBHOOK_URL` absent; risk_alerts primary). Carried. |
| 12 | CLOSE-side Polygon-only reads (N1) / stamp-gap (N2) | **OPEN, unchanged** | No code in-window touches those sites (P1 backlog "Close-side quote check"). No stage-time rejections in-window (weekend) → N2 stamp paths unexercised. |

---

## 4. Areas 1–7

**AREA 1 — PROFITS: UNCHANGED.** No new fills in-window (weekend). Baseline 06-12 fill quality (live SPY/NFLX/MARA closes, the conservative-shadow NFLX TP) is fully reconciled and ledgered. Honest-EV debit verification (raw-mode passthrough) and the calibrated-vs-raw question wait on ≥8 post-epoch closes (5/8; ~06-20). No finding.

**AREA 2 — MINIMIZING LOSSES: signal moved (SPY manual bench) → root cause already-fixed; no finding.** The one new loss-control event is the SPY `manual_bench` cooldown, which traced to the pre-#1062 cohort-stop-no-cooldown gap — **closed in deployed code** (`_write_cohort_stop_cooldown`, Pending-Verif 7). The live book is one QQQ condor at −$45 with a broker-resting GTC TP (phantom-mark-immune) and stops never-suppressed (double-guarded). Loss topology is intact. Watch (today): the shadow neutral MARA 21ct is at −$382 unmarked since 06-13 00:28Z; the q15min cohort stop (#1048) evaluates it at 13:30Z+ — its designed protection.

**AREA 3 — SELF-LEARNING: UNCHANGED.** 5/8 post-epoch closes, raw mode persisting by design; all 06-12 closes ingested with the corrected NFLX value; dedup held earlier in the week. is_paper still tags all rows paper (ledgered P2). No new finding.

**AREA 4 — SELF-SUSTAINING: signal moved (recycle + manual bench + missed weekend report); no finding.** The #1065 recycle landed cleanly on both workers with no orphaned mid-cycle jobs (weekend = nothing in flight). The SPY manual bench (a human intervention) is explained by pre-fix code, now automated. The missing 06-14 Sunday FULL report is the audit loop's own liveness gap (local Task Scheduler, likely host-offline over the weekend); the loop self-recovered tonight — observation, not a production finding. Today is the first RTH test of three deployed-but-unexercised controls (#1034 corrected normalization + Stage-2 enforce, cohort-stop auto-cooldown, resting-TP deferral) — tracked as pending verifications.

**AREA 5 — EFFICIENCY: UNCHANGED.** No scheduled jobs ran over the weekend (correct). The two jobs that did run were fast (precheck 4.15s; exit_evaluate 5.7s). order_sync cadence unverifiable in-window (no RTH); last known 1.43s holding. No finding.

**AREA 6 — TRADING VOLUME: UNCHANGED.** Zero scans/stages over the weekend (correct). The one-shot/day executor (P1 backlog) and post-#1047 funnel pass-rates are unchanged; next exercise is today 16:30Z. No finding.

**AREA 7 — TIME IN TRADE: UNCHANGED.** No new holds. The live QQQ condor is aging (opened 06-11 16:20Z, ~4 days held; expiry 07-10, 25 DTE) with the resting TP as its profit-capture mechanism — the GTC-vs-poll decision is being piloted live (#1064). No new mechanism.

---

## 5. AREA 8 — SELF-EXTENSION (full protocol, every run)

**Spec re-evaluation → LENS KEPT** (Negative-Decision Efficacy, `audit/area8.md`). Candidate lenses considered against this window: (a) "deployed-but-never-fired control decay" — several controls are deployed yet unvalidated in production (single-submitter on a resting close, cohort-stop auto-cooldown, #1034 corrected normalization + Stage-2 enforce, resting-TP deferral); but Area 4 ("merged-but-not-running" + recovery-after-recycle) plus the ledger's pending-verification list already cover this — it is a tracking discipline, not a structurally-invisible lens. (b) "mark-integrity / phantom-mark detection" — the QQQ 13:30Z mark (−0.65 phantom vs −7.6 real, a $695 implied gap) is exactly what #1034 corroboration (Area 2) exists to catch; not a missing lens. Negative decisions remain the only evidence class with **zero** outcome measurement anywhere in the live system.

- **S2 inventory (30d):** 2,959 rejections + 33 negative-status suggestions vs **11 measured closes ≈ 272:1** (improved from 403:1 on 06-12 — closes grew 7→11 from the 06-12 session while rejections rose only ~5%). 06-12 cycle: 298 rejections, mix all-real (cost-avoidance + condor-scanning), no feed artifacts.
- **S3 reconstructability: NO DRIFT.** 06-12 rejections still carry no expiry (`top_has_expiry=false`); `execution_cost_exceeds_ev` rows carry no `capture_version` at all. Scanner rejects remain **0% repriceable**. No capture-field code shipped (backlog RESEARCH "Area-8 capture fields" — underlying-spot proxy, unbuilt).
- **S4 hand-marked counterfactuals:** **none new this window** — zero scans/suggestions over the weekend, and frozen Friday-close quotes would only re-derive the 06-13 result. The standing evidence is the 06-12 NFLX-fork sign-flip (counterfactual sign varies day-to-day; a single-day mark is noise — only a systematic marker integrates it). Constraint reaffirmed: the marker must not depend on session-local tooling or live RTH quotes.
- **S5 survivorship under the epoch:** post-epoch training set = **5 closes across 3 strategies** — IRON_CONDOR ×2 (QQQ shadow −234.78, SPY live −45), LONG_CALL_DEBIT_SPREAD ×1 (MARA live −28), LONG_PUT_DEBIT_SPREAD ×2 (NFLX shadow +133.35, NFLX live +48). Composition diversified vs the 06-12 single-row set, BUT all 5 are `is_paper=true` — when n≥8 triggers calibration, live and shadow fills will train one predictor indistinguishably (ledgered P2). Classes structurally absent from training: anything the one-shot/day executor + CHOP regime never staged.
- **S6 surface integrity:** stamp coverage unexercised (no stage-time rejections in-window); the 2 `pending` 06-12 suggestions are the dismissed-status funnel-gap residue (ledgered P1); `outcomes_log`/`policy_decisions` unchanged (no relevant code touched).
- **S7 finding determination: NO new ledger-grade A8 finding.** Everything maps to the standing recommendation (expiry/proxy capture + systematic read-only marker + per-gate reject-vs-accept metric), which the improving-but-still-272:1 ratio and the recurring counterfactual-sign volatility reinforce but do not change. Hard boundary honored: nothing above loosens a gate, expands audit writes, or touches the contract.

---

## 6. New findings this run

**NONE.** This is a creditable empty run — a quiet weekend with clean operation. The one candidate (SPY manual-bench → cohort-stops-don't-auto-cooldown, Area 4) was **overturned by reading the code**: the auto-cooldown path (`_write_cohort_stop_cooldown`) shipped in #1062 and the manual bench predated it. Net value of the run: clean-operation confirmation (H8 dual-parity #1065, weekend silence, H11 zero, broker healthy) + resolution of four long-pending verifications (#908 credit-close, the corrected-NFLX ingest, the TP corroboration row, the pre-fix double-submit) + three newly-sharpened pending verifications for today's session (the #1034 corrected-normalization first fire, cohort-stop auto-cooldown first fire, resting-TP deferral first fire).

---

## 7. TOP-3 · conflicts · four-source disagreements

1. **Today 13:30Z–20:00Z is the first RTH validation of three deployed-but-unexercised live-path controls** (watch, not build): (a) #1034 **corrected divergence normalization + Stage-2 enforce** — the QQQ condor's marks have repeatedly phantomed (−0.65 vs −7.6 on 06-12; −1.77 vs broker −2.06 now); a phantom TP today should re-score ~0.91 and be SUPPRESSED (stops never are); (b) **cohort-stop auto-cooldown** (`_write_cohort_stop_cooldown`) on any cohort-stop force-close; (c) **resting-TP exit-evaluator deferral** (`skipped_resting_tp_owns_profit_side`) on the first QQQ-condor monitor tick. Also: the SPY bench expires 13:30Z; the shadow neutral MARA (−$382) faces its first weekend-gap cohort-stop evaluation.
2. **Land the #1059 synthetic-proof wiring durably** (carried from 06-12): the ops-delivery proof still exists only as the unmerged `9c957d9`; merging closes Pending-Verif 11's asterisk and makes ops delivery reproducible. Effort small.
3. **Audit-loop liveness** (Area-4 observation): the 06-14 Sunday FULL produced no report (local scheduler likely offline over the weekend). The loop has no liveness monitor — a silently-missed nightly is invisible until the next manual/successful run. Low-effort mitigation: a heartbeat/last-run check (out-of-scope for this READ-ONLY loop to build; flagged for the operator).

**Conflicts/synergies:** none conflicting. TOP-1's three controls are independent observations on the same session. The ledgered close-side quote check (N1) and the ghost-sweep shadow-scoping (A5/A2) still share the `position_scope`/live-routed pattern (natural same-PR pair) — unchanged.

**Four-source disagreements encountered:** (1) **QQQ condor mark: DB −1.77 (−$16) vs broker −2.06 (−$45)** — ALPACA outranks; the gap is the known DB-mark-lag/leg-timestamp-skew class (#1022 / chart-artifact), DB marks frozen since 06-13 00:28Z over the weekend. Reported, not averaged. (2) `GATE_DEPLOY_PARITY` env marker (`5681919-…`) vs the actual running SHA (`5778760`) — stale informational var, not the deploy truth. (3) Bash tool unavailable → `git ls-remote` not run; origin/main confirmed via cached fetch + Railway dual-deploy listing instead (conclusive given weekend merge-silence).

---

## 8. Ledger updates made

- Appended a `2026-06-15 NIGHTLY run` section to `audit/ledger.md`: **no new findings**; recorded the verifications closed (#908 credit-close, corrected-NFLX ingest, TP corroboration row, pre-fix double-submit), the overturned candidate (cohort-stop auto-cooldown already shipped #1062), and the newly-sharpened pending verifications for today's session (#1034 corrected-normalization first fire; cohort-stop auto-cooldown first fire; resting-TP deferral first fire; #1058 brake log; #1065 DEGRADED line).
- Updated `audit/area8.md` footnote with run-4 numbers (272:1; no new sample under weekend silence; tooling-independence constraint reaffirmed).

**STOP.** Recommendations only; no production surface touched. Writes: this report, `audit/ledger.md`, `audit/area8.md`.
