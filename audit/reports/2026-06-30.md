# Audit v5 — 2026-06-30 NIGHTLY (Tuesday)

Window: prior baseline = 06-15 NIGHTLY (NO NEW FINDINGS). 15-day gap (parked week
06-18→06-29; trading resumed-armed tonight). MASSIVE state movement: 5 PRs merged
(#1094–#1098) + `PAPER_AUTOPILOT_ENABLED` flipped 0→1 (first live-autopilot arm of
the learning phase). Mode NIGHTLY; deep-dive moved areas, UNCHANGED otherwise.

## RUNNING STATE (H8)
- Worker + worker-background both **SUCCESS @ `f7dab1d`** (= origin/main HEAD; deploy
  `92bd0360` 06-30 05:05:15Z — the autopilot-flip recycle). Merged == running ✅.
- Flags (confirmed this session): `EXECUTION_MODE=alpaca_live`, `LIVE_ENABLED=true`,
  `ALPACA_PAPER=false`, **`PAPER_AUTOPILOT_ENABLED=1`** (flipped 05:05Z), `STALENESS_GATE_ENABLED=1`;
  `ops_control` paused=false / **entries_paused=false** (column live, migration applied tonight).
  Safety/tightening flags default-ON (§3) — not re-dumped (secrets hygiene); none observed falsy.
- Broker: equity **$2,134.80**, OBP 2134.8, cash 2134.8, daytrade_count 0, **FLAT** (0 positions).
- Shadow book: also **FLAT** (0 open paper_positions). Whole system flat going into live-autopilot.
- H11: critical/high `risk_alerts` 7d = **0 critical**, 25× `ops_data_stale` (high) — the raw-mode/
  `CALIBRATION_ENABLED=0` freshness artifact, not an incident.

## PART 1 — WEEK IN REVIEW (06-08 → now), ranked by P&L
Nothing new traded since **06-17** (parked week = zero live/shadow activity). Table = cumulative
open book history; LIVE = champion `3d289dca`, SHADOW = `c8a3a3b0`/`ec545555`. LIVE rows graded
LIVE-BROKER (DB realized_pl ⟷ ledger-verified broker fills; 109k order dump NOT re-parsed — A5).
SHADOW = SHADOW-MODELED (#1017 fill-optimism bias).

| pos | cohort | structure | entry→exit | P&L $ | staged-EV | grade |
|---|---|---|---|---|---|---|
| NFLX | SHADOW c8a3a3b0 | LPD | 06-08→06-16 | **+662.10** | +85.6 | SHADOW |
| NFLX | SHADOW ec545555 | LPD | 06-08→06-12 | +133.35 | +85.6 | SHADOW |
| NFLX | LIVE 3d289dca | LPD | 06-08→06-12 | **+48.00** | +95.67 | LIVE-BROKER |
| MARA | LIVE | LCD | 06-17→06-17 | −15.00 | +29.7 | LIVE-BROKER |
| MARA | LIVE | LCD | 06-12→06-12 | −28.00 | +29.7 | LIVE-BROKER |
| SPY | LIVE | IC | 06-11→06-12 | −45.00 | +40.9 | LIVE-BROKER |
| QQQ | LIVE | IC | 06-11→06-15 | −73.00 | +40.9 | LIVE-BROKER |
| NFLX | LIVE | LPD | 06-04→06-08 | −84.00 | (protective close) | LIVE-BROKER |
| QQQ | SHADOW c8a3a3b0 | IC | 06-11→06-11 | −234.78 | +46.1 | SHADOW |
| NFLX | SHADOW ec545555 | LPD | 06-03→06-08 | −273.00 | +85.6 | SHADOW |
| NFLX | SHADOW c8a3a3b0 | LPD | 06-03→06-08 | −546.00 | +85.6 | SHADOW |
| MARA | SHADOW c8a3a3b0 | LCD | 06-12→06-18 | **−675.99** | +29.7 | SHADOW (#1071 shadow_only) |

Rollups: **by cohort** — LIVE realized **−$197** (6 closes); SHADOW realized **−$934** (6 closes,
mark-biased). **by family** — LPD net live −36 / shadow +66; IC live −118 / shadow −235; LCD live
−43 / shadow −676. **by symbol** — NFLX dominates both tails.
**WEEK VERDICT:** Live realized **−$197** cumulative (delta-since-06-15 = MARA −15 only); open
unrealized **$0** (flat); fees negligible. Every staged EV positive (+30…+96), aggregate realized
negative → the known raw-mode optimism (≈−$67/trade, post-#1051, uncalibrated by design). The
parked week added **zero** trades; the whole story this run is infra (5 merges + live arm), not P&L.

---

## PART 2 — EIGHT AREAS

**A1 PROFITS — NO NEW FINDING.** Honest-EV ordering weakly agrees (top realized winner NFLX +662
sat in the high-EV bucket); uniform +EV optimism is raw-mode (calibration ×1.0), ledgered 06-13.
No new fills since 06-17 to re-test. UNCHANGED.

**A2 MINIMIZING LOSSES — REFINEMENT (now live-relevant, P2).** 4c config fail-open CLOSED tonight
(`config.py` TIGHT_FALLBACK live @ f7dab1d) — the 2.17× silent stop-widen is gone. With autopilot
now ON the book will hold >1 position for the first time live → multi-position correlated-loss +
several-envelopes-one-cycle + loss-precedence (−3% envelope→cohort stop→vestigial 0.50) become
live-relevant. Per §5 the precedence coherence is **backlog P2 (don't fix ad hoc)** — no NEW finding,
but flag: this exposure goes live on the next 11:30 CT cycle. Gap risk bounded (defined-risk only).

**A3 SELF-LEARNING — REFINEMENT.** Post-epoch live closes **5/8** (is_paper=false, 06-12→06-17);
relearn stays RAW (do-no-harm, by #1076 design). Good news: the ingest break (06-23→06-29) lost
**zero** outcomes — the book was flat that whole window, so nothing was there to ingest; #1098 fixed
the writer before the next close. STRUCTURAL Q (A3's charter): at the live trade rate (~5 live
closes/3wk across 3 strategy buckets → ~1-2/bucket), per-bucket significance (≥8) **may never be
reached** → calibration could stay raw indefinitely. That's the known #1076 tradeoff, not a defect;
the path to ≥8 now depends ENTIRELY on autopilot volume (newly unblocked tonight). No new finding
beyond the ledgered raw-mode/is_paper items; monitor whether autopilot volume matures the buckets.

**A4 SELF-SUSTAINING — FINDING (the one this run). Silent in-result-error masking.**
- WHAT: `paper_learning_ingest` ran **5×/7d, 0 hard-fails** — but every run 06-23→06-29 was
  `status=succeeded` with `result.counts.errors=1, outcomes_created=0` (the `opened_at` 42703,
  swallowed per-user). The learning loop was DEAD for 6 days and **nothing alerted**.
- WHERE: detection gap — `ops_health_service` OUTPUT_FRESHNESS watches `calibration_adjustments`
  ONLY; EXPECTED_JOBS checks job **status**, not `result.counts.errors`/output-table freshness.
- WHY #1: a week-long silent learning-loop death just happened; the masking is generic (any future
  ingest fault recurs silently). #1098 fixed THIS cause; the DETECTION gap is unfixed.
- IMPACT: detection latency 6d → 0d if `job_runs.result.counts.errors>0` (status-succeeded included)
  alerted, or `learning_feedback_loops` added to the freshness registry. The parked week masked the
  P&L cost (zero closes) — under live autopilot a recurrence silently starves calibration AND loses
  real outcome rows.
- HOW: extend OUTPUT_FRESHNESS / a health check to flag `errors>0 OR (closes_today>0 AND
  outcomes_created=0)`; reuse the existing alert() path (no new infra).
- EVIDENCE: job_runs aggregate (5 succeeded/0 fails) + the per-run notes (`42703 opened_at`).
- RISK: additive detection only; zero blast radius. CONFIDENCE: HIGH. (Refines the ledgered
  "OUTPUT_FRESHNESS watches ONE table" P2 — the new incident **changes the action**: P2→P1.)

**A5 EFFICIENCY (incl. this loop) — FINDING (this run's own spend).** Largest avoidable cost THIS
run: `get_orders(status=all, nested, limit 50)` returned **109k chars** (token-budget overflow,
auto-saved to file) — should have used `symbols=`/date-narrow or a subagent+jq. Otherwise within
budget (≤13 SQL, ≤3 broker, 0 web; ledger/area8 read once, reused). System-side: `iv_daily_refresh`
4.0min + `suggestions_open` 24s both on the background queue (fine); no new system inefficiency
(post-#1049). UNCHANGED system; the loop self-cost is the only A5 item.

**A6 TRADING VOLUME — NO NEW FINDING.** Funnel: **2–4 emitted/day** vs 180–500 rejected/day, **0
executed** all parked week (autopilot off). Emission low; stage-deaths real (spread/no-fallback,
post-#1052), not feed artifacts. Universe 75→84 (06-17) did NOT raise emission (binding constraint =
EV-gate + one-shot/day executor, both ledgered P1 — not loosenable). Autopilot-ON will reveal actual
execution volume on the next 11:30 CT cycle — monitor, not finding. UNCHANGED.

**A7 TIME IN TRADE — UNCHANGED.** Hold-time bimodal, unchanged from 06-13: LPD ~4–8d
(NFLX 06-08→06-16), IC 1–4d (SPY 1d, QQQ 4d), MARA same-day (cohort-stop velocity, 06-12/06-17).
PDT retired; velocity cost = fees + cooldown. Binding velocity constraint = one-shot/day executor
(ledgered P1). No new finding.

**A8 SELF-EXTENSION — lens KEPT (5th audit); replacement candidate named.**
Current lens = Negative-Decision Efficacy. AUDIT: ratio more extreme this window (~180/day rejected,
**0 executed** parked week → unmeasured:measured ≈ ∞ during the freeze); reconstructability NO DRIFT
(rejects still 0% repriceable, no expiry in capture); no new counterfactual sample (no executes to
compare). Lens still valid. **Replacement candidate (defer to next run, on data): "Entry-Fill
Efficacy."** What the current lens structurally MISSES: a STAGED live order that passes every gate
then **watchdog-cancels** unfilled at the DAY-limit (06-03 NFLX precedent, ~5min) — it's an "accept"
to the negative-decision lens, yet produces no position/fill/outcome → invisible to A1/A3 (no
outcome), miscounted by A6 (counts "staged"). Autopilot-live makes this the dominant invisible loss
class — but it has **zero data tonight** (autopilot off until 05:05Z). Evidence-discipline: KEEP the
counterfactual lens this run; ADOPT Entry-Fill Efficacy next run once staged-live orders exist.

---

## TOP-3 (value / effort / risk)
1. **A4 silent-ingest-error detection** — high value (week-long silent death just occurred), low
   effort (alert on `result.counts.errors>0`), zero risk (additive). **#1 this run.**
2. **A8→Entry-Fill Efficacy lens** — newly critical (autopilot live), captures the watchdog-cancel
   blind spot; adopt next run when data exists.
3. **A3 relearn maturation watch** — confirm autopilot volume actually advances 5/8→8 post-epoch
   live; else calibration is permanently raw (by design, but worth an explicit decision).

## CONFLICTS & SYNERGIES
- A4 (detect ingest errors) + A3 (relearn maturation) **synergize** — A4's detector is exactly the
  alarm that would flag "closes happened but outcomes_created=0," protecting A3's path-to-8.
- A6 volume↑ (autopilot on) × A7 hold-time → more round-trips → fees + #1040 cooldown benches (NOT
  PDT, retired). Mild; learning-mode low frequency contains it.
- No conflict between any finding and a risk control (none loosen anything).

## FOUR-SOURCE DISAGREEMENTS HIT
- **paper_positions vs learning_trade_outcomes_v3 on the −84 NFLX (06-08 live close):**
  paper_positions → cohort `3d289dca` (LIVE champion); v3 → `is_paper=true` (paper). The LIVE close
  is **mislabeled paper** in the learning view (the ledgered is_paper P2; pre-epoch, so it does NOT
  corrupt the post-epoch relearn count, but it understates live realized in v3: −113 vs the true
  −197). Reported, not averaged.

## A8 LENS DECISION
KEPT: Negative-Decision Efficacy (no data-backed replacement available tonight). NAMED next-run
replacement: **Entry-Fill Efficacy** (adopt once autopilot produces staged-live orders). area8.md
updated with the dated rationale.

— END. No code/orders/env/migrations touched this run. Findings appended to ledger as status:reported.
