# Audit v5 — 2026-07-01 NIGHTLY (Wednesday, manual on-demand run)

Baseline = 06-30 NIGHTLY. First full session after the live-autopilot arc (SOFI trade +
Phases 0/1/2 + precursor #1102, all shipped 06-30). Mode: verification-first — did what
shipped behave on its first live day. No ALERT file: the one critical alert today was a
functioning control on a shadow position (A2), not a defect; the operator flag that matters
(webhook unset) is a known owned item, escalated in TOP-3, not an incident.

## RUNNING STATE (H8)
- Worker + worker-background both **SUCCESS @ `b3479a8`** (= origin/main HEAD; deploys
  23:30:22–24Z 06-30, the #1102 recycle). **No recycle today** — one container spans the
  whole session. Merged == running ✅.
- Flags (read back this run, both workers unless noted): `PAPER_AUTOPILOT_ENABLED=1` ✅,
  `EXECUTION_MODE=alpaca_live`, `LIVE_ENABLED=TRUE`, `ALPACA_PAPER=false`,
  `SCHEDULER_ENABLED=1`, `RISK_ENVELOPE_ENFORCE=1`, `TASK_NONCE_PROTECTION=1`,
  `MIN_EDGE_AFTER_COSTS=15`, `MIN_CALIBRATION_TRADES=8`, `EXIT_MARK_SANITY_ENFORCE=1`.
  Default-ON family (incl. **`ENTRY_ROUNDTRIP_COST_GATE_ENABLED`**, `CLOSE_QUOTE_VALIDATION`,
  `CALIBRATION_TRAIN_LIVE_ONLY`, cooldown/re-arm/cohort-stop/TTL/source-aligned/funnel-truthful)
  all env-ABSENT → ON per §3 polarity ✅. `CALIBRATION_ENABLED=0` both workers yet
  calibration_update runs daily (unchanged 06-30 observation — flag doesn't gate the scheduled
  job; raw mode anyway). **`OPS_ALERT_WEBHOOK_URL` UNSET / `HEARTBEAT_PING_URL` UNSET** (A4).
  Parity drift (inert): `RISK_UTILIZATION_GATE_ENABLED`/`RISK_MAX_UTILIZATION_PCT`/
  `EXIT_MARK_SANITY_OBSERVE` present on worker only — bg never stages/monitors; REGIME_V4 class.
- ops_control: paused=false, **entries_paused=false**.
- Broker (LIVE 211900084): equity **$2,093.74** == last_equity (zero P&L movement today),
  OBP $2,093.74, cash $2,093.74, **FLAT**, 0 fills today (activities query). Δ vs 06-30
  ($2,134.80) = **−$41.06** = SOFI −$40.00 + ~$1 fees. daytrade_count 0.
- Shadow: 0 open (the last shadow position force-closed 13:30Z today, below).
- H11 (criticals/high since prior run): **1 critical** — `force_close` 07-01 13:30:09Z (shadow
  SOFI, functioning control, A2). Noise: `ghost_position` warn **51× 06-30 + 7× 07-01** (one
  shadow position; ledgered sweep-scoping P2, escalation argued in A4), `ops_data_stale` high
  4×/day (chronic calibration-freshness artifact, ledgered), chain_mechanics warns (ledgered).
- job_runs 7d: **zero failures across all 21 job types** (order_sync 480×, ≤3.5s post-#1049;
  monitor 32×/day ≤6.2s; learning chain 6/6 on bg queue; iv_daily_refresh 240s on bg).
- Active cooldowns: **1** — (cohort c8a3a3b0, SOFI) until 07-02 13:30Z,
  reason=cohort_stop_force_close, realized_loss −1044.48 (first automatic #1062 write).

## PART 1 — WEEK-IN-REVIEW DELTA (append to the 06-30 table; that table not re-pulled)

Two new closes since the 06-30 report (which was written pre-RTH 06-30 — the SOFI arc
happened after it):

| pos | cohort | structure | entry→exit | P&L $ | staged-EV | grade |
|---|---|---|---|---|---|---|
| SOFI | LIVE 3d289dca | LCD 17/20.5 ×5 | 06-30 16:30Z→16:45Z (15 min) | **−40.00** | +30.6 | LIVE-BROKER |
| SOFI | SHADOW c8a3a3b0 | LCD 17/20.5 ×17 | 06-30 16:30Z→07-01 13:30Z (21 h) | **−1,044.48** | +30.6 | SHADOW-MODELED |

- LIVE grade: 4 broker fills verified (entry buy C17 2.10 / sell C20.5 0.66 ×5 = 1.44 net
  debit; close sell 2.13 / buy 0.77 = 1.36 net) → (1.36−1.44)×5×100 = −40 ✓; equity walk ✓.
  Third fork (conservative) was rejected `NOT_EXECUTABLE edge_below_minimum` (EV 19.1) — no
  position; the day's best decision in hindsight (see A8 retirement note).
- SHADOW grade: internal fill **at the executable side** (fill_quality=executable, #1017):
  entry 1.4544 ×17 (max structural loss $2,472 — the loss is arithmetically legal), exit 0.84
  = the corroborated full-cross estimate at 13:30:05Z, seconds after the open. At MID the
  position was **+$26** (mark 1.57 > entry); the full-cross side said −$1,044 (divergence_frac
  0.869, opening-rotation quotes). Sizing note: shadow book sized 17 lots vs live 5 (synthetic
  cohort capital — known design), which with the overnight hold drives the 26× loss multiple.
- Rollups: LIVE realized cumulative **−$237** (7 closes; delta −40). SHADOW cumulative
  **≈−$1,979** (7 closes; delta −1,044.48, mark-bias caveat as always).
- **DELTA VERDICT:** first live-autopilot round trip complete: entry admitted on EV +30.6 that
  ~$135 of executable round-trip cost made underwater-from-entry; live loss bounded at −$40 by
  the force-close; the admitting defect was closed the same night (#1101). Today: a correct
  zero-entry day (382 scanner rejections, 0 emitted) and the shadow twin's open-bell force-close.

## PENDING VERIFICATIONS (06-30 list — all three CLOSED, plus bonus closures)
1. ✅ **First autopilot executor cycle**: 06-30 16:30Z staged + broker-filled SOFI live (+2
   cohort forks per design); 07-01 both cycles ran clean with 0 candidates (nothing to stage).
2. ✅ **First post-fix ingest**: 06-30 21:20Z `closed_positions_found=1, outcomes_created=1,
   errors=0`; 07-01 21:20Z `found=2, created=1, skipped_duplicate=1, errors=0` (dedup ✓).
   A4 cols: `entry_iv_rv_spread` populated (0.1166, live SOFI — first ever);
   `realized_vol_over_hold` NULL on both rows — DESIGNED: `A4_MIN_HOLD_BARS=3` daily bars
   required, and 15-min/21-h holds can't qualify. Only a NULL on a ≥3-day hold is a writer gap.
3. ✅ **Entry-Fill Efficacy baseline recorded**: staged-live lifetime N=2 (06-03 NFLX
   watchdog-cancel ~5min; 06-30 SOFI filled AT the 1.44 limit within ~10s). See A8.
Bonus closures this run: **#1073 Layer A first exercise** (2 suggestions stamped `executed`
06-30 at position-insert) ✅ · **#1062 first automatic cohort-stop cooldown write** ✅ ·
**#1080 corroborated per-position trigger first live fire** (trigger 3, cohort stop, on
corroborated UPL not raw mark) ✅ · **Phase-B EXIT_EVAL_DEBUG honesty in the wild** (printed
the cohort threshold −494.496 = 0.20×basis, not the flat default) ✅.

## PART 2 — EIGHT AREAS

**A1 PROFITS — UNCHANGED (delta noted).** Today: 0 entries (2 scans, 0 emitted, 382 honest
rejections). First autopilot outcome: staged EV +30.6 → realized −40; the −70.6 gap is
execution-cost (the #1101 class, fixed 06-30 22:35Z — its SOFI fixture now rejects this
entry), not model sign. Raw-mode EV optimism remains ledgered, by design until 8 live closes.

**A2 LOSSES — NO NEW FINDING; controls exercised cleanly on the one force-close.**
The 13:30:09Z shadow SOFI force-close ran the full post-arc stack as designed: cohort stop
evaluated on **corroborated** UPL (#1080 first fire: mid 1.57/+$26 vs achievable 0.84/−$1,044,
divergence 0.869, stop never suppressed by doctrine) → internal fill at executable with
fill_quality flag (#1017) → automatic cooldown bench (#1062/#1040 armed: SOFI/c8a3a3b0 until
07-02 13:30Z) → critical force_close alert written. Realized-blind brake (#1058/#1071): live
book flat + zero live realized → evaluated q15min all session, stayed clear; the −$1,044 is
shadow_only and correctly **excluded** from the live brake (same as the 06-18 −675.99
precedent). The mid-vs-cross magnitude at the open is the **GATED Phase-3 class** (deferred
06-30, reopen on ≥10–15 live close fills) — cited, NOT re-found. Operator awareness line:
this loss lands on the neutral cohort's policy-lab ledger at 26× the live twin's — cohort
comparability carries the known #1017 modeled-fill bias, now with a large concrete instance.

**A3 SELF-LEARNING — GATE CHECK PASS (the run's target verification).**
- SOFI −$40 live close ingested clean: **is_paper=FALSE ✓, errors=0 ✓, pnl −40.0 ✓**,
  entry_iv_rv_spread populated ✓ (realized_vol_over_hold NULL — hypothesis above).
- Post-epoch live closes **5→6 ✓** (6 live / 11 total post-epoch).
- 07-01 10:00Z calibration: window escalation 30/60/90 → **sample_size=6 at every window =
  exactly the live count** → #1076 live-only training EMPIRICALLY CONFIRMED (11 post-epoch
  outcomes exist; only the 6 live ones are seen) → insufficient_data → raw_mode_reset written
  (do-no-harm holding, correct). Path to 8: two more live closes.
- Today's −$1,044 shadow outcome ingested is_paper=true → will be EXCLUDED from tomorrow's
  relearn (pending-verification added: 07-02 10:00Z sample stays live-only).
No finding — the learning loop did on its first real live close exactly what Phase 0 checked.

**A4 SELF-SUSTAINING — detector verified-negative; one refinement (ledgered-item priority).**
- #1100 detector: ops_health_check ran 10× today; **zero** succeeded-with-errors jobs existed
  (sum result.counts.errors=0 across 190 runs / 21 jobs) → correct silence on a clean day
  (synthetic end-to-end proof was 06-30; today adds the clean negative).
- **Stranded critical (the prompt's question, answered concretely):** today's critical
  force_close alert reached the DB and NOTHING else — `OPS_ALERT_WEBHOOK_URL` UNSET on both
  workers; zero webhook/egress lines in logs. First critical since #1096 shipped = first
  concrete stranded-critical. Operator owns setting the webhook (+`HEARTBEAT_PING_URL` for
  #1095's dead-man's switch, also unset). Escalated to TOP-3 #1.
- REFINEMENT (changes action on a ledgered item): **ghost_position noise escalation under
  autopilot** — 58 shadow-ghost warns in 2 days from ONE shadow position (51× 06-30 + 7×
  07-01) vs the ~73/week baseline. The un-scoped sweep (`alpaca_order_sync.py:245`, ledgered
  P2) now floods exactly when live orders auto-flow and a REAL broker desync matters most.
  Action change: P2 → **P1**, pair with the fail-closed config item per the existing backlog
  note. Additive scoping only; zero blast radius.

**A5 EFFICIENCY — FINDING (system) + self-audit (loop).**
- **FINDING: the DB disconnect burst RECURRED — it's a pattern, not a blip.** 16:00:09Z
  today, 8× `Server disconnected`, all `suggestion_rejections` inserts inside the scheduled
  scan (verbatim: `suggestion_rejections insert failed (symbol=NFLX reason=spread_too_wide_real
  …): Server disconnected`); job result corroborates `persist_failures: 8`. Same class and
  same window as 06-30's storm. #1100's right-sized retry wraps ONLY `observability/alerts.py`
  alert() inserts — scanner persists are unprotected → **8 rejection rows lost today**
  (observability data: funnel mix + reject-capture; no live-risk surface). WHERE: scanner
  persist seam (suggestion_rejections writer). HOW (additive): reuse the exact #1100
  transient-retry (0.25/0.5s on RemoteProtocolError/"Server disconnected") at the scanner
  persist seam — or pre-ping/recycle the connection before the post-scan write burst.
  EVIDENCE: worker logs (8 lines, 16:00:09Z) + job_runs result. RISK: zero (retry-on-write).
  CONFIDENCE: HIGH. Note: 14:01Z scan had persist_failures=0 — the burst correlates with the
  idle-keepalive gap before the 16:00Z grid, consistent with the 1100-diagnosed signature.
- Loop self-audit vs guardrails: **13 SQL** (⚠ 1 over the ≤12 cap — the 13th corrected my own
  wrong-key probe of order_json; counted honestly), 3 broker calls (≤4 ✓, activities
  date-filtered — no get_orders paging this run ✓), 2 subagents (≤8 ✓), log excerpts ≤5 lines
  ✓, report ≤ ~180 lines ✓, ledger items cited not re-verified ✓. Residual exposure note: the
  flag read-back subagent's `list_variables` necessarily materialized full env values in its
  own transcript (tool has no names-only mode); only the allowlisted flag names/values entered
  this session. System-side timings all healthy (see RUNNING STATE).

**A6 VOLUME — NO NEW FINDING; the Phase-2 test answered.**
- Funnel: 06-30 emitted 3 (2 `executed` — the SOFI live+neutral forks; 1 NOT_EXECUTABLE
  edge_below_minimum); 07-01 emitted **0** from 2 scans; rejects 382 vs 375, mix stable
  (no_fallback 116/110 · spread_too_wide_real 95/81 · execution_cost_exceeds_ev 43/62 ·
  all_strategies_rejected 49/50 · iv_rank_insufficient_history 28/28).
- **ENTRY_ROUNDTRIP_COST_GATE: 0 evaluations** — nothing reached the stage seam today, so the
  gate remains LIVE-UNEXERCISED (0 `ev_below_roundtrip_cost` stamps; 0 gate log lines — note
  `execution_cost_exceeds_ev` in the reject mix is the SCANNER's older cost check, not #1101).
  Its first exercise pends the next staged candidate; pending-verification added.
- **A near-zero-entry day on honest math is CORRECT, and that is what happened** — candidates
  died at the scanner on real spread/cost/fallback grounds before any gate #1101 owns.
- The unscheduled 14:01:50Z scan + 14:04:14Z executor pair: payload user-pinned (scheduled runs
  carry user_id=null) + off-grid times + no scheduler marker in logs → operator/API-triggered
  manual cycle (HYPOTHESIS; logs carry no trigger metadata). Both clean: 0 candidates, 0
  executed. Non-incident. Cadence itself stays backlogged (P1 anti-list — premise unchanged).

**A7 TIME IN TRADE — verification; #1102 exercised correctly; no informative datapoint yet.**
- Holds: live SOFI **15 min** (envelope force-close on entry day — cohort-stop-velocity class,
  like MARA); shadow SOFI **21 h** (overnight → open-bell force-close). Distribution otherwise
  unchanged from 06-30.
- **#1102 first event (the prompt's question):** today's shadow close DID exercise it — the
  fill-side persist wrote `close_fill_gap_fill=0.84` into the close order_json with
  cross/mid/fraction NULL, which is the DOCUMENTED design for internal/shadow closes
  (fill-only; a shadow fill is AT cross by construction → gap carries zero information). The
  `[CLOSE_FILL_GAP]` log line emitted at **INFO** (`close_fill_gap.py::log_close_fill_gap`)
  and the worker surfaces WARNING+/print → grep-invisible in Railway (the ledgered
  [UTILIZATION_GATE] observability class; DB is the designed durable channel, so Phase-3 data
  accrual is unaffected). Cosmetic rider for the next touch: bump the line to WARNING.
- **First INFORMATIVE gap_fraction still pends the first LIVE close post-#1102** (informative
  = broker fill vs cross). Accrual math the operator should hold: at ~1 live close/week, the
  Phase-3 reopen gate (≥10–15 live fills) is **~2–3 months out** — the deferral's data clock
  is slow; that is the honest price of the N=1-overfit refusal, worth knowing not changing.

**A8 SELF-EXTENSION — LENS SWAPPED: Entry-Fill Efficacy ADOPTED (replaces Negative-Decision
Efficacy after 6 runs).** The 06-30 adoption trigger ("once staged-live data exists") is met:
one complete staged→broker-filled→closed live lifecycle (SOFI 06-30) + the armed #1101 gate
stamping `ev_below_roundtrip_cost` + #1102's order_json persistence = evidence base AND
forward feed. What the old lens structurally missed — stated per contract: (1) an ACCEPT that
fills and loses to crossing cost: SOFI passed every gate on EV +30.6 while ~$135 of executable
round-trip made it underwater-from-entry — no reject row for the counterfactual lens, generic
"optimism" to A1, "volume" to A6; the mechanism was measurable at stage time and nobody's
charter; (2) staged-then-watchdog-cancelled accepts (06-03 NFLX) that produce no outcome row
at all. FIRST AUDIT under the new spec (full protocol, this run): staged-live inventory N=2
lifetime (1 fill at-limit ~10s, 1 watchdog-cancel ~5min — instant-or-never shape holds);
fill-vs-limit slippage on the one fill: 0.00; round-trip gate 0 evaluations (armed,
unexercised); close-fill-gap feed live and correct on its first event (fill-only by design);
informative gap datapoints: 1 ever (SOFI 06-30 live, ≈0.23, the pinned fixture). NO FINDING
beyond the accrual-rate observation (A7). Old lens retired with its parting datapoint: the
conservative fork's REJECT (EV 19.1) beat both accepting books (−40, −1,044) — its standing
capture/marker recommendation stays in backlog RESEARCH, not withdrawn. `audit/area8.md`
rewritten (spec + rationale + first-audit result + hard boundary restated).

## TOP-3 (value / effort / risk)
1. **Set `OPS_ALERT_WEBHOOK_URL` (+ `HEARTBEAT_PING_URL`)** — operator, minutes, zero risk.
   Today produced the first stranded critical since the egress code shipped; until set, every
   critical is DB-only while autopilot trades live. (A4)
2. **Extend the #1100 transient retry to the scanner persist seam** — the disconnect burst is
   now a 2-day pattern in the same window; 8 rows lost today; additive, low effort. (A5)
3. **Ghost-sweep shadow scoping P2→P1** — 58 noise alerts/2d from one shadow position buries
   the desync signal exactly when live order flow needs it; additive scoping. (A4)

## CONFLICTS & SYNERGIES
- TOP-3 items 1+2+3 share one surface (alert/persist reliability) — one PR-window could carry
  2+3; 1 is env-only (no deploy; safe mid-week per §2 since no recycle is required — env var
  reads happen per-call in the egress path; verify read-back regardless).
- Phase-3 stays gated: today's shadow instance REINFORCES the deferral (the class is real) and
  A7's accrual math is the only actionable edge — no conflict with any control.
- The A8 swap synergizes with #1101/#1102: the new lens's feed IS their telemetry.

## FOUR-SOURCE DISAGREEMENTS
- None on dollars/positions today (broker/DB/Railway/code agreed everywhere checked). One
  taxonomy note: the same event carries three reason labels across three tables
  (paper_positions.close_reason=`envelope_force_close` · risk_alerts msg=`intraday_stop_loss` ·
  reentry_cooldowns.reason=`cohort_stop_force_close`). Cosmetic, but a future query that
  filters on any single label will undercount force-closes — worth one alignment line when
  that surface is next touched.

## SCHEDULER DOUBLE-WRITE CHECK (per run instructions)
No collision: v5-prompt writes reports by LOCAL DATE AT RUNTIME; tonight's automated run fires
00:23 CT = local date 2026-07-02 → it writes `2026-07-02.md` and will diff against THIS file
as its baseline (expect a near-empty overnight diff). This manual run IS the 7/1 report.
Suppressing tonight's run is optional (saves tokens; loses nothing but the overnight check).

## LEDGER UPDATES MADE
Appended `status:reported — 2026-07-01` block (A5 disconnect-recurrence finding; A4
ghost-noise P2→P1 refinement + stranded-critical instance; A7 #1102 INFO-level note),
closed the three 06-30 pending verifications (+4 bonus closures), added 5 new pending
verifications (first live close post-#1102 · first #1101 evaluation · 07-02 relearn stays
live-only n=6 · realized_vol_over_hold on a multi-day close · SOFI cooldown expiry/gate
behavior). `audit/area8.md` rewritten for the lens swap.

— END. READ-ONLY respected: no merges, no flag flips, no orders, no migrations; writes
confined to audit/reports/2026-07-01.md, audit/ledger.md, audit/area8.md.
