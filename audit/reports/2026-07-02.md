# Audit v5.1 — 2026-07-02 NIGHTLY (run ~03:00–04:00Z, interactive; first run under the v5.1 nine-area contract)

Baseline = 2026-07-01 report + ledger. Quiet-night run: zero market activity since the prior
report (broker flat, no fills, no scans — next scan 16:00Z today). The night's movement is
infra: PRs #1104 (scanner persist-seam retry, the prior run's TOP-3 #2 — shipped) + #1105
(audit docs) merged 02:39/02:41Z, both workers recycled clean. One NEW finding (A9, the fresh
rotating-slot lens's first audit). No ALERT file — nothing critical (H11: the only critical in
24h is the ledgered 07-01 shadow force-close).
⚠ SCHEDULER COLLISION: the Task-Scheduler nightly (00:23 CT, v5 prompt) writes by local date →
it will target `2026-07-02.md` = THIS file. Operator: skip tonight's scheduled run or expect
an overwrite (this run already covers its window).

## RUNNING STATE (H8) — re-verified this run
- origin/main HEAD `b6a28e1` (#1105) atop `cb8ae28` (#1104). Worker + worker-background BOTH
  **SUCCESS @ `b6a28e1`**, containers started 02:41:38Z > merge 02:41:36Z ✅. The intermediate
  `cb8ae28` deploys were superseded in ~2 min by the docs recycle (rapid sequential merges —
  one discarded build, harmless). Recycle was mid-night: no in-flight cycles orphaned; first
  scheduled work post-recycle is 10:00Z calibration.
- Flags (filtered read, worker; names-only hygiene for URLs): EXECUTION_MODE=alpaca_live,
  LIVE_ENABLED=TRUE, PAPER_AUTOPILOT_ENABLED=1, SCHEDULER_ENABLED=1, RISK_ENVELOPE_ENFORCE=1,
  RISK_UTILIZATION_GATE_ENABLED=1 + RISK_MAX_UTILIZATION_PCT=0.85, POLICY_LAB_ENABLED=true,
  ENTRY_QUOTE_VALIDATION_ENABLED=1; default-ON family env-ABSENT → ON (roundtrip gate,
  cooldown, cohort-stop, quarantine, source-aligned) ✅ — unchanged vs 07-01.
  **`OPS_ALERT_WEBHOOK_URL` + `HEARTBEAT_PING_URL` STILL ABSENT** (standing TOP-3 #1, not
  re-found; now compounded by A9 — see ordering note in TOP-3).
- Broker LIVE 211900084: equity **$2,093.74** == last_equity, cash/OBP $2,093.74, **FLAT**,
  0 open positions, accrued_fees 0, daytrade_count 0. Zero fills since 06-30 16:45Z (verified:
  FILL activities since 06-08 enumerate exactly the known week — no strays).
- DB delta since prior report: positions — only the known shadow SOFI close (07-01 13:30Z);
  suggestions since 07-01 12:00Z: **0**; job_runs 7d: **zero failures across 21 job types**
  (the 3 `result.counts.errors` in-window are the pre-#1098 06-25→29 ingest runs, pre-detector
  — consistent with the detector's 07-01 clean-negative).
- H11 (24h): 1 critical = ledgered 07-01 shadow force_close. 4 high `ops_data_stale` → A9.
- Active cooldown: (c8a3a3b0, SOFI) until 07-02 13:30Z — expiry/gate behavior is a standing
  pending-verification for today's session, not this run.

## PART 1 — WEEK IN REVIEW (06-08 → now), ranked by P&L
No new trades since the prior report — table = 06-30 canonical + 07-01 delta, re-anchored to
tonight's broker pull (fills enumerated, equity walk consistent). LIVE=champion 3d289dca.

| pos | cohort | structure | entry→exit | P&L $ | staged-EV | grade |
|---|---|---|---|---|---|---|
| NFLX | SHADOW c8a3a3b0 | LPD | 06-08→06-16 | **+662.10** | +85.6 | SHADOW-MODELED |
| NFLX | SHADOW ec545555 | LPD | 06-08→06-12 | +133.35 | +85.6 | SHADOW-MODELED |
| NFLX | LIVE | LPD | 06-08→06-12 | **+48.00** | +95.67 | LIVE-BROKER |
| MARA | LIVE | LCD | 06-17→06-17 | −15.00 | +29.7 | LIVE-BROKER |
| MARA | LIVE | LCD | 06-12→06-12 | −28.00 | +29.7 | LIVE-BROKER |
| SOFI | LIVE | LCD 17/20.5 ×5 | 06-30→06-30 (15m) | **−40.00** | +30.6 | LIVE-BROKER |
| SPY | LIVE | IC | 06-11→06-12 | −45.00 | +40.9 | LIVE-BROKER |
| QQQ | LIVE | IC | 06-11→06-15 | −73.00 | +40.9 | LIVE-BROKER |
| NFLX | LIVE | LPD | 06-04→06-08 | −84.00 | (protective close) | LIVE-BROKER |
| QQQ | SHADOW c8a3a3b0 | IC | 06-11→06-11 | −234.78 | +46.1 | SHADOW-MODELED |
| NFLX | SHADOW ec545555 | LPD | 06-03→06-08 | −273.00 | +85.6 | SHADOW-MODELED |
| NFLX | SHADOW c8a3a3b0 | LPD | 06-03→06-08 | −546.00 | +85.6 | SHADOW-MODELED |
| MARA | SHADOW c8a3a3b0 | LCD | 06-12→06-18 | −675.99 | +29.7 | SHADOW (#1071 shadow_only) |
| SOFI | SHADOW c8a3a3b0 | LCD ×17 | 06-30→07-01 (21h) | **−1,044.48** | +30.6 | SHADOW-MODELED (#1017) |

REJECTED/UNFILLED class (A8 inputs, not P&L): XLE dead-leg forks 06-10 (UNMARKABLE by
doctrine); 06-30 conservative SOFI fork REJECT (edge_below_minimum, EV 19.1 — beat both
accepting books); `ev_below_roundtrip_cost` rejects: **0 lifetime** (gate armed 06-30,
nothing has reached the stage seam since); 07-01: 382 scanner rejections, 0 emitted.
Rollups — **cohort:** LIVE realized **−$237** (7 closes) · SHADOW **≈−$1,979** (7 closes,
#1017 mark-bias). **family:** LPD live −36/shadow −24; IC live −118/shadow −235; LCD live
−83/shadow −1,720. **symbol:** NFLX both tails; SOFI now the largest shadow loss.
**WEEK VERDICT:** live realized −$237, open unrealized $0 (flat), fees ≈$1–2/RT (accrued 0);
every staged EV positive (+29.7…+95.7) vs aggregate-negative realized = the known raw-mode
optimism ≈−$67/trade post-#1051 — uncalibrated BY DESIGN until 8 live closes (now 6).

## PART 2 — NINE AREAS

**A1 PROFITS — UNCHANGED.** 0 new fills since the prior report; the EV-vs-realized story is
unchanged (raw-mode optimism, ledgered; #1101 closed the SOFI execution-cost class at entry).

**A2 LOSSES — UNCHANGED.** No new force-closes/envelope events since the ledgered 07-01
shadow stop (controls verified exercising correctly in that report). Book flat overnight.

**A3 SELF-LEARNING — UNCHANGED (counter re-verified).** 30d outcomes: live n=6 post-epoch
(sum −153) / paper n=9 (5 post-epoch, sum −1,870.80) — relearn counter **6/8**, live-only
training holds. Today's 10:00Z relearn must show sample_size=6 (the −1,044.48 shadow row
is_paper=true MUST NOT appear) — standing pending-verification, fires after this run.

**A4 SELF-SUSTAINING — recycle-recovery clean; the next silent-staleness question is answered
by A9.** Tonight's double recycle (02:39/02:41Z) landed mid-night with no in-flight cycles;
both workers healthy at `b6a28e1`. Webhook/heartbeat remain operator-owned (standing).
OUTPUT_FRESHNESS registry: the job-freshness arm EXISTS and fires — but mislabeled (A9);
fixing A9's wiring is a precondition for trusting that arm's signal at all.

**A5 EFFICIENCY — loop self-audit + one system note.** This run: **11 SQL** (≤12 ✓ — but 3
were column-name misses; lesson pinned: introspect `information_schema` FIRST, it costs one
query and saves three), **3 broker calls** (≤4 ✓), **0 subagents** (≤8 ✓), no full-file
reads >300 lines ✓, prior-session H8/H11/deploy pulls REUSED not re-pulled ✓ (the main
economy win). One honest miss: Q9's `persist_failures` JSON path was wrong (real key:
`result.counts.rejection_persist_failures` per `options_scanner.py:311`) — immaterial on a
zero-scan night; noted so tomorrow's verification queries the right key. System note: rapid
sequential merges cost one discarded Railway build (cb8ae28, ~2min lifetime) — harmless;
sequencing doc-PRs a few minutes after code-PRs avoids it. Persist-retry (#1104) is SHIPPED —
excluded; first live test at today's 16:00Z scan.

**A6 VOLUME — UNCHANGED.** 0 scans/0 suggestions since the prior report (pre-market run);
roundtrip gate still 0 evaluations lifetime. First-exercise verification stands for 16:00Z.

**A7 TIME-IN-TRADE — UNCHANGED.** No new closes; hold distribution and the Phase-3 accrual
math (~1 informative live close/week → reopen gate months out) stand as reported 07-01.

**A8 NEGATIVE-DECISION EFFICACY — RESTORED (owner correction) + audited: NO FINDING.**
Restored per the v5.1 one-time correction (the 07-01 swap to Entry-Fill Efficacy was adopted
in error; EFE retired, preserved in area8.md SUPERSEDED section — move-don't-lose honored;
correction recorded in the ledger). This run's audit: negative-decision population delta
since prior run = **0** (no scans); 30d ratio and reconstructability unchanged from ledgered
baselines (d8_v1 rejects 0% repriceable; dismissed/blocked suggestions 100% via order_json);
the new fully-reconstructable reject class (`ev_below_roundtrip_cost`) is N=0. The 06-30
conservative-fork REJECT (EV 19.1 vs −40/−1,044.48 accepts) remains the strongest single
counterfactual datapoint — N=1, illustrative, not a verdict. Look-at item 6 updated in the
spec to include the roundtrip-reject class each run.

**A9 SELF-EXTENSION — LENS CHOSEN FRESH: ALERT & SIGNAL INTEGRITY. FINDING on first audit.**
- WHAT: **83% of a high-severity alert class is self-contradictory.** `ops_data_stale` fired
  69× in 30d; **57/69** carry "Market data is stale … Stale: 0 (). Reason: ok" with
  `stale_symbols=[]` — the message asserts market-data staleness while its own payload says
  market data is fine (one instance fired at `age_seconds=54`).
- WHERE: `jobs/handlers/ops_health_check.py:117` — `is_data_stale = market_freshness.is_stale
  OR job_freshness.is_stale`; but the alert message (:141-143) and details (:144-149) are
  built from `market_freshness` ONLY. Every firing of the job arm wears a market-data costume.
  The correct `stale_reason` is computed at :120-121 and goes only to `issues_found`, never
  into the alert. Fingerprint (:124-128) hashes empty `stale_symbols[:5]` + source → all
  job-arm firings share one fingerprint (cooldown can suppress a genuinely NEW condition as a
  repeat, and the alert can never say WHICH table/job was stale).
- WHY #1: the alert layer is every other area's instrument, and it deceived its own audit —
  the 07-01 report mislabeled this exact class "chronic calibration-freshness artifact"
  because it read the message. A dishonest alert is a phantom-read generator system-wide.
- IMPACT (quantified, this system's data): 57 misleading HIGH alerts/30d ≈ **2–4 per RTH
  day** entering the H11 sweep; the day `OPS_ALERT_WEBHOOK_URL` is set (standing TOP-3 #1),
  that volume egresses to the operator channel and buries the criticals it exists to carry.
  Realized diagnostic cost: one audit report (07-01) already misdiagnosed it.
- HOW (additive, no predicate loosening): thread the FIRING arm into the alert — message from
  `stale_reason` (:120), details gain `trigger_source: market|job`, `job_freshness.reason`,
  `job_freshness.age_seconds`; fingerprint per-arm (job-arm fingerprint keyed on the stale
  job name). The job arm's 30-min-vs-daily-cadence predicate itself is the LEDGERED
  2026-06-10 root cause (`ops_health_service.py:198-227`, threshold 30min vs 1×/day jobs) —
  cited, not re-found; the mislabel wiring is the NEW surface.
- EVIDENCE: SQL aggregate (69/57/57, universe sizes 8–11, span 06-12→07-01) + row detail
  (4× 07-01 rows verbatim) + code read `ops_health_check.py:100-170` +
  `ops_health_service.py:198-227`. Two-source (DB + code) agreement = FINDING.
- RISK: fix is observability-content only; touches no close/stop/decision path. CONFIDENCE:
  HIGH.
- Spec persisted to `audit/area9.md` (slot had no incumbent; graduation NOT proposed — one
  run, one finding; the bounded-growth rule requires multi-instance + owner sign-off).

## TOP-3 (value / effort / risk)
1. **Fix the data_stale mislabel BEFORE (or with) webhook arming** — A9's wiring fix
   (`ops_health_check.py:139-149` + per-arm fingerprint), additive, small. Sequencing matters:
   arming the webhook first (standing TOP-3) would pipe 2–4 false highs/day to the operator.
2. **Set `OPS_ALERT_WEBHOOK_URL` + `HEARTBEAT_PING_URL`** — operator, minutes. Standing item
   (3rd consecutive report); every critical remains DB-only until set. Pair with (1).
3. **Ghost-sweep shadow scoping P2→P1** — carried from 07-01 unchanged (58 noise warns/2d
   from one shadow position); additive scoping, zero blast radius.

## CONFLICTS & SYNERGIES
- (1)+(2) are order-coupled: (2) without (1) = cry-wolf channel from day one. (1)+(3) share
  the alert/persist-reliability surface — one post-close PR window could carry both.
- A9's fix surface overlaps the ledgered severity-map/threshold items on ops_health — the
  wiring fix should NOT silently retune the 30-min threshold (that's the ledgered predicate
  item, separate decision).
- The A8 restoration + A9 adoption are complementary, not competing: A8 audits decision
  outputs (rejects), A9 audits the instrument layer every area reads through.

## FOUR-SOURCE DISAGREEMENTS
- Dollars/positions: none (broker = DB = prior reports everywhere checked tonight).
- ONE intra-source disagreement IS the A9 finding: `risk_alerts.message` contradicts
  `risk_alerts.metadata.details` on 57/69 rows — reported, not averaged.

## CONTRACT CONFIRMATIONS
- **A8-swap reversal RECORDED** (ledger one-time correction + area8.md restored with the EFE
  spec preserved under SUPERSEDED — move-don't-lose honored). Entry-Fill Efficacy is RETIRED,
  not relocated; it may compete for A9 in future runs with no incumbency.
- **A9 lens chosen fresh:** Alert & Signal Integrity (spec at `audit/area9.md`), audited this
  run at full evidence standard. **No graduation proposed.**
- READ-ONLY respected: no merges, env changes, orders, or flag flips; writes confined to
  `audit/reports/2026-07-02.md`, `audit/ledger.md`, `audit/area8.md`, `audit/area9.md`.

— END. Awaiting owner: which findings to implement and in what order.

---

## POST-REPORT DISPOSITION (same night, operator-directed; details in ledger)

All three TOP-3 items executed in the order-coupled sequence the report specified:
1. **#1106 data_stale content fix** — merged, both workers SUCCESS @ `91b1319` 03:20:55Z.
2. **Egress arm** — operator set both vars ~03:35Z; var-change recycle SUCCESS 03:35:44Z.
   `OPS_ALERT_WEBHOOK_URL` is LIVE (code-read confirmed). ⚠ `HEARTBEAT_PING_URL` is INERT —
   no code reads it; the external dead-man's switch was never built (only the A4 half of
   oversight Window 1 shipped). Pre-staged env-first; do not count it as armed.
3. **#1107 ghost-sweep live scoping** — merged, both workers SUCCESS @ `6898bf9` 03:44Z.
Pending verifications for today's session are consolidated in `audit/ledger.md`.
