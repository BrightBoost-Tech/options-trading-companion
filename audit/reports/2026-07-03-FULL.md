# AUDIT v5.3 — FULL (Weekend Deep-Dive) — 2026-07-03 (July-4th-observed holiday)

Mode: FULL · READ-ONLY held · Budgets: SQL 16/20 (4 inline + 12 across 4 subagents) ·
broker 2/6 · subagents 4/12 · full-file reads: 24 files (listed per pass below).
Running SHA both workers + BE: `e0bbe6e` (verified, no unexpected recycle; the two
var-change recycles at 15:18Z are the T-phase arming, ledgered). Broker: FLAT,
equity = last_equity = $2,093.74, OBP $2,093.74, zero orders. Market CLOSED (holiday;
next open 07-06). H11: un-acked critical/high = 4 — ALL today's holiday `ops_data_stale`
highs (13:37/14:37/15:37/16:37Z; see A9/A10). No ALERT file: no capital-risk critical.

## PART 1 — WEEK IN REVIEW (frozen since 07-01; deltas only)
Verdict line stands: live −$237 / 7 closes · relearn 6/8 live post-epoch (re-verified:
exactly 6) · raw mode holds. Zero closes since 07-01. The 07-02 SOFI gate reject remains
the week's only staging decision (correct by arithmetic; re-confirmed at row level, see
A8). Typed-segment NULL residual: exactly 1 of 98 (the no-suggestion legacy row) —
backfill intact. signal_accuracy baseline: 1/6 wins, Brier 0.2751, correctly below the
n≥8 alert gate.

---

## THE HEADLINE (TOP-3 #1) — #1126 viability bias is BUILT-NOT-WIRED; the armed flag is INERT

`rank_suggestions_canonical` (where the bias lives, `canonical_ranker.py:238-239`) has
**zero production callers** — grep: tests only. Production candidacy ordering is a LOCAL
`sort_key` in `paper_autopilot_service.get_executable_suggestions` (:118-131) that
recomputes `compute_risk_adjusted_ev` directly (:105-116) and never touches the bias.
Arming `UNIVERSE_VIABILITY_BIAS_ENABLED=1` (done 15:18Z today) changes nothing anywhere.
- Confirmed twice: subagent full-file read + primary auditor independent grep/read.
- The shipped tests (10, all green) pin the ORPHAN function — the exact `9a2cef1`
  DOC≠BUILT class §8 warns about, self-inflicted same-day, caught by this audit's PASS-2.
- CONSEQUENCE: Monday's ledgered pin ("16:00Z scan shows the reorder") is VOID until
  wired. The in-isolation properties (strict '=1', sort-key-only, positive-only) are all
  true — the control is simply unreachable.
- FIX (Monday post-close, one call site): apply `_viability_rank_key` inside
  `get_executable_suggestions`' sort (flag-gated), + a wiring test that pins the
  EXECUTOR path (not the orphan), + fold into M4's window as item 0. Leave the env var
  armed — it is inert either way and correct once wired.

## PART 2 — AREA BLOCKS (PASS 1 logic · PASS 2 code · PASS 3 value)

### A1 PROFITS
- P1 LOGIC: **NEEDS-REFRAME.** "Profits" at 7 lifetime closes with governance that has
  NEVER executed is the wrong lens. Reframe: *governance readiness & comparison honesty*
  until a challenger reaches Gate 4. Volume math (code-verified): PROMOTION_WINDOW=7d ·
  MIN_TRADING_DAYS=3 · **MIN_TRADE_COUNT=10 ← the standing binding gate** ·
  margin 0.15 · posterior 0.70 · AUTOPROMOTE OFF (recommend-only). Fork ceilings
  (neutral ≤3/day·3 slots, conservative ≤2/day·2 slots, champion-clone-only) put 10
  closes/7d at the cohorts' theoretical MAXIMUM churn; at actual volume promotion is
  **frozen** — consistent with learning-mode, but the binding constraint is volume, not
  utility.
- P2 CODE (files fully read: evaluator, scoring, promotion_normalization, config, fork):
  **F-A1a (MED, latent):** `check_rollback` mis-restores after a "recommended" row —
  recommendations are INSERTED into `policy_lab_promotions` even with AUTOPROMOTE off
  (evaluator.py:552-565) and both the rollback's "previous champion" read (:657-666) and
  the Gate-7 cooldown (:499-520) consume them; an interleaved recommendation inside a
  rollback window nets NO champion → ungated fallback. Inert today; live the moment
  promotions move. Fix shape: skip the insert when not auto-promoting, or filter reads
  to `auto_promoted=true`.
  **F-A1b (MED):** "multi-factor utility" is structurally single-factor —
  `expected_shortfall`/`execution_quality`/`calibration_quality` are NEVER WRITTEN by
  `evaluate_cohorts` (:109-121) so tail/slippage penalties are permanently 0; the
  concentration penalty inputs are never populated; the sole live penalty is
  unit-mismatched (drawdown FRACTION ×2.0 ≈ ≤$0.40 vs dollar P&L). Gates 4/5 compare
  ≈ Σ realized_pnl. (Gates 3/6 still read drawdown directly.)
  **#1124 verified MATCHES** its registry claim (+2 recorded observations: the 0.31
  discount would also hit a future live-routed challenger; symmetric loss-shrink eases
  Gate 4 in both-losing regimes — honest under the model, adverse-fill-selection
  unmodeled). `policy_lab/fork.py:68-71` docstring lies about promoted_at writes.
- P3 VALUE: **EARNING** (this area's lineage produced #1124 + the comparability caveat).

### A2 LOSSES
- P1 LOGIC: **SOUND** (live capital; breaker HOT).
- P2 CODE: **F-A2a (MED): the one-beta bucket {SPY,DIA,QQQ,IWM} is genuinely
  uncontrolled at block level** — inventory: `max_correlation_cluster_pct` declared
  (risk_envelope.py:58, 0.60) and **never read anywhere** (config fiction; from_env
  doesn't even load it); ranker correlation factor is same-symbol-only
  (canonical_ranker.py:149-164, TODO admits it); sector check warn-only and ETFs land in
  one accidental "Unclassified" bucket; stress `correlation_one` warn-only; the 25%
  block is per-symbol. The viability bias (once wired) steers candidacy INTO this bucket
  — 3 of 6 viable names are the same trade. ADDITIVE control candidate (owner):
  bucket-aware correlation factor / read the dead knob — never loosens anything.
  **F-A2b (LOW-MED):** "per-symbol" loss envelope is per-POSITION (risk_envelope.py:
  445-482) — two same-symbol positions each at −2.9% never trip the −3% "name" cap;
  §5 wording inherits the overstatement. Near-inert at current book shape.
  **F-A2c (LOW):** breaker: NULL/0 pnl on a loss row silently breaks the streak
  (streak_breaker.py:138 `or 0` default). **F-A2d (LOW):** the fail-closed contract has
  a wrapper-import-seam skip (paper_learning_ingest.py:177-182) doctrine words slightly
  stronger than built. **F-A2e (LOW, runbook):** while the trailing-3 stays all-losses,
  the critical RE-EMITS every nightly ingest and an un-pause without a new WIN re-trips
  on ZERO new closes — "un-pause is futile until a win lands" belongs in the recovery
  runbook. Next-losing-close fire-chain enumerated and verified (envelopes → cooldown →
  breaker tail); no double-write path; skip paths enumerated (F-A2c/d + insert failure).
  #1044/#1071/#1119 registry claims all MATCH code.
- P3 VALUE: **EARNING.**

### A3 SELF-LEARNING
- P1 LOGIC: **SOUND, with a decision to frame** (owner): at ≤6 viable names and the
  measured ~1 live close/week, 8/8 is ~2 weeks away IF cadence holds — but the honest
  range is "2 weeks to indefinitely." Raw-mode-indefinite is SAFE (×1.0 do-no-harm) —
  the decision is whether to accept that as steady state or treat volume as the fix
  (already partially chosen: bias arming + MRK/CVX conditionals). RECOMMENDATION:
  accept-with-review at close #8 (the convergence rule already in doctrine); no new code.
- P2 CODE: calibration flags ALL verified built (live-only, epoch, TTL, escalation, floor
  dates — table in the A3/A4 agent report). One strict-parse note: `CALIBRATION_ENABLED`
  `=="1"` — `true/on` silently DISABLES the master switch (same class as
  SCHEDULER_ENABLED; neither on §3's strict-parse warning list). Typed segments + A4
  fields + dedup verified end-to-end.
- P3 VALUE: **EARNING.**

### A4 SELF-SUSTAINING
- P1 LOGIC: **SOUND.**
- P2 CODE: **F-A4a (MED): stuck-`running` job_runs have NO REAPER** — a mid-run recycle
  orphans the row permanently (claim RPC takes queued/failed_retryable only; scheduler
  retry takes failed_retryable; admin endpoint refuses `running`; same-day re-trigger
  returns the stuck row). Every merge recycles, and the 16:00–17:00 CT learning chain
  overlaps the post-20:00Z merge window — realistic, not theoretical. Detection today is
  indirect (job_late 26h / data_stale 360m). Fix shape: startup or scheduled reaper that
  marks stale `running` rows failed_retryable after a TTL.
  **F-A4b:** next OUTPUT_FRESHNESS registration = `suggestion_rejections.created_at` @
  96–120h (highest-frequency unwatched durable output; #1104 is its live precedent; also
  guards A8's data). Caveat: no weekend exclusion in get_output_freshness → 120h absorbs
  holiday runs at ~1 day extra latency.
  **suggestions_open 3× mystery RESOLVED — benign:** every extra run is the operator CLI
  force path (`run_signed_task.py --force`, nonce-keyed, user-scoped); 10 scheduled + 5
  force = the 15 observed; daily idempotency held perfectly. Residual: a force run landing
  between 16:00Z scan and 16:30Z executor could double the day's pending set — worth
  knowing, not fixing. Recycle-recovery map delivered (orphans: in-flight job_runs,
  ≥5-min-straddled cron fires, cooldown fingerprints of a crashed check; self-heals:
  relay marks, ingest dedup, breaker pause, calibration blob, heartbeat).
  Stale-doc note: "HEARTBEAT_PING_URL inert" claims in older ledger entries are
  historical (append-only); the reader is #1109, built + proven.
- P3 VALUE: **EARNING.**

### A5 EFFICIENCY
- This run: 16/20 SQL · 2/6 broker · 4/12 subagents · report within cap. Prior-session
  H8/broker pulls reused; zero wasted column-miss queries (introspect-first held).
- The 77 dead names: ~92% of daily scan work (chains, quotes, IV, rejections) produces
  structurally-unusable candidates. M4's strong-tier prune (7 names) ≈ 8% of scan/API;
  the HELD second tier ≈ another 12%. Real but small in dollars; the bigger cost is
  ATTENTION (rejection-row noise into A8's data). Verdict: budgets honored; prune value
  correctly characterized as A5-minor.

### A6 VOLUME
- P1 LOGIC: **NEEDS-REFRAME.** "Why so few trades" is ANSWERED (1-of-84, structural;
  quiet days are CORRECT). Remaining question: *are the ≤6 viable names handled
  correctly, and does the (wired) bias change their candidacy rate?* Adopt that framing.
- P2 CODE: gate ORDER documented (see A6/A7/A8 agent report — differs slightly from
  doctrine sketch; iv_rank sits before strategy selection). **No mechanical over-reject
  on the viable 6** (14d): binding gates are execution_cost_exceeds_ev + selector HOLDs
  (economics, working as designed). Notes: **SLV is structurally benched until ~Sept**
  (iv_rank_insufficient_history every cycle since the 06-17 add — correct H9 behavior;
  the viable-set tier for SLV is aspirational until warmup or seeding); NFLX
  spread_too_wide_real ×10 — the flat $0.20 ABSOLUTE_SPREAD_THRESHOLD has no high-price
  analogue of #1047's re-key (calibration QUESTION, not wrongness). LOW: scanner
  RejectionStats built without job_run_id (plumbing exists, never passed) — rows
  joinable only via cycle_date.
- P3 VALUE: **MARGINAL as framed / EARNING once reframed** (it produced the universe
  verdict that reshaped strategy this week).

### A7 TIME-IN-TRADE
- P1 LOGIC: **WRONG-QUESTION at current state.** 1 close/week, CLOSE_FILL_GAP live
  datapoints = 0 (confirmed at row level), resting-TP pilot NO LONGER RESTING (0 open
  GTC TPs — book flat; unexercised, not broken). Hold-time is not a lever anyone can
  pull at n=7. **PROPOSAL: merge A7 into A1/A3 as a dormant checklist line until ≥10–15
  live close fills (the same threshold as the Phase-3 gate) — reinstate then.**
- P2 CODE: exit flow verified end-to-end (#1079 → clamp → cohort conditions → #1072
  defer-before-staging → single-submitter → #1017 → #1102 stamps). All 8 exit-territory
  flags read-site/polarity verified (table in agent report). TWO registry corrections:
  **(1) kill-switch COUPLING — flipping `ENTRY_QUOTE_VALIDATION_ENABLED` off silently
  disables the #1101 roundtrip gate too** (gate no-ops on empty entry_leg_quotes,
  paper_endpoints.py:1217-1218; docstring knows, §4 doesn't). **(2) GTC pilot-list
  semantics: UNSET pilot list = ALL eligible live-routed positions**, not pilot-off
  (gtc_profit_exit.py:194-198) — §4's "pilot scope" wording implies narrower.
- P3 VALUE: **MARGINAL → RESTRUCTURE-OR-MERGE** (proposal above).

### A8 NEGATIVE-DECISION EFFICACY (standing)
- P1 LOGIC: **SOUND** — this week is its richest input ever (2,641 rejection rows/14d;
  the SOFI reject; the universe class-verdict).
- Row-level spot-check (14d): **spread_too_wide_real (566): 100% carry per-leg quotes**
  → executable-cost-at-decision fully reconstructable, BUT per_leg omits OCC/expiry/type
  → counterfactuals are *approximate* (strategy_key + DTE-rule approximation).
  **execution_cost_exceeds_ev (376): EV+cost aggregates only, 0% leg identity** →
  unmarkable from the row. **ev_below_roundtrip_cost: NOT in the rejection ledger at
  all** — one last-writer-wins stamp on the suggestion (the 92-vs-88 "discrepancy" is
  RESOLVED: two real evaluations on fresh quotes; each log line true; the stamp keeps
  only the last; a gate-rejected suggestion stays same-day re-stageable because dedup
  keys on order rows). **Spot-at-decision: 0% across every class — the area8 proxy
  field remains ABSENT** (RESEARCH item stands, now quantified; partial mitigation:
  option_liquidity_observations carries underlying_price at cycle grain).
  Unmeasured:measured trend: dead-leg class (unmarkable-by-doctrine) excluded; of the
  rest, ~64% of spread-class rows are markable-approximate, ~0% of EV-class rows.
- P3 VALUE: **EARNING** (the roundtrip gate + universe verdicts trace here).

### A9 ALERT & SIGNAL INTEGRITY (standing — GRADUATED, owner decision 2026-07-03,
first exercise of the graduation rule; recorded in the ledger this run)
- P1 LOGIC: **SOUND** — and this run proves the lens generalizes beyond its founding
  finding. Nine findings, ranked (full detail in the A9 agent report):
  **F1 (MED): `force_close` is a costume worn by three realities** — submitted / FAILED
  to submit / warn-only-nothing-closed all share the type; **31% of historical rows
  (109/356) describe no close**; post-epoch, all three shapes relay to the phone under
  one critical title, demanding opposite operator responses. Fix: split types
  (`force_close_failed`, `envelope_violation_warn_only`).
  **F6 (MED): the "[OPS_ALERT] no supabase client — webhook-only legacy mode" WARNING
  fires on every DESIGNED client=None egress** (relay + allowlist path) — a reader
  triaging an incident is told the pipeline was degraded when it worked. Fix: explicit
  channel2-only wording/param. NOT on §8.
  **F7 (MED): severity vocabulary fragmentation** — `medium` (533/30d) and `warn`
  (828/30d) are the two LARGEST warning-class buckets and both off-vocabulary; a reader
  filtering `severity='warning'` misses **83%** of warning-class rows. `_VALID_SEVERITIES`
  guards only the alert() path. NOT on §8.
  **F2 (MED): `alert_type="warn"` carries zero semantics** (706 rows/30d; high ones
  would relay as "ERROR: warn").
  **F8 (LOW-MED):** the A4 silent-failure detector reads ONE result convention
  (`counts.errors` — only paper_learning_ingest emits it); executor 'partial' errors are
  invisible. §8-class entry, unlisted.
  **F4 (LOW):** the row's stored `metadata.fingerprint` ≠ the fingerprint that dedups
  (recomputed over full details incl. age_seconds) — an auditor joining them concludes
  dedup is broken.
  **F5 (LOW, doc-lie):** "at most one benign data_stale per holiday" docstring vs true
  rate ~7/holiday (today's 4-by-16:37Z is live evidence).
  **F3 (LOW):** job_succeeded_with_errors marks "sent" + arms cooldown without checking
  the insert succeeded — a dropped alert isn't retried.
  **F9 (LOW):** EXIT_EVAL_DEBUG's "now honest" claim holds ONLY on cohort paths; the
  default path prints flat 0.35 while the decision time-scales (±43% of entry cost) —
  §8's liar entry must NOT be retired.
  **Cooldown-vs-cadence (#1114) VERDICT: volume NOT doubled** — suppression binds on
  alternate runs (today's hourly :37 anchor is the proof) but the margin is
  SECONDS-thin (jitter could intermittently yield 2×/hour). chain_mechanics: already
  backlogged, verified honest-message/no-dedup, in-scope of the existing item.
  signal_accuracy content: honest (one inherited is_paper-seam note).
- P3 VALUE: **EARNING** — founding finding shipped #1106+#1115 with measured 39→0
  false-HIGH elimination; this run adds a 9-finding taxonomy cluster.

### A10 SELF-EXTENSION — lens ADOPTED: **CALENDAR & CLOCK INTEGRITY** (audit/area10.md)
- WHY THIS LENS: five time-boundary failures/near-misses in 72 hours, and no area owns
  the class: (1) #1123 — a hardcoded test expiry crossed its DTE window at midnight UTC
  and broke ALL CI (shipped fix); (2) the watch→merge chain fired into a presumed-RTH
  that was actually a holiday (false alarm retracted, P2 guard filed); (3) holiday-blind
  `is_us_market_hours` → 4-7 false HIGHs per holiday + a docstring claiming "at most
  one" (A9-F5); (4) **NEW (A3/A4 agent): winter-close asymmetry — the open side got the
  14:30Z warm-up anchor but the close side is hardcoded 20:00Z, so in EST the final
  session hour (20:00–21:00Z) has data_stale suppressed AND `_rth_job_status`
  unconditionally 'ok' — a dead monitor in that hour is UNDETECTED; benign until
  November**; (5) the cooldown-vs-cadence suppression that works by a 2-second offset.
  A1–A9 audit components; none audits the calendar seams BETWEEN them.
- IMMEDIATE AUDIT (this run): the five instances above + sweep verdicts — scheduler
  day_of_week=mon-fri is holiday-blind BY DESIGN (jobs no-op safely; staleness gate
  blocked today's executor correctly — control working); `_weekend_excluded_age`
  UTC-weekend skew documented/accepted; `_RTH_WARMUP_OPEN_UTC` correct for winter opens;
  #1123's fixture sweep found the other 3 hardcoded test expiries concentration-only.
  NEW finding count from the lens: ONE (the winter-close blind hour) + one doc-lie
  (A9-F5 shared). Spec, look-at list, disqualifiers persisted to `audit/area10.md`.

---

## AREA SCORECARD (FULL deliverable)

| Area | Logic | PASS-2 findings | Value | Recommendation |
|---|---|---|---|---|
| A1 | NEEDS-REFRAME → governance readiness | 2 MED (rollback ghost-restore; single-factor utility) | EARNING | reframe; fix F-A1a cheap |
| A2 | SOUND | 1 MED + 4 LOW | EARNING | bucket-correlation control (owner) |
| A3 | SOUND + decision to frame | strict-parse note | EARNING | accept raw-mode w/ review at #8 |
| A4 | SOUND | 1 MED (no reaper) + registration pick | EARNING | reaper P2; register suggestion_rejections |
| A5 | SOUND | budgets honored | EARNING (cheap) | none |
| A6 | NEEDS-REFRAME → viable-set handling | 2 LOW notes | EARNING once reframed | adopt reframe |
| A7 | WRONG-QUESTION at n=7 | 2 registry corrections | MARGINAL | **MERGE into A1/A3 until ≥10–15 fills** |
| A8 | SOUND | reconstructability gaps quantified | EARNING | spot proxy field stays RESEARCH; EV-class rows unmarkable |
| A9 | SOUND (standing, graduated) | **9 findings** | EARNING | taxonomy PR (owner) |
| A10 | adopted: Calendar & Clock Integrity | 1 NEW + 1 shared | first run | keep lens |

## OWNER-DECISION LIST (proposals only — nothing enacted)

1. **Authorize the F1 wiring fix** (bias → executor sort, one call site + executor-path
   test) as item 0 of Monday's M4 window. The Monday "bias live" pin is VOID until then.
2. **Alert-taxonomy PR** (A9 F1+F2+F7+F6 as one post-close PR): split force_close types,
   give "warn" real types, normalize `medium`/`warn` severities, fix the legacy-mode
   wording. Direct consequence: the relay/phone channel stops lying post-epoch.
3. **One-beta-bucket control** (A2): read the dead `max_correlation_cluster_pct` knob or
   make the ranker correlation factor bucket-aware ({SPY,DIA,QQQ,IWM}). Additive only.
4. **A7 merge proposal** (dormant until ≥10–15 live fills — Phase-3's own gate).
5. A1/A6 reframes (wording of the audit contract, owner-enacted).
6. F-A1a rollback/cooldown recommendation-row fix (cheap, latent-until-promotions-move).
7. F-A4a stuck-`running` reaper (P2).
8. OUTPUT_FRESHNESS: register `suggestion_rejections` @ 120h.
9. §4 registry corrections: ENTRY kill-switch coupling note; GTC pilot-list unset
   semantics; §8 additions (A9 F6/F7/F8; EXIT_EVAL_DEBUG entry stays).
10. Winter-close blind hour (A10): fix alongside the next ops_health PR before November.

## CONFLICTS & SYNERGIES
- F1 (bias inert) × M4: fold wiring into M4 as item 0 — one recycle serves both.
- A2 bucket gap × M4's DIA add: adding DIA to the viable set (once bias is wired)
  sharpens the one-beta concentration — sequence the bucket control WITH or before it,
  or accept short-term with the ledgered note.
- A9 taxonomy × relay: fix the force_close costume BEFORE the next live force-close
  egresses (all three shapes currently share one critical phone title).
- A10 winter-close × #1115: same file/paths; one PR can carry both when scheduled.

## FOUR-SOURCE DISAGREEMENTS
- CODE vs LEDGER/docs: F1 (registry says armed control; code says unreachable) — the
  audit's job, resolved as finding. Older "HEARTBEAT inert" ledger claims superseded
  (append-only history, no edit needed). No SUPABASE/RAILWAY/ALPACA contradictions:
  flat book agrees everywhere; SHA agrees; alerts agree with today's holiday model.

## GRADUATION / RETIREMENT
- **A9 GRADUATION RECORDED** (owner decision 07-03, first exercise of the rule) —
  ledger entry this run; area9.md header frozen as STANDING.
- **Retirement-candidate tracking starts this run** (6-no-finding-run rule): all
  standing areas produced findings this run → all counters at 0.
- A10 slot: filled by Calendar & Clock Integrity (this run). No graduation proposed
  (rule requires multi-instance finding that SHIPPED a control; the lens is new).

— END (READ-ONLY held; doc writes: this report · ledger status:reported + graduation ·
area10.md created · area9.md header freeze. Human acts in the morning.)
