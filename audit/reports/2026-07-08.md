# AUDIT v5 — NIGHTLY — 2026-07-08 (Wed 00:01 CT scheduled run)

**Mode:** NIGHTLY (DOW=3; FULL is Sundays only). **Window:** 2026-07-06 05:01Z →
2026-07-08 05:01Z (covers 07-06 RTH + the 07-06 post-close M4/#1132 ship, and
07-07 RTH + the 07-07 post-close #1134 taxonomy and #1135 edge-trigger ships).
**STEP 0 clock grounding:** DB `now()` = 2026-07-08 05:01:50Z = 00:01 CT, DOW 3 —
agrees with the prompt header and the midnight-CT nightly cadence. No premise
correction needed.

**Budget:** ~22 SELECT-only SQL · 3 Railway reads · 1 Explore subagent · 0 broker.
**⚠ RUN LIMITATION:** the `alpaca-live`/`alpaca-paper` MCP tools did not surface
this session (repeated ToolSearch returned none). Broker snapshot was NOT read
directly. The live QQQ round-trip below is corroborated three ways in DB
(`paper_orders.execution_mode='alpaca_live'` + `broker_status='filled'`,
`fill_source='alpaca_fill_reconciler'` which reconciles against Alpaca fills, and
`learning_trade_outcomes_v3.is_paper=false`). Equity/OBP not re-read; last known
$2,093.74 (07-06) − QQQ −15 − fees ≈ $2,078.7 (DB-derived, not broker-confirmed).

---

## 1. ALERT

**None.** No un-handled critical/high finding. The window's 6 criticals are all
designed controls or already-remediated: 4× `alpaca_options_buying_power_query_failed`
(07-06 incident, M4-fixed same night, all `resolved=true`); `force_close QQQ` (the
designed stop, functioning control); 2× `streak_breaker_tripped` (07-06 resolved,
07-07 the standing pause). No ALERT-2026-07-08.md written.

---

## 2. GROUND-TRUTH DELTA vs prior run (07-06)

The book was **NOT quiet** — the 15-day flat stretch ended.

- **H8 CLEAN:** worker + worker-background both SUCCESS @ `be137338` (#1135
  edge-trigger breaker) = origin/main HEAD; container start 07-07 22:17:35–36Z;
  prior `5809505`(#1134) and `7cddddd`(#1132) REMOVED. Merged = running.
- **07-06 RTH — inverted-universe OBP incident (self-resolved by M4 that night):**
  Alpaca nulled retired PDT fields → `int(None) TypeError` in the OBP serializer →
  $500 baseline → micro tier → **168 `micro_tier_underlying_too_high` rejects**,
  0 candidates; 4× critical `alpaca_options_buying_power_query_failed`. M4 (#1132)
  shipped 07-06 evening.
- **07-07 RTH — M4 HELD + first LIVE fill since 06-30:** 0 micro_rejects, 0 OBP
  TypeErrors, 76 symbols scanned (642 rejections). A **LIVE QQQ iron condor**
  (`386a39fe`, aggressive cohort `3d289dca`) staged+filled at an off-schedule
  14:37Z executor run, force-closed 17:45Z on `intraday_stop_loss`, realized
  **−$15.00** (broker). First live round-trip since SOFI 06-30.
- **Streak breaker:** re-tripped 07-06 21:20Z (window SOFI−40/MARA−15/QQQ−73) and
  again 07-07 21:20Z — window ROLLED (QQQ−73 dropped, QQQ−15 added) →
  `tripped:true, paused_written:true`. `entries_paused=TRUE` now (since 07-07
  22:22Z), reason "QQQ −15 · SOFI −40 · MARA −15". **Operator un-pause required
  before the next session.**
- **A3:** post-epoch live closes 6 → **7** (QQQ added); 07-07 10:00Z calibration ran
  pre-close at `sample_size:6` → raw mode holds (needs 8). Live champion cumulative
  **1 win / 7 closes, −$168** (hit-rate 14.3%, Brier 0.296; IRON_CONDOR 0/3, Brier 0.414).
- **Jobs:** all `succeeded`, 0 failures/0 stuck across 72h; order_sync avg 1.32s
  (#1049 intact); iv_daily_refresh 238s (heavy but nominal). No new cooldowns beyond
  QQQ (`cohort_stop_force_close`, until 07-08 13:30Z).
- **H11 baseline:** critical/high since prior run = the designed set above; nothing
  stranded (all criticals egressed via relay or inline; `force_close` egressed 18:07Z).

---

## 3. PENDING-VERIFICATIONS (from 07-04/07-06 Monday list + 07-06/07-07 post-close)

| Item | Result |
|---|---|
| M4 post-fix healthy scan (0 micro_reject, deployable read correctly) | ✅ **PASS** — 07-07: 0 micro_reject, 76 syms, 0 OBP TypeError |
| CVX IV-eligibility (no `iv_rank_insufficient_history`) | ✅ **PASS** — CVX scanned 07-07, iv_insuff=0, rejected on real `spread_too_wide_real` |
| GLD modulus first scan (collapse to $5 strikes) | ✅ **PASS-adjacent** — GLD scanned clean, no strike/IV errors (rejected on strategy candidates) |
| Bias first live cycle (executor-path viability ordering) | ⏳ **PARTIAL** — executor staged+filled QQQ live 14:37Z; ordering not independently observable from data (wiring test pins it; flag armed) |
| 21:20Z breaker re-eval (zero-close → re-trip) | ✅ **PASS** — 07-06 re-tripped; 07-07 re-tripped on new QQQ−15 |
| **#1135 edge-trigger FIRST SUPPRESSION test** (unchanged window → `suppressed_standing_window`) | ⏳ **STILL PENDING** — 07-07 21:20Z ran on #1134 (pre-#1135 deploy 22:17Z) AND a NEW loss landed → window changed → tripped. The distinctive suppress-unchanged path is UNEXERCISED. Fires 07-08 21:20Z iff operator un-pauses and no new loss lands. |
| #1134 first `envelope_violation` typed rows + egress receipt | ⏳ **PENDING** — 07-07 envelope rows predate the 21:10Z recycle (still `warn`/`force_close` typed). Streak-breaker 07-07 critical DID carry `egressed_at` (receipt partial-confirm). Fires next hold day. |
| First live `[CLOSE_FILL_GAP]` gap_fraction | ⚠️ **LANDED BUT CORRUPT** — QQQ close stamped fraction **15.08** (sign bug; see Area 4 FINDING) |

---

## 4. AREAS 1–7

**A1 — PROFITS. No new finding (raw-mode structural).** First live entry since 06-17
priced by honest raw math: QQQ IC EV +35.61 / PoP 0.6487 → realized −15 (stopped 3h
in, thesis never ran). Live champion 1/7 wins, −$168 cumulative; every staged EV was
positive against a negative realized aggregate — the un-calibratable raw-mode optimism
(ledgered; cannot correct until live close #8, now 7/8). Not a defect.

**A2 — MINIMIZING LOSSES. No new finding; the GATED Phase-3 class exercised a 2nd time
(cited, not re-found).** The QQQ cohort stop force-closed on the FULL-CROSS corroborated
UPL (`mark_corroborated −1.98`, unrealized_corroborated −$49) when the achievable close
filled at **1.64 (−$15)** — raw mid said −25. Full-cross over-pessimism = 3.3× the real
loss, same class as SOFI 06-30 (−65 est / −40 fill). This is the deferred Phase-3
"two-quote confirmation" case (backlog, GATED on ≥10–15 real close fills). **The
instrumentation that gate depends on is broken — see A4 (the reopen data is corrupt).**
Not a loosening; the doctrine forbids loosening the stop on this outcome.

**A3 — SELF-LEARNING. UNCHANGED (raw mode holds).** 07-07 ingest clean (errors=0;
`closed_positions_found=2, created=1, skipped_dup=1` — dedup ✓). Calibration
`insufficient_data` at sample 6; QQQ makes it 7/8. Next calibration 07-08 10:00Z still
raw. No finding.

**A4 — SELF-SUSTAINING. ★ FINDING — `close_fill_gap` sign-convention bug corrupts every
live-close gap_fraction (the data the deferred Phase-3 safety fix's reopen gate depends
on).**
- **WHAT:** the #1102 close-fill-gap instrumentation computes `gap_fraction=(fill−cross)/(mid−cross)`
  with no sign normalization. On the live/reconciler close path, `fill` is forced to a
  positive magnitude while `cross`/`mid` are stamped as SIGNED marks → the fraction is
  garbage.
- **WHERE:** `services/close_fill_gap.py:62-78` (raw `(f-c)/(m-c)`, no normalize) ·
  `services/paper_exit_evaluator.py:1913,1957,1976` stamp `cross`/`mid` from the SIGNED
  `current_mark`/corroboration estimate · `brokers/alpaca_order_handler.py:571` forces
  `fill = abs(filled_avg_price)` (comment :569-570 wrongly asserts cross/mid are
  "positive-magnitude"). The internal/shadow exit path
  (`paper_exit_evaluator.py:2235-2239,2269`) passes a SIGNED fill → self-consistent, so
  only the LIVE/reconciler path is wrong.
- **WHY #1 in-area:** it is a silent, verified-wrong instrumentation defect on the LIVE
  close path — the #1126 / 9a2cef1 test-green-production-wrong class (a §9 never-do):
  `tests/test_close_fill_gap.py:44-47` feeds all three inputs consistently positive
  (SOFI fixture → 0.2326) so CI is green while production emits mixed signs.
- **IMPACT (system data):** QQQ 07-07, the FIRST live close to emit a full quad, stored
  `cross=−1.98, mid=−1.74, fill=+1.64 → fraction 15.0833` (arithmetic verified:
  3.62/0.24). Correct-sign value is **1.417**. Of the 2 closes since #1102 shipped
  (06-30 23:30Z): QQQ = sign-corrupt; SOFI 07-01 (shadow) = cross/mid null (degenerate,
  fill-only). Result: **zero usable live gap_fractions**, and every future
  live/reconciler close (the exact population the Phase-3 reopen needs) will be corrupt.
  The Phase-3 fix reduces over-pessimistic premature force-closes (QQQ −49-est/−15-fill,
  SOFI −65/−40) — it stays deferred on a poisoned distribution.
- **HOW:** one-line sign normalization — at `alpaca_order_handler.py:571` sign-match
  `fill` to the stamped signed marks (drop the unconditional `abs()`), OR abs `cross`/`mid`
  at `:567` so all three share one convention; add a mixed-sign fixture to
  `test_close_fill_gap.py`.
- **EVIDENCE:** DB (stored quad + fraction match the mixed-sign formula exactly);
  code-confirmed file:line (Explore subagent). **RISK:** touches observe-only instrumentation
  — zero live-decision/close-path blast radius (the stamp is best-effort try/except).
  **CONFIDENCE:** high (DB arithmetic + code both dispositive).

**A5 — EFFICIENCY. ★ FINDING — standing-envelope alerts re-egress to the operator phone
every 15-min monitor cycle (no content-dedup), burying real criticals.**
- **WHAT:** while ONE live QQQ was held, `intraday_risk_monitor` re-wrote the standing
  concentration violation "QQQ is 100% of risk (limit 40%)" as a fresh `risk_alerts` row
  (severity HIGH) every cycle, and the #1111 relay egressed EACH one — **13 phone egresses
  in 3h** (14:45–17:45Z) — plus 26 non-egressed `medium` expiry+sector rows. The
  `force_close` critical that actually mattered egressed 18:07Z, AFTER the 13 concentration
  highs.
- **WHERE:** `risk/risk_envelope.py:316-354` appends a fresh violation on every
  `check_concentration` · `jobs/handlers/intraday_risk_monitor.py:449-496,491-492`
  `_log_alert`s each with no changed-since-last-cycle guard (concentration severity
  defaults `"block"` → HIGH) · relay poller `services/ops_health_service.py:1431`
  (`relay_direct_insert_alerts`) suppresses only per-row already-egressed stamps
  (`:1479`) — **no fingerprint on type+symbol+content, no cross-row cooldown**.
- **WHY #1 in-area:** it is the "84 high-sev warns/week" noise class made concrete and
  now on the LIVE egress path — the oversight channel the whole unattended-operation arc
  depends on. Confirmed to **persist post-#1134** (the taxonomy renamed `warn`→`envelope_violation`
  but kept concentration→high→relay).
- **IMPACT:** 13 relay egresses per 3h hold for one position; the steady-state target is a
  book holding 1–4 positions all session → dozens of duplicate phone alerts/day for
  unchanged standing conditions, cry-wolf burying the criticals.
- **HOW (additive):** apply the #1135 edge-trigger philosophy to standing-condition egress
  — suppress relay egress of an `envelope_violation` whose (type, symbol, bucket)
  fingerprint is unchanged since its last egress within the hold (content fingerprint in
  the relay selector, or a per-condition cooldown). Never delete the class; dedup the
  re-egress. **RISK:** additive, egress-only — no decision/close-path touch.
  **CONFIDENCE:** high (13 egresses measured + code-confirmed no dedup post-#1134).

**A6 — TRADING VOLUME. No finding; one open question for the operator.** The binding
constraint remains EV-after-cost, not cadence: the aggressive candidate `38d57d55` was
rejected by the #1101 roundtrip gate on all three 07-07 evaluations at net −1.55 / −13.55
/ **+14.45** (all < the $15 floor — edge-lost, not spread-eaten). **Open question:** the
executor `paper_auto_execute` ran **4× on 07-07** (14:37 exec 1, 16:30/17:59/18:47 exec 0)
vs the documented "one shot/day at 16:30Z." The QQQ filled on the off-schedule 14:37Z run.
Likely operator-driven manual retries around the morning un-pause (entries were un-paused
07-07 then re-paused 21:20Z), not a scheduler change — worth an operator confirm.

**A7 — TIME IN TRADE. UNCHANGED (ledgered bimodal).** QQQ IC held 3h07m
(14:37→17:45Z), force-closed by the cohort stop — condor thesis (45-DTE decay) never ran.
Reinforces the ledgered pattern: cohort stops dominate condor hold time (fast exits) vs
debit spreads ~94–116h. The over-pessimistic stop BASIS is the A2/A4 item, not a hold-time
loosening.

---

## 5. AREA 8 — NEGATIVE-DECISION EFFICACY (standing, full protocol)

- **Inventory (07-07):** 642 `suggestion_rejections` (76 syms) + 9 pending suggestions
  swept + the #1101 roundtrip-gate rejects (4–5/cohort/cycle). Ratio still hundreds:1
  vs 1 live close.
- **Roundtrip reject class (the reconstructable one, per the spec's item-6 addition):**
  now EXERCISED live — aggressive `38d57d55` at net +14.45 is **edge-lost** (positive net
  edge killed by the $15 floor), the others (neutral/conservative round_trip 98–385) are
  **spread-eaten** (net deeply negative). First live confirmation that the gate
  discriminates the two classes as designed.
- **Reconstructability:** unchanged — d8_v1 still no expiry (scanner rejects unrepriceable);
  dismissed/blocked suggestions repriceable via `order_json.legs`. No capture_version change.
- **Counterfactual:** the day's clearest datapoint — the QQQ that PASSED all gates lost
  −$15, while the 4 roundtrip-rejected candidates avoided their negative net edge. A reject
  was again a WIN for this lens (cost-avoidance). N-instances illustrate; the systematic
  per-gate marker remains backlog RESEARCH (no new finding — structural gap unchanged).

## 6. AREA 9 — ALERT & SIGNAL INTEGRITY (standing)

- **No new INTEGRITY finding.** Every alert this window is HONEST about its trigger
  (streak-breaker window matches; force_close matches; OBP TypeError real). The
  concentration re-egress is NOISY-BUT-HONEST → routed to Area 5 per A9's own disqualifier,
  not an A9 integrity finding.
- **Founding class quiet:** zero `ops_data_stale` rows this window (market open both days →
  `is_us_market_hours` gated) — nothing to measure for the self-contradiction rate.
- **Watch (cited, not re-found):** `paper_autopilot_cohort_per_suggestion_failed` reports
  DESIGNED roundtrip-gate rejections as "N per-cohort suggestion executions failed"
  (warning) — conflates gate-rejects with genuine failures; this is the ledgered A9-F8
  one-convention / executor-`status:partial`-invisible-to-A4 item (Tue taxonomy PR context).
- **Pending:** #1134's renamed `envelope_violation` types + egress receipt await first
  post-recycle fire (§3).

## 7. AREA 10 — CALENDAR & CLOCK INTEGRITY (rotating lens)

- **No new instance; incumbent KEPT.** Upcoming-boundary preview: no DST change until
  2026-11-01 (the ledgered winter-close blind hour activates then — fix still queued for
  the pre-Nov ops_health PR); next holiday Labor Day 2026-09-07; no fixture date inside 45
  days. No new `weekday()`-vs-broker-calendar site in the M4/#1134/#1135 ships (the
  edge-trigger breaker keys on content fingerprints, not dates). The 07-06 OBP incident was
  an external field-nulling event (M4-fixed), not a calendar-math defect. Retirement counter
  increments; incumbent still valid (Nov boundary unaddressed).

---

## 8. TOP-3 · CONFLICTS · DISAGREEMENTS

**TOP-3 (value/effort/risk):**
1. **A5 — standing-envelope egress dedup** (highest operator value; the phone is the
   oversight instrument and it floods every hold-cycle). Low effort (content fingerprint in
   the relay selector). Zero risk (additive, egress-only).
2. **A4 — `close_fill_gap` sign fix** (one line at `alpaca_order_handler.py:571` + a
   mixed-sign test). Unblocks the Phase-3 safety-fix data foundation; catches a live-path
   #1126-class defect. Zero live-path risk.
3. **A6 — confirm the 4× executor cadence on 07-07** (operator question, not a defect):
   scheduled-multi-cycle vs manual retries around the un-pause — resolve before assuming a
   volume change.

**Conflicts:** none. **Synergies:** #1 and #2 share the alert/close post-RTH surface but
touch different files (relay/ops_health_service vs alpaca_order_handler) → mergeable in one
recycle after 20:00Z. Both are additive/observe-only, no CI cross-dependency. #1 is the
natural extension of #1135's edge-trigger principle to the egress path.

**Four-source disagreements encountered:**
- **Broker source UNREAD this run** (Alpaca MCP tools absent). The live QQQ round-trip is
  DB-corroborated (alpaca_live/broker_status=filled/reconciler/is_paper=false) but not
  broker-snapshot-confirmed. Reported as a limitation, not averaged.
- **CODE vs DB on gap_fraction:** the stored 15.08 (DB) disagrees with the intended
  execution-quality fraction (~1.42) — the disagreement IS the A4 finding (the sign bug),
  reported not reconciled.

---

## 9. LEDGER UPDATES MADE

Appended `status:reported` 2026-07-08 NIGHTLY entry to `audit/ledger.md` with the two
FINDINGs (A4 close_fill_gap sign bug; A5 standing-envelope egress dedup), the verifications
CLOSED (M4 healthy scan, CVX IV-eligible, GLD scan, breaker re-evals), and the carried
PENDINGs (#1135 first suppression test 07-08 21:20Z; #1134 typed-rows/receipt first fire;
A6 executor-cadence confirm). Un-pause precondition flagged: `entries_paused=TRUE`.
