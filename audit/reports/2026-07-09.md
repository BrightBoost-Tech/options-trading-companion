# AUDIT v5.4 — NIGHTLY — 2026-07-09 (Thu 00:01 CT scheduled run)

**Mode:** NIGHTLY (DOW=4; FULL is Sundays). **Window:** 2026-07-08 05:01Z → 2026-07-09 05:01Z.
**STEP 0 clock grounding:** DB `now()` = 2026-07-09 05:01:23Z; broker clock 2026-07-09
01:02:13 ET (= 05:02Z), market closed, next open 09:30 ET. Both clocks agree with the
prompt header and midnight-CT cadence — no premise correction.
**Run limitation: NONE on broker truth this run** — the Alpaca MCP surfaced (unlike 07-08);
account + positions read directly at the broker. Equity numbers below are broker-confirmed.
**Budget actuals:** 11 SELECT-only SQL · 3 broker reads · 6 Railway reads · 0 subagents.

---

## 1. ALERT

**None.** No un-handled critical. The window's 2 criticals are designed controls, both with
delivery receipts: `force_close` QQQ (18:00:11Z, egressed 18:07Z) and
`streak_breaker_tripped` (21:20:05Z, egressed, edge-trigger case 2 — window CHANGED, new
QQQ −10 in / MARA −15 out; fingerprint stamped at trip time). No ALERT-2026-07-09.md.

---

## 2. PART 1 — WEEK IN REVIEW (delta vs 07-08 report)

- **H8 CLEAN:** BE + worker + worker-background all SUCCESS @ `7db5a36` = local main HEAD,
  deployed 22:29:35Z 07-08. Movers since the prompt's pinned `2a83174`: `e26bcfe` (#1138
  docs) and `7db5a36` (#1139 one-beta tripwire) — both post-close merges, named, expected.
- **Broker truth (direct read):** equity $2,067.87 = cash = options_buying_power (all
  settled, book FLAT, 0 positions); `last_equity` $2,078.30 (asof 07-07) → 07-08 day P&L
  −$10.43 (QQQ −10 + fees; $0.23 reg/TAF pending).
- **THE POOL SEALED 8/8:** live QQQ iron condor #3 (position `305e476a`, aggressive)
  staged 17:41Z (net_ev +36.95, ev 41.75 / pop 0.6425 raw), position row 17:45:05Z,
  **force-closed 18:00:11Z — a ~15-minute hold** — cohort stop on corroborated UPL
  **−$155** (mark_corroborated −3.04); broker fill −1.59 → realized **−$10**. Live pool:
  **8 closes, 1W/7L, −$178 cumulative** (hit-rate 12.5%).
- **CALIBRATION BOUNDARY IS TODAY:** 07-08 10:00Z calibration ran pre-seal at sample_size
  7 (insufficient at 30/60/90d, raw reset written). **First calibrated multipliers print at
  the 2026-07-09 10:00Z calibration_update** — the three 8th-close checks (raw-mode exit ·
  0.5-clamp review · winsorize gate) are due tonight, owner-gated. Attribute any
  post-10:00Z scoring/gate shift to the multiplier before suspecting a bug.
- **Breaker ritual DESIGNED:** entries_paused=TRUE since 07-08 21:20Z, reason = the new
  window (QQQ −10 · QQQ −15 · SOFI −40); `paused_written`, `fingerprint_stamped`, receipt
  egressed. Operator un-pause required. The QQQ cooldown bench (until 07-09 13:30Z) also
  fired correctly: the 19:02Z pending aggressive QQQ suggestion was NOT staged at the
  19:03Z executor run (bench held), while neutral/conservative shadow attempts were
  roundtrip-gate-rejected.
- **Jobs:** 100% green (0 failed/stuck, 21 job types); order_sync 96× @1.1s; ingest clean
  (errors=0, created 1, dup-skip 1); iv_daily_refresh 244.8s (heavy-nominal, unchanged).
- **Shadow/paper counts:** mechanism evidence only (gap-3(b) open) — not cited as edge.

## 3. PIN GRADES (prompt STATE re-verified)

| STATE pin | Verdict |
|---|---|
| Running SHA = origin/main HEAD | ✅ moved `2a83174`→`7db5a36`, movers named (#1138, #1139) |
| Active universe = 78 | ✅ verified by query |
| Pool seals 8/8 at 07-08 21:20Z | ✅ n=8, 1W/7L, −178 (learning_trade_outcomes_v3, is_paper=false) |
| Close-fill-gap: 3 honest rows | ✅ DB: QQQ 1.4167 + QQQ 0.9635 (corrected-in-DB per #1137 session) + SOFI 0.23 (offline-derived; 06-30 close predates #1102). Phase-3 counter **3** of 10–15 |
| Breaker: designed-trip-only | ✅ edge-trigger case 2 (window changed); no re-pause on unchanged window observed (none occurred) |
| 9 ops_output_stale RESOLVED (false-ager fixed) | ⚠️ **HALF-TRUE — see A9 FINDING** (11 rows 07-08; latest 2 UNRESOLVED and still aging; #1137 stamp live but UNEXERCISED) |

**Pending-verifications from 07-08:**
- **#1134 typed rows + egress receipt** → ✅ **CLOSED**: first `envelope_violation` typed
  rows landed (2 HIGH 17:45/18:00Z, both relay-egressed with `egressed_at`; 4
  warning-severity siblings not egressed — designed); `job_succeeded_with_errors` rows
  carry the full `egress_receipt {sent, receipted_at, webhook_sent}` — receipt proven on
  BOTH egress owners (alert + relay).
- **#1135 first suppression test** → ⏳ **STILL PENDING** (a NEW loss landed 07-08 →
  window changed → designed trip, not suppression). With the book flat and entries paused,
  **07-09 21:20Z should produce the first `suppressed_standing_window: true`** — that is
  the distinctive edge-trigger proof. Check tomorrow.
- **First corrected [CLOSE_FILL_GAP]** → ⏳ partial: the 07-08 quad in DB is honest
  (corrected in the #1137 session); the first NATIVE post-fix stamp awaits the next live
  close (fix deployed 20:50Z, after the 18:00Z close).
- **A6 executor cadence** → ⏳ repeated on 07-08 (scans 16:00/17:41/19:02Z, executor runs
  16:30/17:43/19:03Z). 2nd occurrence; operator confirm still open.

---

## 4. THE TEN AREAS

**A1 — PROFITS.** Charter answer: the binding constraint is still **edge at N=8, not
volume** — and it changes character TODAY at the 10:00Z calibration boundary: on a 1W/7L
pool the first calibrated multipliers will down-weight EV/PoP (that is the design working;
expect fewer/zero staged entries and do not read it as a regression). Structural item:
the ledgered **EV-basis/fee-unit recon reproduced on the LIVE cohort with dispositive
numbers** — aggressive QQQ 16:00Z stamped `net_ev +35.62` yet gate-blocked
`ev_below_roundtrip_cost`, then an equivalent structure passed at 17:41Z; the gate log
shows its own basis (`gross_ev=42.14 round_trip=154.00 net=−111.86` for the neutral twin;
stamped `net_ev` NULL on neutral rows). Two bases disagree on the same candidate — cited
(KNOWN-PENDING), urgency ↑ now that it demonstrably times live entries and the boundary
crosses today. Sizing custody: no sizing-territory change this window (no spot-check
triggered). N-disqualifier respected: no outcome-pattern claims at n=8.

**A2 — LOSSES.** SETTLED condition checked: book held ≤1 live position (broker-confirmed
0 now) — one-beta stays latent; #1139 tripwire is LIVE but unexercisable at this book.
The GATED Phase-3 over-pessimism class exercised a **3rd time, worst ratio yet** (cited,
not re-found): cohort stop fired on corroborated −$155 while the broker close filled −$10
(15.5×; prior instances 3.3× QQQ, 1.6× SOFI), 15 minutes after entry. Doctrine holds: no
loosening on outcome; the fix path is the gated Phase-3 reopen, its counter now 3/10–15
and its data foundation HONEST since #1137. **Refinement of the 06-15 deferred item
(changes its weight, metadata-only impact):** `reentry_cooldowns.realized_loss` stores the
trigger-time ESTIMATE, not the fill — now 2-for-2 on live closes post-#1080 (−48.99
recorded vs −15 realized; −155 vs −10). Bench durations unaffected; anything reading that
column for magnitude is misled.

**A3 — SELF-LEARNING.** Pool sealed 8/8; boundary at 10:00Z today; the three owner-gated
8th-close checks are DUE TONIGHT. `ops_signal_accuracy_degraded` fired its first 3 rows
(21:37–22:37Z; n=8, hit 0.125 < 0.2) — the #1118 detector arming exactly at its designed
threshold, not an incident (noise-cadence note → A5). Ingest dedup ✓. [A7-dormant rider:
hold-time bimodal reinforced — 15-min cohort-stop condor vs ~94–116h debit spreads.]

**A4 — SELF-SUSTAINING. FINDING (small, additive): the rejection-persist writer loses
rows when the retry itself hits the same dead connection — first data loss since #1104.**
- WHAT: the 19:02Z scan hit a broken-pipe burst; the #1104 single retry recovered 7
  inserts but **6 rejection rows were permanently lost** (SLV, ISRG, C, HOOD, PLTR, AMGN —
  `[Errno 32] Broken pipe` on the retry too). `counts.errors=6` on a green run.
- THE CHAIN WORKED: #1100's detector caught it, wrote `job_succeeded_with_errors` HIGH,
  and it reached the phone with a receipt — silent-failure → loud, end-to-end. This is the
  designed loudness functioning; the residual is the writer, not the watcher.
- WHERE: scanner persist seam (the #1104 retry wrapper); evidence Railway worker deploy
  logs 19:02:33–37Z + `job_runs ef8a2d4e` counts.
- IMPACT: 6/677 rejection rows (0.9%) lost on 07-08 — A8's counterfactual dataset is
  lossy under connection bursts; also `result.errors` carries NO detail (null) — the
  operator needs Railway logs to identify what was lost (F8 surfaced the COUNT, not the
  items). HOW (additive): reconnect-then-retry (fresh client) or bounded backoff ×2; stamp
  the failed symbols into `result.errors`. RISK: none (write-side observability).
  CONFIDENCE: high (logs + counts agree).

**A5 — EFFICIENCY. FINDING (refinement that broadens the ledgered re-egress item): the
duplicate-egress class is NOT envelope-specific — the A4 detector re-egressed the SAME
stale run hourly.** `job_succeeded_with_errors` for the one 19:02Z run (`run_id
ef8a2d4e`) was re-written and re-egressed at 19:07, 20:07, 21:07, 22:07Z — **4 phone
hits for one already-reported condition** (receipts prove delivery; `egress_owner:
'alert'`, so this bypasses the relay poller the ledgered fix targets). The queued dedup
fix must fingerprint the CONDITION (here: run_id) across BOTH egress owners, or it fixes
half the class. Also filed as watch, same shape: `ops_signal_accuracy_degraded` re-writes
~2/hour while hit-rate < 0.2 (3 rows in 90min; warning-severity, not egressed — row noise
only, but it will run indefinitely at n≥8 on a losing pool). Budget actuals this run:
within caps (11 SQL / 3 broker / 0 subagents).

**A6 — VIABLE-SET HANDLING. UNCHANGED (conditions checked).** SETTLED verdict stands: no
tier change (equity $2,067.87), no rejection-mix regime shift (07-08: 677 rejections —
no_fallback 219 · execution_cost>EV 105 · spread_too_wide 104), no structurally-dead name
cleared a gate. QQQ clearing the gate twice in two days is consistent with the
one-bucket {SPY,DIA,QQQ,IWM} view. `iv_rank_insufficient_history` 40 rejects / 10 syms =
the 06-17 universe additions still seasoning toward MIN_IV_HISTORY_DAYS=60 (eligible
~mid-Aug) — mechanical, not a defect. Feed note: Polygon quoted DARK on 8 liquid QQQ legs
at 19:03Z while the truth layer priced them — #1052 alignment saved the staging attempts
(the 06-10 XLE class); watch frequency.

**A7 — dormant, fills 3/10, exit-code custody: A2.**

**A8 — NEGATIVE-DECISION EFFICACY.** SOFI sentinel QUIET (did not clear the gate —
SETTLED holds). The roundtrip gate discriminated correctly again: neutral/conservative
spread-eaten (net −111.86 / −224.25), aggressive edge passed (+36.95 staged). The
16:00Z aggressive BLOCK with a stamped-positive net_ev is the A1 EV-basis item — from
this lens: **the NO at 16:00Z and the YES at 17:41Z were computed on different bases for
near-identical candidates**; until the recon ships, per-decision reconstructability of
the NOs is compromised on exactly the highest-value class (the almost-viable). Caveat
recorded: tonight's rejection ledger is 0.9% lossy (A4 finding) — 3 of the 6 lost rows
were iv-seasoning names (HOOD/PLTR/SLV), immaterial to counterfactuals. Capital-state
rejections: none this window (account readable all day).

**A9 — ALERT & SIGNAL INTEGRITY. ★ FINDING — the `ops_output_stale` paper_positions arm
is a standing HIGH false alarm that the system cannot clear while paused, and the
prompt's RESOLVED verdict is only half-true.**
- WHAT: 11 `ops_output_stale` HIGH rows on 07-08 (13:07→22:07Z, self-superseding; latest
  2 UNRESOLVED, age 176→177h and climbing) claim "the producing job may be silently
  no-opping." The mark-refresh loop is ALIVE — Part-B persisted `mark_corroborated −3.04`
  on the live QQQ that same hour — but `MAX(last_marked_at)` across ALL rows is still
  **2026-07-01 13:00Z**: both July QQQ holds ran on PRE-#1137 code (fix deployed 20:50Z
  07-08, after the close), so the QQQ 07-08 row has `last_marked_at=NULL`, and the
  now-flat book gives the live fix nothing to stamp.
- WHERE: `ops_health_service.py:145-155` (registry entry; the flat-book caveat is
  DOCUMENTED at :149-152 but unguarded) · `:527-548` (no open-positions check).
- WHY IT MISLEADS RIGHT NOW: with entries_paused awaiting operator review, the alarm
  re-fires ~2×/hour indefinitely (~48 HIGH rows/day) asserting a dead loop that isn't
  dead — cry-wolf on the exact severity band H11 baselines every session, and it will
  contaminate every future flat-book pause the same way. STATE's "RESOLVED (Part-B stamps
  since #1137)" holds only for held-and-marked books; the stamp is UNEXERCISED until the
  next post-deploy hold.
- IMPACT quantified: 11 HIGH rows 07-08; projected ~48/day while paused; 0 egressed
  (ops_* relay-skipped — noise stays in-DB but poisons `risk_alerts` triage and the
  audit's own H11 read).
- HOW (additive, observe-only): flat-book guard — when `open_n=0` (or 0 rows with
  non-null last_marked_at newer than the newest close), report status `flat`/INFO instead
  of `stale`/HIGH; separately verify the first post-#1137 hold actually stamps (pending
  verification below). RISK: none (health-check display path). CONFIDENCE: high (DB
  MAX() + code + deploy timeline all agree).

**A10 — CALENDAR & CLOCK INTEGRITY. No new instance; incumbent kept.** No date/weekday
math in the #1137–#1139 ships; the A9 finding is a population/lifecycle boundary, not a
clock one. Next boundaries unchanged: Labor Day 09-07 · DST 2026-11-01 (winter-close fix
still queued pre-Nov). No fixture date inside 45 days. Counter +1 (2 consecutive).

---

## 5. FREE LOOK (2 SQL + 1 subagent allowance; spent ~1 SQL, shared)

`phase2_precheck` — a job name outside the doctrine's scheduler map ran 4×/day green,
including 05:00:03Z today (1 min before this audit); a queued row shows `finished_at`
NULL (future `run_after`, surfaced by Postgres NULLS-FIRST in my probe — not a stuck
run). Presumed the paper-shadow phase-2 gate (memory: D6/D2 observation infra). **No
anomaly found; flagged for a one-line operator naming** so the next audit doesn't
re-wonder. Nothing else pulled.

## 6. TOP-3 (value / effort / risk)

1. **Flat-book guard on the paper_positions freshness arm** (A9): kills a standing HIGH
   false alarm that will otherwise fire ~48×/day for the entire pause. One conditional in
   `ops_health_service.py` + a fixture. Observe-only, zero decision-path risk.
2. **Make the queued re-egress dedup fix condition-fingerprinted across BOTH egress
   owners** (A5): tonight proved the class includes `egress_owner='alert'` writers
   (run_id ef8a2d4e ×4 to the phone). Same PR as #1, same file family, one recycle.
3. **Operator morning ritual (no build):** un-pause decision on the reviewed window →
   tonight 10:00Z calibration boundary: run the three 8th-close checks (raw-exit print ·
   0.5-clamp review · winsorize gate) → confirm 07-09 21:20Z produces the first
   `suppressed_standing_window: true` (#1135 proof) → `git status audit/` sweep.

## 7. CONFLICTS · SYNERGIES · FOUR-SOURCE DISAGREEMENTS

**Conflicts:** none — nothing tonight proposes touching a decision path; both build items
are health-check/observability-side. **Synergies:** TOP-1 + TOP-2 are one PR + one
recycle (post-20:00Z rule); the A4 writer hardening (reconnect + error detail) can ride
along — all three are alert/ops-surface only.

**Four-source disagreements (reported, never averaged):**
- **DB vs BROKER:** `reentry_cooldowns.realized_loss −155` / corroborated UPL −155 vs
  broker realized −10 — trigger-time estimate vs fill; the disagreement IS the Phase-3
  class (3rd instance) + the A2 metadata refinement.
- **DB alert text vs DB writes:** `ops_output_stale` "producing job may be silently
  no-opping" vs Part-B's same-hour `mark_corroborated` persist — the disagreement IS the
  A9 finding (the stamp column, not the loop, is stale).
- **Gate log vs suggestion stamp:** `net −111.86` (gate) vs `net_ev NULL/+35.62`
  (stamped) on the same candidates — the ledgered EV-basis recon, reproduced with exact
  numbers.

## 8. RETIREMENT COUNTERS

| Area | Tonight | Consecutive no-finding |
|---|---|---|
| A1 | charter answered, no new defect (ledgered item cited) | 2 |
| A2 | class cited (3rd instance) + refinement, no new defect | 0 (refinement) |
| A3 | no finding (boundary tonight) | 2 |
| A4 | FINDING (writer loses rows under burst) | 0 |
| A5 | FINDING (re-egress class broadened) | 0 |
| A6 | no finding, conditions checked | 2 |
| A7 | dormant (fills 3/10) | — |
| A8 | no finding (sentinel quiet; gate discriminated) | 1 |
| A9 | ★ FINDING (unclearable flat-book HIGH) | 0 |
| A10 | no finding | 2 |

*(Graduation/retirement remains owner-gated; no proposal pending.)*

---

## 9. LEDGER UPDATES MADE

Appended `status:reported` 2026-07-09 NIGHTLY entry: A9 flat-book false-ager (unclearable
while paused; stamp unexercised), A5 re-egress broadening (alert-owner path), A4
rejection-writer burst loss (6 rows), A2 cooldown realized_loss=estimate refinement;
verifications closed (#1134 typed rows + receipts BOTH owners; H8 @ 7db5a36; pool 8/8) and
pending (#1135 suppression 07-09 21:20Z; first native gap stamp; first post-#1137
last_marked_at stamp; calibration-boundary checks; A6 cadence; phase2_precheck naming).
**Operator preconditions flagged: entries_paused=TRUE (un-pause is operator-only) · the
10:00Z calibration boundary crosses today.**

STOP. Nothing implemented; READ-ONLY contract held (writes: this report + ledger only).
