# AUDIT v5.5 — NIGHTLY — 2026-07-10 (Fri 00:01 CT scheduled run) — first eleven-area nightly on the canonical disk prompt

**Mode:** NIGHTLY (DOW=5; FULL is Sundays). **Window:** 2026-07-09 05:01Z → 2026-07-10 05:01Z.
**STEP 0 clock grounding:** DB `now()` = 2026-07-10 05:01:05Z (= 00:01 CT Friday) — matches the
midnight-CT cadence and the prompt header; no premise correction. **Broker clock NOT read** (see
limitation). All time arithmetic below is DB-grounded.
**⚠ RUN LIMITATION (headless broker-blind, as STATE expects):** the Alpaca MCP did not surface.
Broker-dependent claims are DB-corroborated and DOWNGRADED to hypothesis; no equity number was
fabricated. Equity ≈ $2,067.86 is DB-DERIVED (last broker-confirmed $2,067.87 on 07-09; zero
trades/closes in this window) — HYPOTHESIS pending the next broker read.
**Budget actuals:** 11 SELECT-only SQL · 0 broker · 4 Railway reads · 1 subagent (the free-look).

---

## 1. ALERT

**None.** ZERO critical rows in the window (H11 baseline query, grouped, full window). Highest
severity present = `high`, both types known/ledgered (below). No ALERT-2026-07-10.md.

---

## 2. PART 1 — WEEK IN REVIEW (delta vs 07-09 report)

- **H8: HEAD moved 4× since the prompt pin (`655c9aa`), ALL movers named, all post-close:**
  `d45ad63` #1144 (docs) 23:06Z → `f6b204c` #1145 (docs incl. the F-FREE-1 env scrub) 23:49Z →
  `168a752` #1147 (CODE: PoP clamp-AND-log + walk-forward field contract) 04:57Z →
  **`d275d28` (doc-only #1147 wrap-up, runtime-inert) deployed 05:03:37Z — two minutes INTO this
  audit run.** All three services SUCCESS @ `d275d28` = origin/main = local main (subagent-verified
  via git metadata; shell unavailable, so fetch-freshness beyond 05:03Z unverified). Behavioral
  movers this window: **#1143 and #1147 only**; 5 recycles 22:54Z→05:03Z, zero orphaned jobs.
- **ZERO-trade day (2nd consecutive):** one scored scan (14:17Z) produced 3 SOFI
  LONG_CALL_DEBIT_SPREAD forks (ev_raw 39.71), ALL blocked `ev_below_roundtrip_cost` — the gate
  discriminating on honest raw economics. Book flat all day (0 open, 0 live-routed). Pool stays
  **8/8 post-epoch (1W/7L, −$178)**; close-fill-gap counter stays 3/10–15.
- **★ #1135 FIRST SUPPRESSION — DB-PROVEN (closes the 2-day pending verification):** the 21:20:03Z
  ingest's `job_runs.result.streak_breaker` reads verbatim `suppressed_standing_window: true,
  tripped: false, paused_written: false, edge_trigger: true`, reason "standing_window_already_
  reviewed — fingerprint matches the last trip", on the UNCHANGED 07-08 window (QQQ−10/QQQ−15/
  SOFI−40). `ops_control`: `entries_paused=false` (operator un-paused 11:53:33Z), fingerprint
  stamp intact from the 07-08 trip. Edge-trigger case 3 = designed, exercised, correct. Entries
  are ARMED going into 07-10.
- **Calibration state at window close:** stored multiplier 0.5 (10:00:03Z run, ledgered); apply
  ENABLED since 21:29Z (env flip + recycle); the day's only scored scan (14:17Z) ran PRE-flip —
  `ev == ev_raw == 39.7088` on the champion row, consistent with the ledgered finding #1, NOT a
  new defect. **NO production ev has been calibrated yet.** First calibrated production ev still
  pends the 07-10 16:00Z scan (outside this window). Option-B observe clock: ZERO clean lines yet
  (no scored scan after the 655c9aa recycle; the 21:34:49Z forced scan was staleness-gated) —
  clean count starts 07-10 16:00Z, as the marker says.
- **Jobs 100% green:** 21 job types, 0 failed/stuck, Σ result.counts.errors = 0 (today's persists
  lossless — the 6-error alert content is the STALE 07-08 run, see A5). Post-recycle learning
  chain clean: 21:00 → 21:20 → 21:30 → 21:45 → 22:00Z all succeeded (pending verification CLOSED).
  Overnight silence after 22:37Z is schedule-designed; `phase2_precheck` 05:00:03Z ✓.
- Shadow/paper counts: mechanism evidence only (gap-3(b) open).

## 3. PIN GRADES (prompt STATE re-verified)

| STATE pin | Verdict |
|---|---|
| Running SHA = origin/main HEAD (`655c9aa` at refresh) | ✅ moved → `d275d28`; 4 movers named (2 behavioral: #1143, #1147); ⚠ one deploy landed mid-audit (doc-only, runtime-inert) |
| Calibration ENABLED, first calibrated proof pends 07-10 16:00Z | ✅ condition verified (flip 21:29Z; 14:17Z scan pre-flip ev==ev_raw; nothing calibrated yet); proof still FUTURE — carry with the F-A1-3 re-scope wording |
| Option-B clock from `655c9aa`, GATE_QTY_FIX_LIVE_ENABLED OFF | ✅ zero observe lines counted post-marker (no scored scan since); flag not re-read this run (env read-back needs operator/session — assumed unchanged, hypothesis) |
| Greedy-stop DOWNGRADED (reopen: >4 fitting candidates + gate-passing tail) | ✅ condition unmet (3 candidates, 0 passed the gate) |
| E6 / P0-A broker-ack-close invariant = #1 build | ✅ still OPEN — #1147 is calibration/validation-side; not shipped, not re-found |
| Book-blindness P0-B / one-beta | ✅ latent (0 live positions all window; #1139 tripwire unexercisable) |
| F-FREE-1 credential scrub "PR pending" | ⚠ STATE CHANGED: scrub MERGED to main via #1145 `f6b204c` 23:49Z (title-evidenced); operator items (git-history cleanup + secret-scanning) still open |
| Active universe = 78 | ✅ verified by query |
| Pool 8/8, 1W/7L · close-fill-gap 3 honest rows | ✅ both verified (no new closes) |
| Breaker: suppression-is-designed, flag-only-if list | ✅ designed case-3 suppression observed; NONE of the flag conditions occurred |

**Pending verifications from 07-09 (dispositions):**
- **#1135 first suppression** → ✅ **CLOSED, DB-proven** (above).
- **Post-recycle learning chain clean** → ✅ **CLOSED** (21:00–22:00Z all green, errors=0).
- **First calibrated production ev (`ev == ev_raw × 0.5` persisted)** → ⏳ 07-10 16:00Z (future).
  Remember the re-scope: it proves PERSISTED ev + the final gate only — selection/sizing are RAW.
- **First native [CLOSE_FILL_GAP] + first post-#1137 `last_marked_at` stamp** → ⏳ need a live
  close / a held book; `MAX(last_marked_at)` still 2026-07-01 13:00Z (fix live, unexercised).
- **A6 executor cadence + phase2_precheck naming** → ✅ CLOSED as ACK-NO-ACTION (morning-triage
  dispositions: operator manual cycles; paper-shadow phase-2 gate). Observed again today
  (executors 14:18/15:04/16:30Z) — consistent, not re-raised.

---

## 4. THE ELEVEN AREAS

**A1 — PROFITS.** Charter answer: the binding constraint remains **edge at N=8** — and from
07-10 16:00Z its expression changes: gate-pass arithmetic becomes `ev_raw ≥ 2×($15 +
roundtrip_cost)` (floored ×0.5 multiplier composed with the #1101 floor). Against the recent
live candidate set: the QQQ-IC class (roundtrip ≈$4.8, ev_raw 41.75 on 07-08) still passes —
barely (threshold ≈39.6); the SOFI class (cost >$24.7) needs ev_raw >79 — far out. **Expected
staged-entry volume ≈ zero-to-rare, which is the do-no-harm design working, not a regression —
but it exposes a structural coupling for the OWNER-GATED clamp review: the multiplier can only
rise via pool improvement, the pool only grows via live closes, and closes require entries.**
Not a strict deadlock (the 30d window ages the June losses out ~early-Aug; 60/90 escalation
retains them longer; the 0.5 floor-clamp review is the owner's lever) — flagged as arithmetic,
labeled structural, NO loosening proposed. Sizing custody: no sizing-territory change (#1147 is
calibration-apply + a zero-live-path script). N-disqualifier respected: no outcome-pattern claims.

**A2 — LOSSES.** One line: no exit-territory movement (0 closes, 0 positions; settled ≤1-live
condition held; P0-A broker-ack-close remains the open #1 build in this custody — not shipped
this window). Conditions checked.

**A3 — SELF-LEARNING.** Boundary mechanics all verified in-window: 0.5 stored at 10:00Z ·
apply enabled 21:29Z · nothing calibrated yet · first calibrated ev pends 16:00Z today.
**#1147 materially advances this charter: the walk-forward script now reads the REAL view
columns (A1a field contract CLOSED), the 0.5-PoP fabrication is DELETED (H9), and prequential
validation is UNBLOCKED** (remaining: is_paper=false filter + confirm ev_predicted is raw).
Ingest clean (dedup ✓). [A7-dormant rider: no fills; hold-time file unchanged.]

**A4 — SELF-SUSTAINING.** No new silent-failure class. Today's writer data: 0 rejection rows
lost (Σ errors=0 across 6 scans — the 07-08 burst didn't recur). Observed detector lifecycle
fact worth recording: `job_succeeded_with_errors` re-alerts on a stale run cease when the run
ages past the detector's lookback (last fire 18:07Z ≈ 23h after the 07-08 19:02Z run) — the
class self-terminates at ~24h, which bounds the noise but doesn't fix it (A5). Five recycles in
6h produced zero orphaned/stuck jobs. Known filed gaps (iv_daily_refresh ok-on-all-missing;
watched `learning_ingest` stub vs unwatched real producer) unchanged — cited, not re-found.

**A5 — EFFICIENCY. Quantified continuation (urgency ↑, no new class): on a ZERO-trade day the
system wrote ~53 warning-or-higher alert rows, 6 of them phone-egressed for ONE day-old
already-reported condition.** Breakdown: `ops_output_stale` ×10 HIGH (the unclearable flat-book
false-ager — A9's ledgered item, still unguarded) · `job_succeeded_with_errors` ×6 HIGH all
egressed, ALL carrying the SAME stale run_id `ef8a2d4e` (07-08's scan) — cumulative **10 phone
hits across 2 days for one condition** · `ops_signal_accuracy_degraded` ×20 (~2/hr, will run
indefinitely at n≥8 on a losing pool) · `chain_mechanics_formula_anomaly` ×14 (ledgered legacy
noise) · autopilot costume ×3 (A9). **The 3-in-1 observability PR has now slipped TWO
consecutive build days** (07-09 morning FIX-TODAY → gate-fix took the slot; 07-09 EOD build
order ③ → #1147 took the slot). Budgets this run: within caps (11/12 SQL, 1/8 subagents).

**A6 — VIABLE-SET HANDLING. UNCHANGED (conditions checked).** No tier change (equity DB-derived
≈$2,067.86 — hypothesis, broker-blind); rejection mix stable across 6 scan runs (no_fallback 221 ·
spread_too_wide_real 208 · execution_cost_exceeds_ev 166 · iv-seasoning 50, still the 06-17 adds
maturing ~mid-Aug); no structurally-dead name cleared a gate (SOFI blocked). Totals over-count by
design (inner+outer record) — not deduped tonight, mix-shape only.

**A7 — dormant, fills 3/10, exit-code custody: A2.**

**A8 — NEGATIVE-DECISION EFFICACY.** SOFI sentinel QUIET — SOFI did NOT clear the roundtrip gate
(39.71 raw vs >24.7 cost; SETTLED holds). The NOs discriminated on honest math for a 3rd
consecutive session; today's rejection ledger is LOSSLESS (0 persist errors), so the
counterfactual dataset carries no caveat tonight. Reconstructability caveat stands (EV-basis
recon, KNOWN-PENDING): shadow forks again stamped `ev_raw=NULL`/`net_ev=NULL` while carrying
`ev`=champion's value — same-candidate cross-cohort stamps remain basis-inconsistent. Cited.

**A9 — ALERT & SIGNAL INTEGRITY.** No NEW liar tonight. Both known costumes exercised and
verified at the metadata level (cited, not re-found): (1) `paper_autopilot_cohort_per_suggestion_
failed` ×3 — `distinct_error_classes` = `["EntryRoundtripCostExceedsEV"]` on all three, i.e.
**100% designed economic NOs reported as "executions failed"** (the A9-F8 one-convention item;
taxonomy PR context). (2) The stale-run `job_succeeded_with_errors` text claims "a silently
masked failure" on its 10th delivery — after the first egress the "silent" claim is self-
falsifying; fold the message-honesty fix into the queued condition-fingerprint dedup (same
seam). #1134 types: no new pre-split costume observed. Counter +1.

**A10 — CALENDAR & CLOCK INTEGRITY.** No new instance. The #1147 pair merged at 23:56/00:03 CT —
a midnight-boundary straddle, but both post-close and both attributable; the ISO8601
mixed-timestamp bug #1147 fixed was IN A ZERO-LIVE-PATH script (caught before it could lie).
Import-time flag inventory unchanged (CALIBRATION_ENABLED note now has its fail-loud logging via
#1143). Next boundaries: DST 2026-11-01 winter-close fix still queued. Counter +1 (3 consecutive).

**A11 — SELF-EXTENSION (nightly one-liner).** What no area asks: **run-boundary integrity — a
deploy landed 2 minutes into this audit and H8 is graded point-in-time; propose the nightly pins
the running SHA at run-START and run-END and names any mid-run mover as a standing header field**
(cost ~1 Railway read; tonight it was caught only because deployment listing happened late in the
run). SECURITY LENS remains recommended-pending as A10's next rotation (owner-gated, on record).

---

## 5. FREE LOOK (2 SQL + 1 subagent allowance; spent: 1 subagent, 1 SQL in-charter-adjacent)

Spent the subagent chasing the mid-audit deploy `d275d28`: it is the **doc-only #1147 wrap-up
(ledger + backlog), runtime-inert**, and local main had already fast-forwarded to it at
05:03:37Z — i.e. the operator's build session was still concluding as this audit started. No
anomaly; produced the A11 proposal above. Notable environment fact for future runs: the
scheduled session has NO shell (subagents included) — git state was reconstructed from `.git`
metadata; fetch-freshness could not be proven.

## 6. TOP-3 (value / effort / risk)

1. **Ship the 3-in-1(+1) observability PR** (flat-book stale guard · cross-owner condition-
   fingerprint re-egress dedup · #1104 writer-hardening · accuracy-warn dedup): slipped 2 build
   days; tonight's floor is ~53 noise rows + 6 redundant phone hits on a ZERO-trade day, and the
   phone-hit class self-terminates only at the ~24h lookback. One PR, observability-side, zero
   decision-path risk.
2. **Morning ritual (no build):** at/after 16:00Z verify the FIRST calibrated production ev with
   the exact re-scoped claim — persisted `ev == ev_raw × 0.5` proves the multiplier reaches the
   persisted row + the final roundtrip gate ONLY (selection/sizing stay RAW); simultaneously the
   first CLEAN Option-B observe lines start counting. Do not misread `POP_CLAMP_ENGAGED` never
   firing — it is dormant-by-arithmetic while pop_mult ≤ 1.0 (ledgered). Sweep `git status audit/`.
3. **P0-A broker-acknowledged-close invariant stays the #1 build** (unchanged; nothing this
   window touched the E6 hole).

## 7. CONFLICTS · SYNERGIES · FOUR-SOURCE DISAGREEMENTS

**Conflicts:** none — tonight proposes nothing on a decision path. **Synergies:** A1's
narrowed-funnel arithmetic and A3's pool-growth coupling are the SAME structural fact — hand
both to the owner-gated 0.5-clamp review as one exhibit (the floor may bind harder than intended
when composed with the $15 gate; reviewing ≠ loosening). A9's message-honesty fix rides A5's
dedup PR (same seam, one recycle).

**Four-source disagreements:** none new. Broker unread (blind run) — DB-internal sources agreed
everywhere checked; the standing gate-log-vs-stamped-net_ev basis disagreement is cited under A8
(KNOWN-PENDING recon), not re-derived.

## 8. RETIREMENT COUNTERS

| Area | Tonight | Consecutive no-finding |
|---|---|---|
| A1 | charter answered; structural arithmetic flagged to an existing owner gate (no new defect) | 3 |
| A2 | no finding, conditions checked | 1 |
| A3 | no finding (boundary verified; #1147 advances charter) | 3 |
| A4 | no finding (lifecycle fact recorded) | 1 |
| A5 | quantified continuation of own ledgered item (no new class) | 1 |
| A6 | no finding, conditions checked | 3 |
| A7 | dormant (fills 3/10) | — |
| A8 | no finding (sentinel quiet; NOs lossless + discriminating) | 2 |
| A9 | no new liar (both costumes cited w/ metadata proof) | 1 |
| A10 | no finding | 3 |
| A11 | nightly one-liner produced (run-boundary pinning proposal) | — (permanent) |

*(Graduation/retirement remains owner-gated; no proposal pending — no area at 6.)*

---

## 9. LEDGER UPDATES MADE

Appended `status:reported` 2026-07-10 NIGHTLY entry: verifications closed (#1135 suppression
DB-proven · post-recycle learning chain clean · H8 @ `d275d28` with movers named · F-FREE-1
scrub state-change) · A5 continuation counts (10 cumulative phone hits, one condition; 3-in-1
slipped ×2) · A1/A3 clamp-review exhibit (funnel arithmetic + pool-growth coupling) · A11
run-boundary proposal · pendings carried (16:00Z calibrated-ev proof · native CLOSE_FILL_GAP ·
last_marked_at stamp · #1139 tripwire).

STOP. Nothing implemented; READ-ONLY contract held (writes: this report + ledger only).
