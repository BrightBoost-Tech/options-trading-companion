# AUDIT v5.5 — FULL — 2026-07-12 (Sunday 00:00 CT scheduled run)

**Mode:** FULL (Sunday; three passes per area). **Window:** 2026-07-10 05:01Z → 2026-07-12 05:01Z
(two nights — the 07-11 nightly DIED, see A4; this run absorbs its window).
**STEP 0 clock grounding:** DB `now()` = 2026-07-12 05:01:47Z = **Sunday 00:01 CT**; broker clock
2026-07-12 01:02:17 ET = 00:02 CT — agree to the minute, market CLOSED, next open Mon 07-13
09:30 ET. No premise correction. All time arithmetic below is clock-grounded.
**Run limitation: NONE material** — unlike 07-08/07-10, the Alpaca MCP surfaced; broker truth read
directly. Permission sandbox verified live (Bash/PowerShell denied; writes only under `audit/`).
**H8 run-boundary pin (first run under the 07-10 A11 proposal):** run-START SHA = run-END SHA =
`a120c5f` on all three services (worker `16a78d81` / worker-bg `de6dfa9b` / BE `a10256ab`, all
SUCCESS, created 02:49Z > merge). No mid-run mover.
**Budget actuals:** 15 SELECT-only SQL (2 were 42703 introspection misses, counted) · 2 broker ·
6 Railway reads · 6 subagents (cap 12) · report < 900 lines.

---

## 1. ALERT

**None. No ALERT-2026-07-12.md.** H11 baseline (48h, grouped): **ZERO critical rows**. Highest
severity = `high`, both types known/ledgered (`job_succeeded_with_errors` ×9, `ops_output_stale`
×10 — see A5). The night's one NEW finding (A9, thesis price-basis) is MEDIUM, observe-only
surface, zero rows written yet.

---

## 2. PART 1 — WEEK IN REVIEW (delta vs 07-10 report)

- **★ FIRST CALIBRATED PRODUCTION EV — PERSISTED, PROVEN, CLOSED.** The 07-10 16:00Z champion
  QQQ IRON_CONDOR row: `ev = 18.73 = ev_raw 37.46 × 0.5000` exactly (plus SOFI 14:02Z:
  `19.9414 = 39.8828 × 0.5`). Both blocked — QQQ `ev_below_roundtrip_cost` (net_ev 33.46), SOFI
  `edge_below_minimum`. Per the F-A1-3 re-scope this proves the multiplier reaches the PERSISTED
  ev + the final gate ONLY; selection/sizing were RAW (the #1174 apply-move is observe-off).
  **The 07-10 report's arithmetic prediction verified in production: QQQ-IC needed ev_raw ≥~39.6,
  came in 37.46 → blocked. The composed floor×gate zero-entry regime is now LIVE-EXERCISED** (A1).
- **Zero live trades both days** — broker-confirmed tonight: equity = cash = OBP = **$2,067.86**,
  book FLAT (position_market_value 0). 1¢ below 07-09's $2,067.87 — hypothesis: a reg-fee posting
  on balance_asof 07-10 (accrued_fees 0; immaterial; not re-derived). Pool stays **8/8 post-epoch
  (1W/7L)** — re-verified by query (live closes 8, wins 1). Close-fill-gap counter stays **3/10–15**
  (broker-corroborated zero closes).
- **Breaker: SECOND designed suppression, DB-proven.** 07-10 21:20Z ingest:
  `suppressed_standing_window: true, tripped: false, paused_written: false` on the UNCHANGED
  07-08 fingerprint (QQQ−10/QQQ−15/SOFI−40). `entries_paused=false` (armed since 07-09 11:53Z),
  fingerprint stamp intact. NONE of the STATE flag-only-if conditions occurred. Weekend evaluated
  nothing (no ingest scheduled) — designed.
- **H8: fifteen merges landed Sat evening (~23:05Z 07-11 → 02:49Z 07-12), ALL ledger-named** by
  the build sessions themselves (#1164 thesis tracker → #1166/#1167 book-scaling+COALESCE →
  #1169 PoP inversion + REPLAY flip → #1171/#1172 bucket control + winter-close → #1174/#1175/
  #1176 apply-move/replay-linkage/prequential → #1178/#1179 PoP clamp + dead-delete → docs/
  adjudication `a120c5f` #1178–#1182). All three services SUCCESS @ `a120c5f` = origin/main =
  local main. Zero orphaned/stuck jobs across ~15 recycles (nothing was running — weekend).
- **The external full audit v1.3 was adjudicated last night** (ledger 07-12 entry): E8 critical
  (latent) promoted, E12/E14/E16 promoted, W1 PASS / W2–W5 FAIL with **arm-clocks RESET**, queue
  re-sequenced ①–⑥. Tonight treats ALL of it as the exclusion floor — nothing re-found; the
  weekend-PR verifications below are fresh territory only.
- **Jobs 100% green through Friday close** (21 job types, 0 failed/stuck since 07-09 05Z; last
  weekday job `phase2_precheck` 07-10 23:00Z). Weekend silence is schedule-designed (mon–fri
  crons). One caveat: the 07-10 **14:02Z** `suggestions_open` lost 5 rejection rows
  (`counts.errors=5` — the reconnect-retry residual's SECOND occurrence); the **16:00Z
  calibration-proof scan was LOSSLESS (errors=0)**.
- **The 07-11 nightly audit never produced a report** — cron.log has `Sat 07/11 0:00:02 start`
  then nothing. Mechanism identified tonight (A4). Realized coverage loss ≈ zero (this FULL
  absorbed the window; Saturday was builds-only, all self-ledgered).

## 3. PIN GRADES (prompt STATE v5.5 re-verified) + pending dispositions

| STATE pin | Verdict |
|---|---|
| Running SHA = origin/main (`655c9aa` at enactment) | ✅ moved → `a120c5f`; ~15 movers, all named; run-START = run-END (no mid-run deploy) |
| Calibration ENABLED; first calibrated proof pends 07-10 16:00Z | ✅ **CLOSED — proven in production** (ev == ev_raw × 0.5 persisted, QQQ + SOFI; re-scope wording honored) |
| Option-B clock from `655c9aa`; GATE_QTY_FIX_LIVE_ENABLED OFF | ✅ W1 code-PASS stands (adjudication); runtime observe lines NOT read tonight (old deployment logs) → Monday ritual item |
| Greedy-stop DOWNGRADED (reopen: >4 fitting + gate-passing tail) | ✅ condition unmet (2 scored candidates 07-10, 0 passed the gate) |
| E6 / P0-A broker-ack-close = queued #1 build | ✅ OPEN, not shipped; seam re-verified UNCHANGED (submit_and_track return still discarded, paper_exit_evaluator.py:2245/2258-2259) |
| Book-blindness P0-B | ⚠ STATE CHANGED: **BUILD half COMPLETE** (#1166 persisted fields + #1171 bucket control, both observe-off, content-verified tonight); enforcement = one composed owner decision on a week of shadow logs; #1139 tripwire still interim guard |
| F-FREE-1 scrub | ✅ merged (07-10); operator items (history cleanup + secret-scanning) still open — feeds A11 |
| Active universe = 78 | ✅ verified by query |
| Pool 8/8 1W/7L · close-fill-gap 3 rows | ✅ both verified (zero closes, broker-corroborated) |
| Breaker suppression-is-designed / flag-only-if list | ✅ second designed suppression observed; zero flag conditions |
| EXCLUDED-EVIDENCE day 07-06 | ✅ respected — the 168 `micro_tier_underlying_too_high` rejects are 100% quarantined to 07-06, zero recurrence (M4 held) |

**Pending dispositions:** first calibrated ev → ✅ CLOSED (above) · 07-10 21:20Z suppression → ✅
CLOSED · native [CLOSE_FILL_GAP] + first post-#1137 `last_marked_at` stamp → ⏳ still needs a held
book/live close · replay first capture → ⏳ Mon 16:00Z (decision_runs = 0 tonight, correct;
**RE-SCOPED per E16: verify rows-exist + timing ONLY — completeness is known-defective**) ·
thesis job first authoritative run → ⏳ Mon 17:00 CT (`position_thesis_outcomes` = 0 rows tonight;
the ledger's 16-row table was an in-session preview) · W2–W5 arm clocks → RESET, awaiting queue-②
repairs · [APPLY_ORDER_SHADOW]/[GATE_QTY]/[RISK_BASIS_SHADOW]/[BUCKET_SHADOW] first clean lines →
⏳ Mon 16:00–16:30Z.

---

## 4. THE ELEVEN AREAS (FULL: PASS-1 charter · PASS-2 reads · PASS-3 value)

**A1 — PROFITS.** PASS-1: the charter is still the right question, and tonight it has a sharper
answer: **the binding constraint is no longer abstract "edge at N=8" — it is the LIVE-PROVEN
composed arithmetic: floor-clamped ×0.5 multiplier × the $15 roundtrip gate excludes the ENTIRE
78-name universe from staging** (best-in-class QQQ-IC missed by ~2.1 EV points on 07-10). Pool
cannot grow without entries; the multiplier cannot recover without pool improvement; the 30d
window ages June losses out ~early-Aug. This is do-no-harm working as designed — NO loosening
proposed; it is THE exhibit for the owner-gated 0.5-clamp review, now with a production
data point instead of a projection. PASS-2 (sizing custody, subagent-read): #1171 bucket-control
wiring verified correct — utilization gate precedes bucket check (paper_autopilot_service.py:
1025→1039), buckets-as-data {SPY,DIA,QQQ,IWM} (bucket_control.py:21-28), strict `=1` opt-in
(:33), same-run reservation resets per cohort (:971) and adds post-stage (:1090), candidate
counted once, units consistent (totals, no ×qty mix). Known W3/L3 fail-opens UNCHANGED (ledgered,
clocks reset). PASS-3: this area's week produced the clamp-review exhibit + the sizing epic's
build half — EARNING. N-disqualifier respected (no outcome-pattern claims at N=8).

**A2 — LOSSES.** PASS-1: charter right; the single most likely wrong-loss path remains the
UNCLOSED broker-ack-close invariant (E6/P0-A, queue territory with E8) — unchanged, not shipped,
re-verified present (submit_and_track return discarded :2245; unconditional routed_to stamp
:2258-2259). PASS-2 (exit custody): #1178/#1169 content-verified — ONE terminal `_clamp_pop`,
ALL ten branches return through it (ev_calculator.py:80-132), credit P(win) = max_loss/(max_gain
+max_loss) (:79) no double-clamp, non-finite → 0.5 + log (:25-29), `[POP_BOUND_ENGAGED]` on bind
(:29-35), exit-metrics abs-delta deliberately separate (:553), `forecast_ev_pop` zero refs.
E12 credit-EV≡$0 UNCHANGED (cited, queue ⑤). Zero closes/positions all window; ≤1-live condition
held. One shared note with A9: `bucket_exposure_would_block` writes severity='warning' → it
neither egresses nor relays (paper_autopilot_service.py:1063-1064) despite the code comment
calling it "the #1139-class alarm" — for the observe week the operator must READ logs; fold the
severity question into the enforcement decision packet. PASS-3: EARNING (the week shipped PoP
integrity end-to-end in this custody).

**A3 — SELF-LEARNING.** PASS-1: charter answered by events — the calibration boundary was
CROSSED in production this window (first calibrated persisted ev, above). What most improves
learning speed now is the QUEUE already filed: prequential falsifier is BUILT (#1176,
content-verified: pure prefix-fit via build_adjustments_from_outcomes, CalibrationService(None),
nothing schedules it, insufficient_data never raises) and runs the day the pool grows. PASS-2:
#1174 apply-move verified observe-safe — flag-off path mutates NOTHING (all candidate writes
under `if armed:`, calibration_apply_ordering.py:99-105), legacy :3601 apply still the single
active site (sentinel-guarded), scanner still scores RAW (options_scanner.py:3751/3919); W4
ticker-only serialization UNCHANGED (:74, ledgered, clock reset). #1167 COALESCE + drift guard
cited (verified by the build session; drift-guard test present). CALIBRATION_ENABLED remains the
one import-time read (calibration_service.py:34, documented). [A7-dormant rider: zero fills;
hold-time/exit-quality untouched.] PASS-3: EARNING — this charter's week of findings (circular-training,
raw-basis, ordering) all shipped within the week.

**A4 — SELF-SUSTAINING.** PASS-1 charter ("which silent failure would take longest to notice?")
answered by a live instance: **the audit loop itself. The 07-11 nightly died silently at start —
and tonight's forensics (subagent, run-nightly.cmd + cron.log) narrow the mechanism: the end
marker is UNCONDITIONAL (fires even on claude errors — proven by the 06-13 model-error line), so
start-with-no-end means the CMD PROCESS was killed externally — machine sleep/power-off moments
after the 00:00:02 start echo. 4th occurrence, ALL weekend nights (06-14 · 06-20 · 06-30 · 07-11
starts-only; 07-05 never fired).** WakeToRun (shipped exclusion) wakes the machine to START but
holds no wake-lock for the ~15-min run — the class needs a sleep-hold (ES_SYSTEM_REQUIRED
wrapper / power-request) or a mid-run-safe schedule. The ping-after-file is POSITIVE-ONLY and the
process died before the ping block — so the ONLY detection was the external healthchecks DOWN on
the missing ping: **operator must confirm that DOWN email actually arrived Saturday; if not, the
nightly's dead-man check isn't armed** (`NIGHTLY_AUDIT_PING_URL` check config unverifiable from
here). Operator-side fix, no repo code. PASS-2: thesis-tracker wiring VERIFIED for Monday
(scheduler.py:75 17:00 CT → /internal/tasks/thesis/score → BACKGROUND queue
(internal_tasks.py:340) → EXPECTED_JOBS (ops_health_service.py:119) → typed contract errors→
PARTIAL (thesis_tracker.py:161-174)). **Two prompt-STATE corrections (movers = 07-11 builds):**
iv_daily_refresh ok-on-all-missing is FIXED (counts.errors→PARTIAL, iv_daily_refresh.py:170-173)
but STILL absent from EXPECTED_JOBS (watchdog half open — ride the 3-in-1 PR); the watchdog now
watches real `paper_learning_ingest` (ops_health_service.py:117) — the "stub watched / real
unwatched" claim is STALE. PASS-3: EARNING (the F-A4-1 typed contract found this week is already
load-bearing in two new jobs).

**A5 — EFFICIENCY.** Quantified continuation, urgency ↑ again, no new class: the 07-10 14:02Z
scan's `counts.errors=5` (5 rejection rows lost — reconnect-retry residual, 2nd occurrence,
11 rows cumulative) re-egressed **hourly ×9 (14:07→22:07Z) for ONE already-known condition**;
`ops_output_stale` fired ×10 HIGH on the flat book (unclearable false-ager, known). **The
3-in-1(+1) observability PR (flat-book stale guard · condition-fingerprint re-egress dedup ·
#1104 reconnect-then-retry writer hardening · accuracy-warn dedup) has now slipped THREE
consecutive build days** — the entire weekend queue shipped without it. Budgets this run: 15/20
SQL · 2/6 broker · 6/12 subagents — held. PASS-3 note: the audit's noise findings remain found-
but-unshipped; everything else found this week shipped within 48h.

**A6 — VIABLE-SET HANDLING.** Conditions checked: NO named reopener fired (tier unchanged at
~$2.07k; rejection mix stable — 30d shape: no_fallback 2157 · spread_too_wide_real 1520 ·
execution_cost_exceeds_ev 1017 · all_strategies_rejected 913; the condor_* reasons have 30d
baselines, not new; iv-seasoning 365 aging toward ~mid-Aug; no dead name cleared a gate). The
SETTLED per-name spread verdict STANDS as spread economics. One flag to the owner (cross-ref A1,
no reopen of the settled verdict): the composed floor×gate currently makes the viable-set
question MOOT upstream — 0 of 78 can stage regardless of spread class. A6 resumes mattering the
day the clamp review or pool aging moves the multiplier.

**A7 —** dormant, fills 3/10, exit-code custody: A2.

**A8 — NEGATIVE-DECISION EFFICACY.** The NOs were verified honest ON THE CALIBRATED BASIS for
the first time: both 07-10 scored candidates blocked with full basis visibility (SOFI
`edge_below_minimum` at calibrated 19.94; QQQ `ev_below_roundtrip_cost` at calibrated 18.73,
net_ev stamped 33.46). SOFI sentinel QUIET (SETTLED holds — blocked on economics, not bug).
Rejection ledger: 07-10 carries a 5-row caveat (14:02Z loss, above); the 16:00Z decision-bearing
scan is LOSSLESS. 30d ratio ≈ 7,278 recorded rejections (inner+outer over-count, not deduped) :
8 live closes. EV-basis recon (shadow ev_raw NULL stamps) unchanged, KNOWN-PENDING, cited.

**A9 — ALERT & SIGNAL INTEGRITY. ★ THE NIGHT'S ONE NEW FINDING — the thesis tracker writes
terminal verdicts from an unflagged fallback price (H9 violation in the week's headline
measurement).** WHAT: `_underlying_at_expiry` silently substitutes the last bar ON-OR-BEFORE
expiry within a 7-day lookback (thesis_tracker.py:55-57); the row persists that price as
`underlying_at_expiry` + a TERMINAL hit/miss (never re-scored, :84-88/:117) with NO
price-source/price-date/fallback field; `thesis_basis` prints it as if it were the expiry price
(thesis_scoring.py:88-121) — while the module claims "NEVER fabricated (H9)"
(thesis_scoring.py:20). WHY #1: this is the new standing metric (thesis hit-rate 81%) the
operator will steer by; a strike-adjacent verdict graded off a days-stale bar is
indistinguishable from an honest one. IMPACT tonight: ZERO rows written yet; the Saturday
preview found none of the 16 near a strike boundary → no verdict currently wrong. The exposure
is STANDING, and the table's FIRST authoritative fill is Mon 17:00 CT. HOW: persist
`price_basis_date` (or a `price_basis='expiry_close'|'fallback_prior_bar'` flag) + surface it in
thesis_basis; strict-mode option: grade `unknown` when no exact-expiry bar (H9-pure). RISK: none
— observe-only table, modulates nothing. CONFIDENCE: high (code-read, single source).
EVIDENCE-ADJACENT (one line, shared w/ A2): `bucket_exposure_would_block` severity='warning' →
unpaged despite the "#1139-class alarm" comment. Otherwise: all weekend log markers honest; no
#1134-vocabulary bypass (all new writers use valid severities); no failure-as-success counters
(thesis errors→PARTIAL verified); **zero stale `hit_rate` readers** post-F-A9-1 (sole consumer
reads `realized_trade_win_rate` with a defensive fallback, ops_health_service.py:628-630).
Counter resets to 0.

**A10 — CALENDAR & CLOCK INTEGRITY.** PASS-2 verified the incumbent's headline SHIPPED ITEM at
content level: `is_us_market_hours` is now ET wall-clock 9:30–16:00 via
ZoneInfo("America/New_York") (ops_health_service.py:43/65-69), EDT byte-identical; repo-wide
sweep found **NO remaining winter-blind session arithmetic** (scheduler crons are CHICAGO_TZ-
aware incl. the #1109 heartbeat, scheduler.py:102-124/252-277; residual "13:30/20:00" hits are
docstrings/tests; `_RTH_WARMUP_OPEN_UTC=(14,30)` is the intentional winter anchor). **The
2026-11-01 winter-close trigger is genuinely retired ~3.5 months early.** Import-time flag
inventory did NOT grow: all six weekend flags are call-time reads inside helpers
(CALIBRATION_APPLY_AT_SCORING :34 · RISK_BASIS_MAX_LOSS_ENABLED :28 · BUCKET_CONTROL_ENFORCE :33
· REPLAY_ENABLE decision_context.py:45 · GATE_QTY_FIX_LIVE_ENABLED paper_endpoints.py:1230 ·
STREAK_BREAKER_EDGE_TRIGGER_ENABLED streak_breaker.py:66). Look-list boundaries: Labor Day
09-07, DST 11-01 (now guarded), no fixture date within 45d. No finding; counter +1 (4).

**A11 — SELF-EXTENSION (FULL form — proposed rotation, OWNER-GATED).**
- **Candidate lens: SECURITY & CREDENTIAL HYGIENE** (secret scanning, git-history exposure, key
  rotation posture, RLS/permission surfaces, MCP/tool allow-lists).
- **What the incumbent structurally misses:** Calendar & Clock watches TIME boundaries; it
  cannot see credential drift, history leaks, or permission-surface growth. The incumbent's
  headline (winter-close) SHIPPED and was content-verified tonight; its look-list is in
  maintenance mode (next boundary already guarded) — the slot's marginal yield is falling.
- **Evidence the new lens catches real things:** F-FREE-1 (checked-in Supabase keys) was found
  by an un-lensed free look, not a charter; its two operator tails (git-history cleanup +
  secret-scanning enablement) remain OPEN and unaudited; tonight's sandbox read shows the
  nightly's own allow/deny list is itself a growing security config with no standing reviewer.
- **Cost:** nightly = 1 subagent question; FULL = quarterly sweep. Never proposes loosening;
  never touches the contract. Rotation decision is the owner's.

---

## 5. FREE LOOK (allowance: 2 SQL + 1 subagent; spent: 1 SQL + 1 file read)

(1) SQL: 30d baseline for the `condor_*` rejection reasons that looked new in the 07-10 mix —
NOT new (condor_ev_not_computed 132/30d · condor_no_viable 136 · condor_no_deltas 24); reported
as nothing. (2) Read `audit/nightly-settings.json`: the loop's cage is real and correctly shaped
(Bash/PowerShell/web denied; all mutating MCP tools denied; writes scoped `audit/**`) — one
observation feeds A11 above (the allow-list is a security surface with no auditor). Instinct
satisfied; no anomaly.

## 6. TOP-3 (value / effort / risk)

1. **Before Monday 17:00 CT if a build slot exists (owner's call): stamp the thesis price basis**
   (A9 finding — one field + one line in thesis_basis, observe-only surface, zero decision-path
   risk). Shipping it BEFORE the first authoritative fill means the new table is born honest
   instead of needing a backfill+re-grade later. If no Sunday build: treat Monday's rows as
   provisional and ship it post-close Monday.
2. **The 3-in-1(+1) observability PR — now slipped 3 build days and it costs real signal:** this
   window alone added 9 phone-egresses for one known condition + 10 flat-book false HIGHs, and
   the #1104 residual lost rows AGAIN (2nd occurrence, 11 cumulative). Fold in the
   iv_daily_refresh EXPECTED_JOBS registration (the surviving half of that gap). One PR,
   observability-side only.
3. **Monday morning ritual (no build):** confirm the healthchecks DOWN email for the missed
   07-11 nightly ping actually arrived (if not, the audit dead-man is unarmed — then fix the
   sleep-hold: WakeToRun starts the machine but holds no wake-lock; add an ES_SYSTEM_REQUIRED
   wrapper or equivalent) · 16:00Z: first [APPLY_ORDER_SHADOW] + [GATE_QTY] clean observe lines
   + replay capture rows-exist (E16 re-scope: existence + timing ONLY) · 17:00 CT first thesis
   job run (expect PARTIAL if any leg unscorable — that's the contract working) · queue ① (E8
   per-user seam / broker-ack territory) remains the #1 code build.

## 7. CONFLICTS · SYNERGIES · FOUR-SOURCE DISAGREEMENTS

**Conflicts:** none — nothing proposed tonight touches a decision path. **Synergies:** the
thesis-basis stamp (TOP-1) and F-A9-1 live in the same two files — one small PR; the
iv_daily_refresh EXPECTED_JOBS half rides the 3-in-1 PR (same ops_health territory); the
`bucket_exposure_would_block` severity question belongs INSIDE the composed observe→enforce
owner decision (don't ship separately). **Four-source disagreements:** none new — broker, DB,
Railway, and code agreed everywhere checked tonight (flat book × 0 open positions × SHA parity ×
persisted calibration math). CODE-vs-PROMPT-STATE drift (3 items: iv_daily_refresh half-fixed ·
watchdog flipped to paper_learning_ingest · P0-B build-half complete) is listed for the owner's
next v5.5→v5.6 STATE refresh — movers named, not alarms; this loop does not edit the prompt.

## 8. RETIREMENT COUNTERS

| Area | Tonight | Consecutive no-finding |
|---|---|---|
| A1 | charter answered; zero-entry regime live-proven (existing exhibit, no new defect) | 4 |
| A2 | no finding; PoP-clamp custody content-verified clean | 2 |
| A3 | no finding; boundary crossed + apply-move verified observe-safe | 4 |
| A4 | FINDING (audit-runner sleep-death mechanism; operator-side) | 0 |
| A5 | quantified continuation (no new class) | 2 |
| A6 | no finding, conditions checked (moot-upstream flag cross-ref'd to A1) | 4 |
| A7 | dormant (fills 3/10) | — |
| A8 | no finding (NOs honest on calibrated basis; sentinel quiet) | 3 |
| A9 | **FINDING (thesis fallback-price grade)** | 0 |
| A10 | no finding; winter-close verified shipped; flag inventory clean | 4 |
| A11 | FULL-form rotation proposal delivered (SECURITY lens, owner-gated) | — (permanent) |

*(No area at 6; no retirement proposal. A10 rotation is A11's proposal above, owner-gated.)*

## 9. FULL SCORECARD — diff vs 2026-07-03 FULL (verdict deltas only)

| Area | 07-03 verdict → 07-12 verdict |
|---|---|
| A1 | NEEDS-REFRAME → **SOUND, reframed & custody-expanded (sizing)**; constraint now proven-arithmetic, owner lever identified |
| A2 | SOUND → SOUND; 07-03's "bucket-correlation control (owner)" **SHIPPED as #1171 observe** — value loop closed |
| A3 | SOUND (raw-mode acceptance) → SOUND; **calibration live in production + falsifier built** |
| A4 | SOUND → SOUND; new liveness class found (audit runner itself); typed-contract found→shipped→load-bearing |
| A5 | SOUND → SOUND-but-SLIPPING (noise fixes found 07-08 remain unshipped; only found-item not converging) |
| A6 | NEEDS-REFRAME → reframe ADOPTED and EARNING; currently moot-upstream (A1 exhibit) |
| A7 | MERGE proposal → enacted; dormant 3/10 |
| A8 | SOUND → SOUND; first calibrated-basis NO verification |
| A9 | SOUND, 9 findings → SOUND, 1 new finding tonight; taxonomy (#1134) shipped and holding |
| A10 | new lens → **lens COMPLETED its headline (winter-close shipped+verified)**; rotation proposed via A11 |
| — | 07-03 owner-decision list: items 1,2,3,4,5,9,10 ENACTED; 6 (F-A1a ghost-restore), 7 (stuck-running reaper), 8 (OUTPUT_FRESHNESS registration) still open — 8 now rides the 3-in-1 PR |

## 10. LEDGER UPDATES MADE

Appended `status:reported` 2026-07-12 FULL entry: A9 thesis-price-basis finding · A4
sleep-death mechanism + dead-man-verify operator item · verifications CLOSED (first calibrated
production ev · breaker suppression #2 · winter-close content-verified · six weekend flags
call-time · weekend-PR wiring verified: #1171 seam order, #1174 off-path byte-identical, #1178
all-branch clamp, #1164 Monday wiring) · prompt-STATE correction list (3 items) · pendings
carried (Mon: replay rows-exist [E16 re-scope] · thesis first run · first shadow observe lines ·
native CLOSE_FILL_GAP/last_marked_at still gated on a live close).

STOP. Nothing implemented; READ-ONLY contract held (writes: this report + ledger only; no ALERT
file — zero criticals).
