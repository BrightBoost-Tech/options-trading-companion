# AUDIT v5.5 — NIGHTLY — 2026-07-14 (Tuesday 00:00 CT scheduled run)

**Mode:** NIGHTLY (Tuesday; FULL is Sundays only). Prior report = 2026-07-13 (Monday nightly,
covered the Sunday build window). **Window audited:** 2026-07-13 05:01Z → 2026-07-14 05:00Z —
a **full Monday RTH day PLUS two post-close merges** (#1198 logging, #1199 tape integrity); the
richest moved-signal window in a week.
**STEP 0 clock grounding:** DB `now()` = 2026-07-14 05:00:35Z = **Tue 00:00:35 CT** (dow=Tue);
broker clock 2026-07-14 01:00:35 ET = 00:00 CT — **agree to the second**, market CLOSED, next open
Tue 09:30 ET. No premise correction; all time arithmetic below is clock-grounded. Tuesday ⇒ NIGHTLY
(not the Sunday-FULL path) — matches the invocation.
**Run limitation: NONE — this run is NOT broker-blind.** Invoked interactively; the Alpaca MCP
surfaced and broker truth was read directly (3 calls: clock + positions + account). Deviates
(favorably) from the "headless broker-blind" STATE expectation.
**H8 run-boundary pin:** run-START SHA = run-END SHA = **`f34d5cd`** (#1199) — the ONLY non-REMOVED
Railway deployment (SUCCESS 2026-07-13 20:08:47Z), = local HEAD = origin/main. No overnight mover.
**Budget actuals:** ~11 substantive SELECT SQL + 4 phantom-column introspection misses (mine;
counted) · 3 broker · 1 Railway read · 0 subagents · FREE LOOK spent (1 SQL, resolved to a known
item — reported below) · report ≪ 500 lines.

---

## 1. ALERT

**None. No ALERT-2026-07-14.md.** H11 baseline (48h, critical/high grouped): the ONLY critical
type is `ops_job_never_run` ×9 (latest 2026-07-13 21:07Z) — the **known thesis_tracker hourly arm**,
self-resolved at its 22:00Z first run (job_runs: `thesis_tracker` succeeded 22:00:02Z; zero
`ops_job_never_run` since 21:07Z). Warning classes are all known/ledgered: `signal_accuracy_degraded`
×20 (the 1/6-win baseline), `chain_mechanics_formula_anomaly` ×10 (§8 noise), the autopilot costume
×4 (see A5/A9), `bucket_exposure_would_block` ×3 (shadow observe). No new critical class.

---

## 2. PART 1 — WEEK IN REVIEW (delta vs the 07-13 nightly)

- **Monday 07-13 was another live-proven ZERO-ENTRY day.** 6 suggestions generated, **0 orders,
  0 opens, 0 closes**, 665 rejection rows (over-counted ~2× per the A5 inner+outer double-record).
  Pool stays **8/8 post-epoch (1W/7L)** by construction; close-fill-gap counter stays **3/10–15**.
- **Broker re-confirmed flat, unchanged to the cent:** positions `[]`; equity = cash = OBP =
  portfolio_value = **$2,067.86**; `last_equity` $2,067.86; position_market_value 0; balance_asof
  still 2026-07-10. A2 one-beta condition holds (0 live positions).
- **Two post-close merges shipped Monday 20:0xZ (both H8-verified in the 07-13 ledger, re-pinned
  tonight):** PR-0 #1198 `1386834` (process logging — root INFO + stream handler; every app
  `logger.info` had been destroyed in-process) and PR-② #1199 `f34d5cd` (replay tape integrity —
  blob bytea hex fix + atomicity gate). **Both fire their FIRST natural test in this coming RTH
  (07-14), not in the window just audited** — the window's RTH cycles ran under the OLD code.
- **F-REPLAY-FK confirmed still-latent-through-07-13 (KNOWN, not re-found):** `data_blobs` = **0
  all-time**; `decision_runs` = 5, **all `failed`** (latest 17:29Z) — exactly the 5 unrecoverable
  runs the 07-13 morning diagnosis named. #1199 merged at 20:08Z, after the last decision cycle;
  first exercise = **07-14 13:00Z** (expect the first-ever `data_blobs>0` + `tape_integrity`
  ='complete'). Nothing to add tonight; verify IF it fired.
- **Calibration ×0.5 CONFIRMED reaching production EV.** Every aggressive (live-champion)
  suggestion this window carries `ev = ev_raw × 0.5000` exactly. Re-scope respected (F-A1-3): this
  proves the multiplier reaches the persisted ev + the final roundtrip gate ONLY — NOT
  selection/sizing (those score RAW upstream). Not a finding; the ×0.5 floor is SETTLED
  (floor-HOLD until ~15–20 live closes).

## 3. PIN GRADES (07-13 pins re-verified tonight)

| Pin | Verdict |
|---|---|
| Running SHA = origin/main | ✅ `f34d5cd` (#1199) — sole live deployment, SUCCESS 20:08:47Z; = local HEAD; no overnight mover |
| Breaker armed-quiet; flag-only-if list | ✅ `entries_paused=false`, reason NULL, unchanged since 07-09 11:53Z; `streak_breaker_state.last_tripped_fingerprint` intact (3 ids, 07-08 21:20Z trip); **0 closes ⇒ window unchanged ⇒ edge-trigger correctly did not re-pause**; zero flag-only-if conditions met |
| Universe = 78 | ✅ unchanged by continuity (no `scanner_universe`-touching merge this window; #1198+#1199 only) |
| Pool 8/8 (1W/7L) · fills 3/10–15 | ✅ unchanged — 0 closes this window; broker-confirmed flat |
| data_blobs / decision_runs (F-REPLAY-FK) | ✅ **0 / 5-all-failed — exactly the known pre-fix state**; #1199 fix awaits 07-14 13:00Z first exercise |
| Calibration ×0.5 in production | ✅ `ev = ev_raw × 0.5000` on all aggressive suggestions (F-A1-3 scope: persisted-ev + gate only) |
| E6/P0-A broker-ack-close | unchanged — runtime first-occurrence pin still open; 0 live closes this window (custody: A2) |
| P0-B book-blindness | unchanged — book flat, latent (reopens at ≥2 concurrent live) |
| SOFI sentinel (A8) | ✅ quiet — 2 SOFI debit spreads blocked at `edge_below_minimum` (UPSTREAM of the roundtrip gate; did NOT clear it) |
| EXCLUDED-EVIDENCE day 07-06 | ✅ respected (no citations) |

**Pending dispositions carried into 07-14 RTH:** (1) **first tape-complete decision_run** at 13:00Z
(`data_blobs>0` first time ever + `tape_integrity='complete'`); (2) **first RTH info-line proof of
#1198** — a `[RISK_MONITOR]`/shadow-heartbeat INFO line reaching Railway during an RTH cycle (the
ledger already proved it post-close via `[ALPACA_SYNC]`; the RTH shadow-clock restarts at
`1386834`); (3) native `[CLOSE_FILL_GAP]` + first post-#1137 `last_marked_at` — still gated on a
live close; (4) first thesis fill with populated `price_basis` — gated on a fill.

---

## 4. THE ELEVEN AREAS

**A1 — PROFITS.** No finding; conditions checked. Monday reproduced the live-proven zero-entry
regime: 6 suggestions, all blocked (SOFI ×2 `edge_below_minimum`; QQQ condor ×4
`ev_below_roundtrip_cost`). Binding constraint UNCHANGED = the $15 roundtrip floor over the current
universe, now further tightened by the SETTLED ×0.5 calibration floor at the gate (do-not-re-flag
per the 07-13 L1 correction). Structural, not outcome-based (N-disqualifier binds at 8 closes).
Next real evidence event: 07-14 16:00Z (first calibrated + logging-visible + tape-complete scan).

**A2 — LOSSES.** No finding. Exit/close territory was NOT moved this window (#1198 logging + #1199
tape integrity touch neither the exit evaluator nor the stop/clamp chain); 0 live closes; book
broker-confirmed flat (one-beta condition holds). The E6 broker-ack-close runtime first-occurrence
pin remains the open item in this custody (P0-A, unexercised — no live close to exercise it).

**A3 — SELF-LEARNING.** No finding. Calibration confirmed applying ×0.5 in production (out of raw
mode, SETTLED). Full learning chain ran clean Monday (learning_ingest → paper_learning_ingest →
policy_lab_eval → post_trade_learning → promotion_check → daily_progression_eval, all succeeded,
21:00–22:00Z). The n=3 segment-calibration admission (ledger 07-13 CLAIM-2a) stays LATENT — the
live blob is still `_overall`-only. [A7-dormant rider: 0 fills; hold-time/exit-quality untouched.]

**A4 — SELF-SUSTAINING.** No finding. The F-REPLAY-FK class (job green while `data_blobs` FK-failed
100% from inception) is the archetypal A4 silent failure — already found and fixed (#1199), its
FIRST loudness proof pending 07-14 13:00Z. All 22 job types succeeded Monday; the ping-after-file
self-check held (this report exists). Known gaps unchanged (iv_daily_refresh ok-on-all-missing +
absent from EXPECTED_JOBS; watched `learning_ingest` no-op stub vs unwatched `paper_learning_ingest`
— watchdog-coverage PR still pending).

**A5 — EFFICIENCY.** Quantified continuation, no new class. The **autopilot costume recurred 4×
Monday** (`paper_autopilot_cohort_per_suggestion_failed` at 14:06/16:09/16:30/17:31Z, all
`distinct_error_classes:["EntryRoundtripCostExceedsEV"]`, QQQ) — the KNOWN 07-10 A5/A9 item
(designed roundtrip-gate NOs surfaced as "executions failed"), fix still riding the slipping 3-in-1
observability PR (now ~5th consecutive build day of slip). NEW blast-radius note: each fire also
emits a `logger.error("policy_lab_execute_error…")` line — an ERROR (so it reached Railway even
pre-#1198) that reads as a real fault. The meter is live again (RTH day); this class + the
flat-book false-ager + hourly re-egress are the standing charge. Observability-side only.

**A6 — VIABLE-SET HANDLING.** No finding; conditions checked. Tier unchanged ($2,067.86 broker),
no rejection-mix regime shift (top classes = the standard economic set:
`no_fallback_strategies_available` 195 / `spread_too_wide_real` 124 / `execution_cost_exceeds_ev`
110), no structurally-dead name cleared a gate. The 1-of-N economics SETTLED condition holds.

**A7 —** dormant, fills 3/10, exit-code custody: A2.

**A8 — NEGATIVE-DECISION EFFICACY.** No finding. 6 NOs to grade Monday, all correct: SOFI ×2
blocked at `edge_below_minimum` (sentinel QUIET — it did NOT clear the roundtrip gate, so the
SETTLED trigger did not fire); QQQ condor ×4 blocked at `ev_below_roundtrip_cost` (the neutral-shadow
twin blocked at raw ev=46.66 too — the block is roundtrip-cost-bound, NOT a calibration artifact,
so no money hidden by the ×0.5 floor on this name).

**A9 — ALERT & SIGNAL INTEGRITY.** No NEW finding. FREE-LOOK dug the sole unexplained warning class
(`paper_autopilot_cohort_per_suggestion_failed` ×4) to its root — it is the KNOWN autopilot costume
(ledger 07-10): `EntryRoundtripCostExceedsEV` lacks a dedicated `except` clause in
`_execute_per_cohort`, so a designed #1101 gate NO falls to the catch-all `except Exception`
(paper_autopilot_service.py:1182-1191) and is dressed as "executions failed / did not execute as
expected," unlike the sibling enforced blocks that each `continue` without polluting the aggregation
(SymbolCooldownActive :1131, EntryUtilizationBlocked :1138). Confirming, not extending — fix already
queued in the observability PR. No new liar surfaced.

**A10 — CALENDAR & CLOCK INTEGRITY.** No finding. This run itself crossed the UTC-midnight boundary
that most exposes A10 (STEP-0: both clocks agreed to the second). Import-time flag inventory did NOT
grow with the two merges: #1199 is a serialization/atomicity fix (no new gated flag); #1198 adds a
process-logging config at startup — a once-per-process side effect worth noting in the recycle-scope
inventory, but not a decision flag. Next boundaries unchanged (DST 2026-11-01 = winter-close, guarded).

**A11 — SELF-EXTENSION (nightly, one line).** Nothing new beyond the already-queued SECURITY lens
(A10's next owner-gated rotation) — but see the retirement-counter note: four charters reaching 6
is an artifact of the structurally quiet zero-entry/flat-book regime, not territory coverage, which
is itself the question no area is asking ("are the no-finding counters measuring quiet-market or
covered-territory?").

## 5. FREE LOOK (allowance 2 SQL + 1 subagent; spent: 1 SQL, 0 subagents)

Instinct target: the one warning class on the H11 sheet I could not immediately name —
`paper_autopilot_cohort_per_suggestion_failed` ×4 (07-13), on the executor entry path. Pulled its
metadata (`distinct_error_classes:["EntryRoundtripCostExceedsEV"]`, ticker QQQ, cohorts
aggressive+neutral) and traced the code. **Resolved to a KNOWN item** (the 07-10 A5/A9 autopilot
costume) — not a new finding; reported per contract. The value of the look: it re-confirmed the
costume is still un-fixed and now firing during a fully-visible RTH day, sharpening the A5 slip case.

## 6. TOP-3 (value / effort / risk)

1. **07-14 RTH is the first day BOTH big fixes get exercised — verify, don't rebuild.** At 13:00Z
   confirm the first tape-complete `decision_run` (`data_blobs>0` first ever, `tape_integrity`
   ='complete'); during an RTH cycle confirm a `[RISK_MONITOR]`/shadow-heartbeat INFO line now
   reaches Railway (#1198). Both are pure verifications of already-shipped code; no build.
   Value: high (closes two multi-day pending pins). Effort: low. Risk: none (read-only).
2. **Give the 3-in-1(+1) observability PR a slot — the autopilot costume is now firing on a
   fully-visible RTH day (4×/day) and the meter is live.** Same recommendation the 07-13 report
   made; the only delta is one more day of accrual. Observability-side only, no decision-path risk.
3. **Nothing else demands action.** Book flat, breaker armed-quiet, calibration behaving, all jobs
   green — an honest quiet night. Do not manufacture a finding.

## 7. CONFLICTS · SYNERGIES · FOUR-SOURCE DISAGREEMENTS

**Conflicts:** none — nothing proposed touches a decision path (all Top-3 items are verify-only or
the already-queued observability PR). **Synergies:** the 07-14 tape-complete verification (Top-3 #1)
and the observability PR (Top-3 #2) share the ops/roll-up surface — sequence, don't entangle.
**Four-source disagreements: NONE** — broker (flat, $2,067.86), DB (0 orders/0 closes/6 blocked
suggestions/quiet alerts), Railway (SUCCESS @ `f34d5cd` ×3 services), and code (content at the
squashed SHA) agreed everywhere checked. **Prompt-STATE drift for the next v5.5→v5.6 refresh**
(movers named, not alarms): running-SHA STATE line still pins `655c9aa`/#1143 — HEAD is now
`f34d5cd`/#1199 (doc + logging + tape-integrity movers since, no behavior change beyond #1198/#1199)
· the "first calibrated production scan pends 07-10 16:00Z" STATE line is stale — calibration ×0.5
has been in production since 07-10, re-confirmed tonight.

## 8. RETIREMENT COUNTERS

| Area | Tonight | Consecutive no-finding |
|---|---|---|
| A1 | no finding; conditions checked | **6** |
| A2 | no finding; exit territory not moved; book flat | 4 |
| A3 | no finding | **6** |
| A4 | no finding (F-REPLAY-FK found+fixed; loudness pending) | 2 |
| A5 | quantified continuation (costume 4× RTH; no new class) | 4 |
| A6 | no finding; conditions checked | **6** |
| A7 | dormant (fills 3/10) | — |
| A8 | no finding (6 NOs graded correct; sentinel quiet) | 5 |
| A9 | FREE-LOOK resolved to a known item; no new liar | 2 |
| A10 | no finding; flag inventory clean | **6** |
| A11 | one-line nightly delivered | — (permanent) |

*A1/A3/A6/A10 reach **6** = retirement-candidate PROPOSAL territory (owner-gated, NEVER unattended).
Honest read for the owner: these counters are climbing because the market/book has been
structurally quiet (weekend + zero-entry + flat book), NOT because the territory is now covered
elsewhere — A1 is the canonical profits charter and A2/A6 guard the live-money viable-set path. A
proposal here would fail its own "territory covered elsewhere" test. A10's path is A11's rotation
(Security lens already queued), not retirement. Recommend: keep all four; the counters are a quiet-
regime artifact worth an owner glance, not a retirement action.*

STOP. Nothing implemented; READ-ONLY contract held (writes: this report + the ledger entry only;
no ALERT file — zero new criticals).
