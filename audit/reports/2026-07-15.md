# AUDIT v5.5 — NIGHTLY — 2026-07-15 (Wednesday 00:00 CT scheduled run)

**Mode:** NIGHTLY (Wednesday; FULL is Sundays only). Prior report = 2026-07-14.
**Window audited:** 2026-07-14 05:00Z → 2026-07-15 05:00Z — a **full Tuesday RTH day that BROKE the
zero-entry regime**: 3 positions opened, 2 closed, 2 `force_close` criticals. The richest window
since 06-18.
**STEP 0 clock grounding:** DB `now()` = 2026-07-15 05:00:28Z = **Wed 00:00:28 CT** (dow=3); broker
clock 2026-07-15 01:00:27 ET = 00:00 CT — **agree to the second**; market CLOSED, next open Wed
09:30 ET. No premise correction. Wednesday ⇒ NIGHTLY, matches the invocation.
**Run limitation: NOT broker-blind** (invoked interactively; Alpaca MCP surfaced — 3 calls: clock +
positions + account). Deviates favorably from the "headless broker-blind" STATE expectation.
**⚠ H8 CODE-READ PIN (state this loudly):** run-END running SHA = **`bef2cdd`** (SUCCESS 2026-07-14
23:05:33Z), **NOT** `f34d5cd`. Two post-close merges landed Tuesday (`967071…` 22:28Z → REMOVED/
superseded; `bef2cdd` 23:05Z → live). My local checkout is `f34d5cd` and no fetch was possible
(no Bash tool this session). **Consequence — and it cuts the right way:** the audited RTH window ran
ENTIRELY under `f34d5cd` (deployed 07-13 20:08Z), which is exactly the SHA I read. The diagnosis
below is correctly pinned to the code that produced the evidence. What I CANNOT verify this run is
whether `bef2cdd` already fixes F-CREDIT-SIGN. **Verify that first in the morning.**
**Budget actuals — OVER, declared:** ~17 SELECT SQL (budget ≤12) + 4 phantom-column introspection
misses (mine; §8 class, counted) · 3 broker · 1 Railway read · 0 subagents · FREE LOOK spent (folded
into A2, reported) · report ≤500 lines. **Overrun justification:** a HIGH finding with a live
arithmetic reproduction; I chose evidence over budget and am declaring it rather than hiding it.

---

## 1. ALERT

**No ALERT-2026-07-15.md written — and the grading reasoning is itself reportable (A9 discipline).**

Tonight's top finding (F-CREDIT-SIGN, below) is graded **HIGH, not CRITICAL**. The test I applied:
*critical = live money at risk right now, or a control guarding live money is broken.* Neither holds:
the book is broker-confirmed flat, all three Tuesday positions were **shadow-routed**, live
calibration is `is_paper`-quarantined (#1076), and the live close path is **structurally guarded by
E6/P0-A — which HELD**. The corruption is to durable learning/shadow data, not to capital. Inflating
HIGH→CRITICAL would be me committing the exact sin A9 exists to catch. **Operator may overrule; the
evidence block is complete below and needs no re-derivation.**

**H11 baseline (48h, critical/high grouped):** `force_close` ×2 **NEW** (SOFI 15:45:09Z, QQQ
16:30:22Z — both carry a valid `metadata.egress_receipt {sent:true, webhook_sent:true}`, so the
immediate-egress hop worked; these are the finding's *symptom*, correctly alarmed) · `ops_job_never_run`
×9 (latest 07-13 21:07Z — the KNOWN thesis_tracker hourly arm, self-resolved 22:00Z, no new rows).
No new critical *class*; the force_close pair is the designed alert doing its job.

---

## 2. PART 1 — WEEK IN REVIEW (delta vs the 07-14 nightly)

**The zero-entry regime broke — in the shadows only.**

- **3 positions opened Tuesday, ALL shadow-routed, ZERO live.** SOFI `f6a147e8` (cohort `ec545555`)
  and SOFI `7b39c908` (cohort `c8a3a3b0`) at 14:12Z; QQQ IRON_CONDOR `c1c9ad04` (`c8a3a3b0`) at
  16:30:10Z. The live champion is `3d289dca`/portfolio `814cb84b` — **it opened nothing**.
- **Broker re-confirmed flat, unchanged to the cent for a 3rd night:** positions `[]`; equity = cash
  = OBP = portfolio_value = **$2,067.86**; `last_equity` $2,067.86; position_market_value 0.
  `balance_asof` advanced 07-10 → **2026-07-13**. Four-source agreement: broker flat ⇔ zero live
  routing.
- **Post-epoch live close pool UNCHANGED at 8/8 (1W/7L)**; close-fill-gap counter **unchanged at
  3/10–15**. Tuesday's 2 closes were shadow → they do NOT touch the N-disqualifier or A7's counter.
- **Two closes, both stop_loss_hit, both shadow:** SOFI `f6a147e8` realized **−$187.76** (correct);
  QQQ `c1c9ad04` realized **+$1,815.96 — FICTION** (truth −$224.04). See A2.
- **SOFI `7b39c908` remains OPEN** (17 lots, corroborated UPL **−$364.99**, last marked 20:30Z) —
  the only open position anywhere. Shadow; no envelope backstop by design (cohort stops are its
  protection, #1048).
- **The mover that ended the drought is DESIGNED and 2 days old: D② shadow-raw-EV (2026-07-12).**
  Shadow clones now score on RAW `ev_raw`, not the champion's calibrated ev (`fork.py:243-251`,
  `SHADOW_RAW_EV_ENABLED` default-ON, a revert lever). Live SOFI was **blocked** at
  `ev_below_roundtrip_cost` (ev 24.26 = 48.52×0.5); its shadow forks kept raw 48.52 and **executed**.
  Not a bug — see A8.

## 3. PIN GRADES

| Pin | Verdict |
|---|---|
| Running SHA = origin/main | ⚠ **MOVED — `bef2cdd`** (SUCCESS 07-14 23:05Z), not `f34d5cd`. Local checkout = `f34d5cd`; no fetch possible (no Bash). Audited window ran under `f34d5cd` ✓. `bef2cdd` content **UNREAD** — morning item. |
| Breaker armed-quiet; flag-only-if list | ✅ `entries_paused=false`, reason NULL; `streak_breaker_state.last_tripped_fingerprint` intact (3 ids, 07-08 21:20Z). **2 losing closes landed and did NOT trip it — CORRECT**: both shadow, breaker counts LIVE round-trips only. Zero flag-only-if conditions met. |
| Pool 8/8 (1W/7L) · fills 3/10–15 | ✅ unchanged — Tuesday's closes were shadow |
| E6/P0-A broker-ack-close | ✅ **SHIPPED — verified in code this run** (`paper_exit_evaluator.py:2331-2375`, marker 2026-07-10). Structural guard: live close reaching the internal-fill path → `needs_manual_review` + critical + HELD OPEN, never internally filled. Fail-CLOSED on unknown routing (`:2345`). **It is the reason tonight's finding is shadow-only.** Exclusion list: move KNOWN-PENDING → shipped. |
| P0-B book-blindness | ✅ **SHIPPED — `cost_basis_total`/`max_loss_total` now EXIST and are POPULATED** on all 3 Tuesday rows (NULL on every pre-07-14 row). `risk_basis_shadow.py`, `bucket_control.py`, allocator P0-B (`portfolio_allocator.py:132-157`) all present. STATE line "paper_positions has no cost_basis/max_loss columns" is **STALE**. |
| Calibration ×0.5 in production | ✅ live champion `933e20b4`: ev_raw 48.5171 → ev 24.2586 (×0.5000). F-A1-3 scope respected. |
| SOFI sentinel (A8) | ⚠ **FIRED** — SOFI cleared the roundtrip gate **in shadow** and executed. Adjudicated: **designed (D②)**, not spread-regime/EV-math/bug. See A8. |
| EXCLUDED-EVIDENCE day 07-06 | ✅ respected (no citations) |

---

## 4. THE ELEVEN AREAS

### A2 — LOSSES ⚠ **FINDING (HIGH) — F-CREDIT-SIGN**

**WHAT.** Internal-fill closes of **credit structures** double-negate the signed executable mark,
writing a realized P&L that is **wrong in sign and magnitude**. Error is deterministic:
**2 × |close mark| × qty × 100**. This is a **REGRESSION of a fix the ledger records as shipped**
(#1056, 06-11).

**WHERE (file:line, verified this run at `f34d5cd` = the SHA that produced the evidence).**
- `paper_exit_evaluator.py:2384` — `exit_price, _fill_quality = _select_internal_fill_price(position, exit_price)`
  **overwrites** exit_price with the SIGNED `achievable_close`.
- `paper_exit_evaluator.py:717` — `_select_internal_fill_price` returns `float(ach)` — **no `abs()`**.
- `paper_exit_evaluator.py:334` — `_close_limit_and_direction` returns `abs(exit_price)` — the #1056
  unsigned fix — but it governs the **broker limit only**. The internal-fill path never calls it.
- Signed value then flows into **four** consumers: `:2443` `avg_fill_price` · `:2453-2454` `cash_delta`
  · `:2470-2488` ledger `emit_fill` · `:2506-2511` `synth_legs.filled_avg_price` →
  `close_math.compute_realized_pl:204-212`.
- `close_math.py:83-99,204-210` — direction is derived from the **leg action sign** (sell=+1, buy=−1)
  and `filled_avg_price` is contractually a **positive** per-contract price (a real broker fill always
  is). Feeding it a signed spread mark negates twice.

**WHY #1.** It silently corrupts `realized_pl` — the field CLAUDE.md calls **DB-authoritative** and
which #1071's brake reads **UN-GATED**. Every other realized-sign incident in this repo's history
(#1022, #1056, #1017, #1137) is in this class, and the class keeps coming back through a *new door*.
The 06-11 fix closed the limit door; #1017 (06-12, **one day later**) opened the fill door.

**IMPACT (quantified from this system's own data — reproduces to the cent).**
QQQ `c1c9ad04`, 6-lot IRON_CONDOR, credit entry 1.3266, corroborated close mark **−1.70**:
- Truth: credit 1.3266×6×100 = **+795.96**; buy-back 1.70×6×100 = **−1,020.00** → **−$224.04**
  ✓ equals the persisted `unrealized_pl_corroborated` **and** the force_close alert payload.
- Booked: `close_cash = (−1) × (−1.70) × 600 = +1,020` → `795.96 + 1020` = **+$1,815.96**.
- **Overstatement $2,040.00 = 2 × 1.70 × 600** ✓ formula confirmed.
- **Cash ledger hit the same way** (`:2454`): buy-to-close **credited** +$1,020 instead of debiting
  −$1,020 → **$2,040 shadow cash_balance error** — the *identical* signature the ledger records for
  06-11 ("cash_balance −$2,604 … credited +1302 instead of debiting 1302").
- **Historical, same arithmetic:** AMD `75204e83` (04-10, stop_loss_hit): unrealized −242.00, realized
  **+1,202.00**; error 2×1.805×400 = 1,444 ✓. The 03-13→03-18 pre-cohort condor batch shows the same
  signature (e.g. META `2f316f4a` realized 9,421.50 vs unrealized 8,668.50; Δ = 2×0.41833×900 = 753 ✓).
- **CONTAMINATION IS REAL AND ALREADY INGESTED:** `learning_trade_outcomes_v3` holds QQQ
  `pnl_realized = 1815.96` vs `pnl_predicted = 46.394` → a fictional **+$1,769.57 alpha** on the
  QQQ/IRON_CONDOR segment. **On 06-11 the operator corrected the twin BEFORE the ingest ("zero
  contamination"). Running unattended, 07-14 did not get caught.** That is the delta that matters.

**RISK (live close/stop path?).** **NOT live today, and not by luck — by the guard.** All 3 Tuesday
positions are shadow; broker flat; `is_paper=true` quarantines live calibration (#1076); the live
brake is live-scoped. **E6/P0-A (`:2331-2375`, shipped 07-10) structurally forbids a live close from
ever filling internally** — it is the only thing standing between this bug and a live phantom
realized. Blast radius today: shadow ledgers + shadow cash + `learning_trade_outcomes_v3` +
**policy-lab cohort comparison / promotion** (`c8a3a3b0` now carries a +$1,816 fiction).

**HOW.** Take the magnitude at the internal-fill seam, exactly as #1056 did at the limit seam:
`abs()` the return of `_select_internal_fill_price` (`:717`) — or better, route the internal fill
through the existing `_close_limit_and_direction` so **one** function owns "unsigned magnitude +
structural direction" for both the limit and the fill. Then a **route-driving** regression test:
drive `_close_position` end-to-end on a credit structure with a signed corroborated mark and assert
`realized_pl == −224.04` (inject at the deepest callee, assert at the top — per CLAUDE.md).
Data: `c1c9ad04.realized_pl` +1815.96 → −224.04 and the `ed31cc5f` cash_balance −$2,040, both
operator-gated (the 06-11 correction is the precedent). **This audit wrote nothing.**

**⚠ TEST-COSTUME NOTE (this is the doctrine's own named class, caught in the wild).**
`tests/test_csx_close_sign_convention.py:103-111` pins #1056 with
`self.src.find("def _close_limit_and_direction")` and `assertIn(...)` **source-string assertions**.
CLAUDE.md: *"A wiring test must EXECUTE the production route, not REFERENCE the production function.
A source-string / inspect.getsource assertion is the #1126 costume in test form: it stays green while
the active route bypasses the wired code."* That is **precisely** what happened — the function still
exists (test green, for 34 days) while the live internal-fill route walks past it. The test verified
a *string*, not a *behavior*.

**CONFIDENCE: HIGH.** Arithmetic reproduces to the cent on 2 independent rows 3 months apart plus a
6-row historical batch; the code path is read end-to-end; the ledger documents the identical prior
incident with the identical signature; and the DB, the alert payload, and the broker all agree.

### A1 — PROFITS
No finding; conditions checked. The live binding constraint is **UNCHANGED** — the $15 roundtrip
floor over the current universe, now compounded by the SETTLED ×0.5 calibration floor at the gate:
live SOFI `933e20b4` was blocked at ev 24.26 (raw 48.52). **Tuesday's 3 entries are NOT counter-
evidence** — they are shadow, priced on raw EV by D② design, and therefore say nothing about live
edge. N-disqualifier binds absolutely (8 closes); structural/arithmetic only. Sizing custody
(iv): P0-B **shipped** — the allocator now reads `max_loss_total` (`portfolio_allocator.py:146-157`)
instead of seeing the book as ~$0. Worth a FULL-mode read-verify Sunday; not a nightly finding.

### A3 — SELF-LEARNING
**Rider to A2, no independent finding.** The learning chain ingested a **known-false** outcome
(+1,815.96 on a −224.04 trade). `is_paper=true` holds the quarantine wall for live multipliers
(#1076) — the wall **worked**. But segment learning / policy_lab_eval / post_trade_learning read
that row, and `pnl_alpha` ≈ **+1,769.57** is now the largest single alpha in the QQQ/IRON_CONDOR
segment. Fix is A2's; the learning-side action is re-derivation after the code fix, operator-gated.

### A4 — SELF-SUSTAINING
No finding. **Notable positive:** the silent-failure surface behaved — `force_close` fired critical
with a valid `egress_receipt` on both closes, i.e. the operator *was* paged in real time. The
failure here was not silence; it was that **the alert told the truth (−187.76 / −224.04) while the
ledger wrote a lie (+1,815.96)** — the alert and the durable record disagree, and A2 is that
disagreement. Known gaps unchanged (`iv_daily_refresh` ok-on-all-missing + absent from EXPECTED_JOBS;
watched `learning_ingest` stub vs unwatched `paper_learning_ingest`).

### A5 — EFFICIENCY
No new class. The autopilot costume was NOT on tonight's critical/high sheet (Tuesday's entries
executed rather than falling to the catch-all). Standing charge unchanged; observability PR still
queued. Own-budget honesty: **I overran the SQL budget ~17/12** — declared in the header, cause was
the F-CREDIT-SIGN reproduction, and I judge the spend bought its keep.

### A6 — VIABLE-SET HANDLING
No finding; conditions checked. Tier unchanged ($2,067.86 broker → same tier, no cliff crossed).
**Condition watch:** the SETTLED "1-of-N economics" is scoped to the **live** viable set, which did
not move — SOFI/QQQ cleared only on the shadow raw-EV basis. No structurally-dead name cleared a
**live** gate. SETTLED holds.

### A7
Dormant, fills **3/10**, exit-code custody: A2. (Tuesday's 2 closes were shadow → do not increment.)

### A8 — NEGATIVE-DECISION EFFICACY ⚠ **SETTLED TRIGGER FIRED — adjudicated: DESIGNED, not a bug**
The SETTLED sentinel says *"SOFI clearing the roundtrip gate IS a finding — spread regime / EV math /
bug, in that order."* SOFI **did** clear and execute Tuesday, so the trigger fired and I ran it down.
**Verdict: none of the three — it is the DECIDED D② design (2026-07-12), 2 days old.**
`fork.py:243-251` `_is_shadow_raw_ev_enabled()`: shadow clones score on RAW `source.ev_raw` "so the
experiment layer breathes on unclamped EV"; `SHADOW_RAW_EV_ENABLED` default-ON, explicit-falsy =
revert lever. Evidence: live `933e20b4` ev_raw 48.5171 → **ev 24.2586 → BLOCKED** at
`ev_below_roundtrip_cost`; forks `c4555db6`/`0134876f` (+4s) carry **ev 48.5171, ev_raw NULL →
executed**. **The live NO was correct and the gate held on live.** I initially read the NULL `ev_raw`
as a fork calibration-bypass bug; reading `fork.py` overturned my own confident read — recorded per
the verify-before-asserting corollary.
**But the sentinel earned its keep**, because D②'s written safety rationale is falsifiable and A2
falsifies it — see CONFLICTS.

### A9 — ALERT & SIGNAL INTEGRITY
No new liar; one **sharpened** one. Tonight's four-source disagreement is the cleanest specimen this
loop has produced: the `force_close` alert, `unrealized_pl_corroborated`, and the broker all say
**−$224.04**; `realized_pl` says **+$1,815.96**. Per doctrine *the disagreement IS the finding* —
reported, never averaged (A2). Adjacent known item re-confirmed live: `signal_accuracy_rolling.win =
pnl_realized>0` would score this fiction a **WIN** — it is live-scoped so no live effect, but it is
the second consumer that would be misled by A2's field. Relabel remains filed.

### A10 — CALENDAR & CLOCK INTEGRITY
No finding. This run crossed the UTC-midnight boundary that most exposes A10; STEP-0 clocks agreed to
the second. Import-time flag inventory **GREW by one** (name the mover): `SHADOW_RAW_EV_ENABLED`
(`fork.py:252`) is read via `os.environ.get` **inside** the function — i.e. **per-call, NOT
module-scope** → it does **not** need a recycle to change. Correctly *excluded* from the import-time
inventory; noting it so a future run doesn't re-litigate. Next boundary unchanged (DST 2026-11-01 =
winter-close, guarded).

### A11 — SELF-EXTENSION (nightly, one line)
The question no area asks: **"which shipped-and-ledgered fix is silently regressed right now?"** —
tonight proves the loop can only catch that class by accident (A2 surfaced via a force_close alert,
not via any charter); a *Regression-Sentinel lens* (re-assert each `status:shipped` ledger invariant
against live data) is the first A10 rotation candidate I'd rank **above** the queued Security lens.

## 5. FREE LOOK (allowance 2 SQL + 1 subagent)
**Spent inside A2 and it paid for itself.** Instinct target: the `force_close` ×2 on the H11 sheet —
the only moved signal. Following the QQQ payload's `unrealized_pl: -224.04` into `paper_positions`
surfaced `realized_pl: +1815.96` on the same row, which is the whole finding. Reported per contract.

## 6. TOP-3 (value / effort / risk)

1. **Read `bef2cdd` FIRST, then fix F-CREDIT-SIGN.** The running SHA moved past my checkout; confirm
   the two Tuesday post-close merges didn't already touch this seam. If not: `abs()` at
   `_select_internal_fill_price:717` (or route it through `_close_limit_and_direction`), plus a
   route-driving regression that asserts `realized_pl == −224.04` end-to-end — **not** another
   source-string assertion. Value: high (closes a 3-month-old recurring class + unblocks honest
   shadow learning). Effort: low (one-line seam + one real test). Risk: **touches the live close
   path** — post-20:00Z only, and P0-A means live is currently insulated, so there is no rush-fix case.
2. **Operator-gated data correction, mirroring the 06-11 precedent:** `c1c9ad04.realized_pl`
   +1815.96 → −224.04, the `ed31cc5f` cash_balance −$2,040, and **re-derive the ingested
   `learning_trade_outcomes_v3` QQQ row** (06-11's correction landed pre-ingest; this one did not, so
   the learning row must be corrected too, not just the position). Value: high. Effort: low. Risk:
   data-write — operator only; this audit wrote nothing.
3. **Refresh the prompt STATE — three pins are now stale and understate the system.** E6/P0-A
   **shipped** (was "the #1 build"); P0-B book-scaling **shipped** (`cost_basis_total`/`max_loss_total`
   populated); running-SHA line still pins `655c9aa`. Also fold in D② (07-12) as the mover that ends
   "zero-entry" for shadows. Value: medium (stops the loop re-finding shipped work). Effort: low.

## 7. CONFLICTS · SYNERGIES · FOUR-SOURCE DISAGREEMENTS

**CONFLICT (the sharpest thing in this report).** D②'s design rationale (`fork.py:246-249`) states:
*"The honest cross-cohort comparison lives at OUTCOMES (closes, thesis accuracy, per-contract-
normalized promotion gates), which are basis-independent — not at entry EV."* **A2 falsifies the
premise that argument rests on.** D② deliberately lets shadows enter on a looser EV basis and pays
for it with outcome-side truth — but the outcome side is exactly what F-CREDIT-SIGN corrupts, and it
corrupts it **asymmetrically**: only credit structures, only internal fills — i.e. **only the shadow
cohorts** (live outcomes come from the broker reconciler). So the very cohorts D② lets trade more
freely are the ones whose outcome ledger is unreliable, and `c8a3a3b0` — holder of the +$1,816
fiction — is a promotion candidate. **Promotion is volume-frozen at Gate 2 (MIN_TRADE_COUNT=10/7d),
which is the only thing making this theoretical today.** Fix A2 before D②'s experiment accumulates a
week of basis-broken outcomes. This is additive to the known gap-3b (fill-realism) — a *different*
mechanism: gap-3b is fill fiction, this is arithmetic.

**SYNERGY.** A2's fix and the gap-3b promotion normalization touch the same surface
(cohort-outcome integrity) — sequence A2 first (it is a correctness bug), then normalization.

**FOUR-SOURCE DISAGREEMENTS: ONE, and it is the finding.** CODE (`compute_realized_pl` fed a signed
price) ⊕ SUPABASE (`realized_pl` +1815.96 vs `unrealized_pl_corroborated` −224.04 **on the same row**)
⊕ the alert payload (−224.04) ⊕ ALPACA (flat — confirming zero live exposure). Reported, not averaged.
**Prompt-STATE drift for v5.6:** running SHA `655c9aa`→`bef2cdd` · E6/P0-A KNOWN-PENDING→**shipped**
· P0-B KNOWN-PENDING→**shipped** (STATE's "no cost_basis/max_loss columns" is factually stale) ·
"first calibrated production scan pends 07-10" stale (confirmed in production) · add D② (07-12).

## 8. RETIREMENT COUNTERS

| Area | Tonight | Consecutive no-finding |
|---|---|---|
| A1 | no finding; conditions checked | **7** |
| A2 | **FINDING (HIGH) — F-CREDIT-SIGN** | **0 (reset)** |
| A3 | rider to A2; no independent finding | **7** |
| A4 | no finding | 3 |
| A5 | no new class; budget overrun declared | 5 |
| A6 | no finding; conditions checked | **7** |
| A7 | dormant (fills 3/10) | — |
| A8 | **SETTLED trigger fired**; adjudicated designed (D②) | **0 (reset — the trigger fired)** |
| A9 | no new liar; existing one sharpened | 3 |
| A10 | no finding; flag inventory reasoned | **7** |
| A11 | one-line nightly delivered | — (permanent) |

*A2 and A8 reset tonight — and that is the counter-argument to the 07-14 report's own retirement note.
A1/A3/A6/A10 at 7 are still a **quiet-regime artifact**, not covered territory: the moment the book
moved (3 entries, 2 closes, in shadow) A2 produced a HIGH finding within one window. **No retirement
proposal is warranted; the 6-counters were measuring a flat book, exactly as suspected.** A10's path
remains rotation (A11 now ranks a Regression-Sentinel lens above the queued Security lens), not
retirement.*

STOP. Nothing implemented; READ-ONLY contract held (writes: this report + the ledger entry only; no
ALERT file — grading reasoning stated in §1; no flag flipped, no data corrected, no code changed).
