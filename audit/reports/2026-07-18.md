# AUDIT v5.5 — NIGHTLY — 2026-07-18 (Saturday 00:00 CT scheduled run)

**Mode:** NIGHTLY (Saturday; FULL is Sundays only). Prior report = **2026-07-15** — the 07-16 and
07-17 scheduled runs both DIED (see A4, tonight's finding).
**Window audited:** 2026-07-15 05:00Z → 2026-07-18 05:01Z — **three full RTH days (Wed/Thu/Fri),
two of them never audited until now.**
**STEP 0 clock grounding:** DB `now()` = 2026-07-18 05:01:05Z = **Sat 00:01:05 CT** — matches the
invocation date; Saturday ⇒ NIGHTLY. **Broker clock UNREADABLE** (Alpaca MCP absent) — grounding is
single-clock (DB) tonight, the exact headless case STATE anticipates. No premise correction needed.
**Run limitations (two, both structural):**
1. **Broker-blind** (headless; Alpaca MCP never surfaced; 0 broker calls). Equity/flat-book claims
   below are DB-corroborated HYPOTHESES, labeled.
2. **Shell-blind — NEW, worse than broker-blind tonight:** no Bash for the session OR its subagents.
   Consequence: origin/main moved **~20 commits** past the local checkout (`f34d5cd7` → `bc4a00b2`)
   and **none of the new code is readable** (packed git objects, no `git show`). Code reads are
   pinned to `f34d5cd7`; every claim about the running code is labeled accordingly.
**Budget actuals — SQL OVER, declared:** 17 SQL executed = 12 productive + **5 phantom-column
failures (mine, §8 class — see Self-criticism)** · 0 broker · 3 Railway reads · 1 subagent (git
metadata; shell-less) · FREE LOOK spent (2 SQL, reported §5). Report ≤500 lines ✓.

---

## 1. ALERT DECISION

**No ALERT-2026-07-18.md.** Tonight's finding (A4: the audit loop itself dead 2 consecutive nights)
is graded **MEDIUM-HIGH, operator-side**. Test applied: *critical = live money at risk now, or a
control guarding live money broken.* Neither holds — production ran provably clean through the blind
window (192–193 job runs/day × 3 days, 22 job types, **zero failures**), the one critical was a
designed, receipted force_close, and the dead-man detection chain around the audit is armed and by
construction should have paged DOWN both mornings (ping withheld when no report exists —
`run-nightly.cmd:19-21` verified this run). The loop being dead is an availability failure of the
*auditor*, not the *system*. Operator may overrule.

**H11 baseline (72h, critical/high):** exactly **one** row — `force_close` critical, SOFI, 07-15
14:15:07Z, `unrealized_pl −534.99`, valid `egress_receipt {sent:true, webhook_sent:true}`. Zero
highs in 3 days (the `ops_output_stale` false-ager class stays dead post-#1137). Quietest
multi-RTH-day sheet on record.

---

## 2. PART 1 — WEEK IN REVIEW (delta vs the 07-15 nightly)

- **The zero-entry regime RESUMED.** The 07-14 shadow burst was a one-day event. Wed–Fri: **0 opens
  anywhere** (live or shadow), 7 total suggestions, all blocked — `ev_below_roundtrip_cost` ×3
  (Wed 1, Thu 2), `edge_below_minimum` ×2, and a **new** `shadow_prerejection_fork` NOT_EXECUTABLE
  status ×2 (Wed) — new vocabulary, code unreadable tonight (shell-blind), flagged for one-line
  verification, not alarmed.
- **One close in 3 days:** SOFI `7b39c908` (17-lot credit structure, shadow `c8a3a3b0`) force-closed
  07-15 14:15Z via intraday stop, `realized_pl = −534.99` — **exactly equal to
  `unrealized_pl_corroborated`**. See §3 pin: this is the F-CREDIT-SIGN seam booking CORRECTLY one
  day after it booked fiction.
- **Post-epoch live pool UNCHANGED at 8 (1W/7L)**; close-fill-gap counter unchanged 3/10–15 (zero
  live closes in window). N-disqualifier binds absolutely.
- **Book flat since 07-15 14:15Z** — zero open positions in DB. Broker-flat at ~$2,067.86 is a
  HYPOTHESIS tonight (broker-blind; last broker-confirmed 07-15).
- **HEAD moved ~20 commits; six-deploy burst Friday evening** (07-17 21:34→22:48 CDT = 07-18
  02:34→03:48Z). All three services (worker, worker-background, BE) SUCCESS at **`bc4a00b2`** =
  origin/main on-disk ref. Commit subjects UNREADABLE tonight — the movers are unnamed, which
  violates the "name the mover" norm through no fault of the window: a shell-capable session must
  backfill the names.
- Breaker: `entries_paused=false`, reason NULL, fingerprint intact (3 ids, 07-08 21:20Z),
  `updated_at` 07-09 — **correctly untouched** (zero live round-trips in window).

## 3. PIN GRADES

| Pin | Verdict |
|---|---|
| Running SHA = origin/main | ✅ all three services `bc4a00b2` (SUCCESS 03:48Z) = `.git/refs/remotes/origin/main`. ⚠ Subjects of all ~20 new commits UNREAD (shell-blind). FETCH_HEAD lags one tip (`aeab21d8`) — stale partial-fetch artifact, named, not a disagreement of record. |
| F-CREDIT-SIGN fix shipped? | ⚠ **HYPOTHESIS: YES, in `bef2cdd` (07-14 23:05Z)** — behavioral evidence: SOFI credit-structure internal fill 07-15 14:15Z booked −534.99 = corroborated truth; under the bug it would have booked **+5,582.97** (2×1.7994×1700 error). One deploy sits between the QQQ fiction (07-14 16:30Z) and the correct SOFI close. **Code-confirm at `bc4a00b2` is morning item #1 — do NOT record CONFIRMED from this report.** |
| F-CREDIT-SIGN data corrections | ❌ **NOT EXECUTED, 3 days on.** `c1c9ad04.realized_pl` still **+1,815.96** (truth −224.04); `learning_trade_outcomes_v3` still carries `pnl_alpha +1,769.57` on QQQ/IRON_CONDOR. Operator-gated; still pending. |
| F-REPLAY-FK first exercise | ✅ **CLOSED.** `decision_runs` 07-14→07-17: **15/15 `status=ok`, `tape_integrity='complete'`**; `data_blobs` 0 → **25 rows** (all post-07-14). #1199's capture path works; deterministic replay now has a write path. |
| Breaker armed-quiet; flag-only-if list | ✅ un-paused, fingerprint intact, zero flag-only-if conditions met (no re-pause, no new-loss trip owed — zero live closes). |
| Pool 8/8 (1W/7L) · fills 3/10–15 | ✅ unchanged (window's only close was shadow). |
| SOFI sentinel (A8) | ✅ quiet — nothing cleared any gate in 3 days. |
| EXCLUDED-EVIDENCE day 07-06 | ✅ respected. |
| Carried pends #3 (#1198 INFO line) / #5 (thesis `price_basis` fill) | NOT CHECKED (budget; shell-blind limits #1198 anyway) — carried. |

## 4. THE ELEVEN AREAS

### A4 — SELF-SUSTAINING ⚠ **FINDING (MEDIUM-HIGH, operator-side) — F-AUDIT-AVAIL**

**WHAT.** The nightly audit loop itself silently died on **2026-07-16 AND 2026-07-17** — now on
**consecutive WEEKNIGHTS**, escalating a class previously confined to weekends (06-14, 07-05,
07-11, plus the 06-30 start-no-end). Loop availability over the last 7 scheduled nights: **5/7**.
**WHERE.** `audit/cron.log:230-235` (verified this run): both nights show the start marker + the
settings permission warning, then **nothing** — no report, no end marker. The end marker is
unconditional (`run-nightly.cmd:9`), so no-end ⇒ the process was killed externally — the known
sleep-kill signature (07-12 finding). In `-p` headless mode Claude emits output only at run end, so
a kill at ANY point leaves zero output; the signature is consistent.
**WHY #1.** Three RTH days went unaudited while a HIGH finding (F-CREDIT-SIGN) sat with uncorrected
data contamination; the morning-ritual chain (sweep, pending verifications) stalled with it. The
07-13 run explicitly queued "confirm the sleep-hold got set" — the evidence says it was not set or
does not work, and the class is no longer weekend-only.
**IMPACT.** 2 lost runs; 3-day blind window (recovered tonight — production was provably clean:
0 job failures, 1 receipted critical). Cost realized ≈ 0 this time; the class's expected cost grows
with book activity.
**HOW (operator, no code):** (1) confirm the two healthchecks DOWN emails actually arrived 07-16 +
07-17 (if not, the audit dead-man is mis-armed — that would upgrade this finding); (2) hold the
machine awake through the run: scheduled-task setting *or* a `powercfg /requests`-visible wake lock
in `run-nightly.cmd` (e.g. run `claude` under `powershell -Command "& {powercfg ...}"` wrapper or
set the task's "wake to run" + disallow sleep for 30 min); (3) longer-term: move the runner off a
sleeping laptop.
**Additionally (same area, structural):** tonight's session was **shell-blind**; with HEAD 20
commits ahead, the loop could not read ANY of the running code. The audit's tool surface varies
night-to-night (07-15: broker yes/shell no; tonight: neither). No area owns "can the auditor audit
tonight?" — see A11.
**CONFIDENCE:** HIGH on the deaths (log is unambiguous); the sleep cause is the standing 07-12
diagnosis, consistent, not re-proven.

### A1 — PROFITS
No finding; conditions checked; counter ticks. Binding constraint UNCHANGED: the 0.5-clamp × $15
roundtrip gate blocked every live candidate for 3 more days (3× `ev_below_roundtrip_cost`). At 8
closes the N-disqualifier bars outcome findings. Sizing custody: no sizing territory moved (zero
entries). The clamp-review exhibit stands as filed (07-12 FULL).

### A2 — LOSSES
**No NEW finding; the open custody item MOVED favorably but is NOT closed.** F-CREDIT-SIGN
verify-if-shipped could not be completed (shell-blind) — the behavioral evidence (SOFI −534.99
correct) makes "fixed in `bef2cdd`" the leading hypothesis, exactly matching the 07-15 morning
item's suspicion. **Carry: code-confirm at `bc4a00b2`, then the operator-gated data corrections
(position + cash ledger + learning row) — the fiction is still live in three tables.** E6/P0-A
custody unchanged (no live closes to exercise it).

### A3 — SELF-LEARNING
Rider to A2, no independent finding. The QQQ +1,815.96 / alpha +1,769.57 fiction is now the
**oldest uncorrected data-integrity item** (3 days). `is_paper=true` still walls it off live
(#1076). SOFI's outcome ingested honestly (−534.99, alpha −583.51) — evidence the ingest itself is
sound when fed honest fills. [A7 rider: hold-time/exit-quality — nothing new; 0 live closes.]

### A5 — EFFICIENCY
No new class; **improvement measured and it's real:** 3 RTH days at 22–30 warnings/day, **zero
HIGH**, zero phone-flood re-egress (1 critical egress total, receipted, designed). Versus the
07-09/07-10 era (~50/day + 10-hit floods) the noise meter collapsed. Attribution CAUTIONED: whether
the 3-in-1 observability PR shipped in the Friday burst is unverifiable tonight (subjects
unreadable) — do not book the win to a named PR yet. Own budget: 17/12 SQL, over, declared, 5 of
them my own failures.

### A6 — VIABLE-SET HANDLING
No finding; conditions checked. No tier change (DB-corroborated ~$2,067.86, hypothesis), no
rejection-mix regime shift (the 7 blocks are the settled classes + the new shadow_prerejection_fork
vocabulary), no structurally-dead name cleared a live gate. SETTLED holds.

### A7
Dormant, fills **3/10**, exit-code custody: A2.

### A8 — NEGATIVE-DECISION EFFICACY
No finding. SETTLED sentinel quiet (SOFI cleared nothing — its 07-15 window rows are the fork/
dismissed blocks). The NOs of the window are the settled economic classes; nothing hides money on
this evidence. New `shadow_prerejection_fork` class (2 rows) queued for cheap code verification
when a shell exists.

### A9 — ALERT & SIGNAL INTEGRITY
No new liar. The known one (A2's realized_pl fiction) stands uncorrected — the alert/DB
disagreement on `c1c9ad04` remains the exhibit. Positive specimen: the force_close critical +
egress receipt + honest payload (−534.99) agreed with the (now-correct) realized — first time this
window's alert chain and durable record AGREE on a credit internal fill.

### A10 — CALENDAR & CLOCK INTEGRITY
No finding. Midnight boundary crossed correctly; DB clock grounded (broker clock unavailable —
noted as the STATE-anticipated headless case, single-clock grounding declared). Import-time flag
inventory: no readable code change tonight (shell-blind) — inventory unchanged by construction;
re-inventory when the 20 commits become readable.

### A11 — SELF-EXTENSION (nightly, one line)
No area asks **"can the auditor audit tonight?"** — tool surface (broker MCP, shell, Alpaca) varies
per run and silently reshapes what every area can verify; propose a STEP-0.5 capability manifest
(one header line: broker Y/N, shell Y/N, MCP set) plus the standing Regression-Sentinel lens
(07-15) which this window VALIDATED — a shipped-fix regression (F-CREDIT-SIGN) and its fix both
moved without any charter watching that seam.

## 5. FREE LOOK (2 SQL + 1 subagent allowance)
Spent both SQL on the suspicious total quiet: (1) the suggestion funnel Wed–Fri — 7 suggestions,
all blocked, zero-entry regime resumed (fed A1/A6/A8 above); (2) the full alert severity
distribution — warnings 22–30/day, 0 high (fed A5). The subagent allowance was effectively consumed
by the git-metadata agent (its shell was denied; it still pinned origin/main = `bc4a00b2` and the
six deploy↔commit matches from refs/reflogs alone). Reported per contract.

## 6. TOP-3 (value / effort / risk)

1. **Operator: fix the audit host's sleep, and confirm the 07-16/07-17 DOWN emails arrived.**
   The loop missed 2 of the last 7 nights and the class is now weeknight-capable. If the DOWN
   emails did NOT arrive, escalate — the audit dead-man would be mis-armed. Value: restores the
   only unattended oversight layer. Effort: minutes. Risk: none.
2. **Shell-capable session: name the ~20 commits, code-confirm F-CREDIT-SIGN at `bc4a00b2`, then
   execute the operator-gated corrections** (QQQ `c1c9ad04` realized_pl → −224.04 · shadow cash
   ledger −$2,040 · re-derive the learning row). The behavioral evidence says the code fix already
   shipped in `bef2cdd`; only the confirm + the data remain. Value: closes the 07-15 HIGH end-to-
   end. Effort: low. Risk: data-write — operator-gated.
3. **Sweep + reconcile the audit trail:** untracked reports 07-14, 07-15, (tonight's 07-18) into
   the next PR; reconcile the local `audit/ledger.md` divergence vs origin/main BEFORE merging
   (memory records a +104/−455-class divergence from the PR #1233 era; unverifiable tonight).
   Value: findings become visible in the committed view. Effort: low.

## 7. CONFLICTS · SYNERGIES · FOUR-SOURCE DISAGREEMENTS

- **Carried CONFLICT (07-15, D② vs F-CREDIT-SIGN) DE-ESCALATES conditionally:** if the fix is
  code-confirmed at `bc4a00b2`, D②'s "outcomes are basis-independent" rationale is repaired going
  forward, and the residual is purely the historical data correction. Until confirmed, treat as
  open.
- **Four-source disagreements: ONE, known:** `c1c9ad04` realized_pl (+1,815.96) vs corroborated
  mark/alert (−224.04) — the uncorrected 07-15 finding, not a new one. Broker column absent tonight
  (blind) — no broker-side disagreement recordable.
- **Prompt-STATE drift for v5.6 (accumulating, 3rd consecutive report):** running SHA pin ·
  E6/P0-A + P0-B shipped · D② mover · now: F-REPLAY-FK "capture tables 0-row" is STALE
  (25 blobs, 15 complete tapes) — replay moves from KNOWN-PENDING toward shipped-verify.

## 8. SELF-CRITICISM (budget honesty)

Five of my 17 SQL calls died on phantom columns (`strategy_type`, `opened_at`, `is_paper` on
paper_positions; `position_id`, `data_blob_count` on learning/replay tables) — the §8 class the
doctrine warns about, and I introspected only AFTER failing, twice. The §8 rule ("introspect
before typed selects") empirically applies to **paper_positions and decision_runs too**, not just
learning tables. Next run: introspect first, always.

## 9. RETIREMENT COUNTERS

| Area | Tonight | Consecutive no-finding |
|---|---|---|
| A1 | no finding; conditions checked | **8** |
| A2 | no new finding; custody item open (carry) | 1 |
| A3 | rider to A2 | **8** |
| A4 | **FINDING (MED-HIGH) — F-AUDIT-AVAIL** | **0 (reset)** |
| A5 | no new class; improvement measured | 6 |
| A6 | no finding; conditions checked | **8** |
| A7 | dormant (fills 3/10) | — |
| A8 | no finding; sentinel quiet | 1 |
| A9 | no new liar | 4 |
| A10 | no finding; single-clock grounding declared | **8** |
| A11 | one-line delivered | — (permanent) |

*A1/A3/A6/A10 at 8: still the quiet-regime artifact (zero entries all window) — the 07-15
counter-argument stands; no retirement proposal. The honest reading: these counters currently
measure market inactivity, not covered territory.*

STOP. Nothing implemented. READ-ONLY contract held — writes: this report + the ledger entry only;
no ALERT file (grading reasoning in §1); SELECT-only SQL; no flag, order, deploy, or data touched.
