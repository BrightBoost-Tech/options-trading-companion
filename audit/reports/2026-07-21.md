# AUDIT v5.5 — NIGHTLY — 2026-07-21 (Tuesday 00:00 CT scheduled run)

**Mode:** NIGHTLY. **Window:** 2026-07-19 05:01Z → 2026-07-21 05:01Z (Sunday build-silence + Monday
07-20 — the FIRST RTH day since 07-15 and the first natural cycle after the calendar hotfix).
**Prior report:** 2026-07-19 (FULL). **There is NO 2026-07-20 report** — the Monday-midnight nightly
did not produce one (see A4; anticipated, power-gated).
**STEP 0 clock grounding:** DB `now()` = 2026-07-21 05:01:24Z = **Tuesday 00:01:24 CT** — matches the
runner manifest (05:00:04Z). Tuesday ⇒ NIGHTLY. **Broker clock UNREADABLE** (Alpaca MCP absent AND
`audit/broker-snapshot.json` `available:false` — F-RUNNER-BROKER-CREDS STILL unfixed; 2nd consecutive
broker-blind wrapper run). Single-clock (DB) grounding, declared. One premise corrected mid-run (A4).
**Run SHA (H8):** origin/main = worktree = **`ad495be3`** (`ad495be3579942f710160651bc776a7a56741cf9`,
`stale:false`), deployed SUCCESS on ALL THREE services at 03:00:28–29Z (worker `e93c44b5` /
worker-background `b3f8ff04` / BE `ad418d50`), start > last merge (~02:56Z; prior deploy at `8eeb11f4`
REMOVED 02:52Z). The evening batch #1330–#1337 landed 02:48–03:00Z tonight — ~2h before this audit.
**Run limitations:** broker-blind (headless; snapshot creds unset) — broker-dependent claims are
DB-corroborated hypotheses, labeled. No shell; code reads at the fresh worktree (claims not degraded).
**Budget actuals:** 12 SQL (cap 12 — incl. 1 free-look; **1 phantom-column failure + 1 unaggregated
raw-row page — self-criticism §7**) · 0 broker · 3 Railway reads · 1 subagent (cap 8) · ≤500 lines.

---

## 1. ALERT DECISION

**No ALERT-2026-07-21.md.** H11 baseline (48h): **0 critical**. 6 HIGH `job_succeeded_with_errors` =
exactly the ledgered 07-20 calendar-incident trail (OBS-1: pairs at 14:07/17:07/20:07Z re-firing on
the two forced partial runs — the 3h re-fire cadence #1332 was built to kill; ZERO new HIGHs after
20:07Z). Everything else: 7 info `migration_apply` receipts (all reconciled, §3), 1 known
`chain_mechanics_formula_anomaly` warn (17:34Z, §6-designed defensive class), 20
`ops_signal_accuracy_degraded` warns (~2/hr while hit<0.2 — LEDGERED design, line 5701; cite, not
re-find). Tonight's findings are LOW/MED; no live money at risk.

## 2. PART 1 — WEEK IN REVIEW (delta vs 07-19 FULL)

- **Monday ran clean end-to-end on the hotfixed calendar path:** full schedule (day_orchestrator,
  96 order-syncs, 32 monitors, 20 health checks, complete 6-job learning chain — all `succeeded`);
  the natural 16:00Z scan + a 17:33Z re-scan both passed the calendar gate and honestly produced
  **0 suggestions, 0 entries, 0 closes**; book FLAT (0 open). The hotfix natural falsifier stands
  PASSED (ledgered); tonight adds the second clean scan + the bounded alert trail.
- **Post-epoch live pool UNCHANGED at 8 (1W/7L)**; close-fill counter **3/10–15**; A7 dormant. The
  ingest found 3 closed positions in lookback — all correctly dedup-skipped (the 07-14/15 shadow
  closes); `outcomes_created=0`, `errors=0`; model_review `no_scorable_closes` (HONEST-EMPTY).
- **Breaker: designed case-3 suppression observed again** — `suppressed_standing_window: true`,
  fingerprint matches the 07-08 trip (3 ids), `paused_written=false`, `entries_paused=false`/reason
  NULL. Zero flag-only-if conditions met.
- **Three new evidence writers produced their FIRST natural rows** (all observe-only, verified in
  DB): `candidate_terminal_dispositions` 4 finals · `option_quote_provenance` 100 rows (53 rejected
  / 26 passed leg-sets + 21 fetch events) · full cost-recon artifacts
  (`cost-basis/phase2-foundation-1.0`) embedded per disposition. Details in A1/A8.
- **Equity hypothesis corroborated DB-side:** Monday's H7 artifacts recorded scan-time
  `available_bp = 2067.86` — matches the standing ~$2,067.86 figure (still a broker-level
  hypothesis; blind).
- Calibration: Monday 10:00Z relearn `succeeded`; floor state not re-queried (budget; no new closes
  to move it) — carried STATE: 0.5 floor binding as of 07-17.

## 3. PIN GRADES

| Pin | Verdict |
|---|---|
| Running SHA = origin/main | ✅ `ad495be3` SUCCESS all 3 services 03:00Z; H8 clean |
| Monday natural cycle post-hotfix | ✅ 16:00Z + 17:33Z scans clean; calendar gate passed; honest zeros |
| Breaker case-3 suppression | ✅ observed in `job_runs.result.streak_breaker` (fingerprint-match, no re-pause) |
| Pool 8 (1W/7L) · fills 3/10–15 | ✅ unchanged (0 closes in window) |
| #1327 disposition falsifier | ✅ **PASS-consistent** — every ranked survivor wrote exactly ONE typed final (4/4 `h7_dropped`, `is_final=true`); `rank_blocked` alternates path UNEXERCISED (no cycle emitted more than the selection cap) — that sub-path stays pending |
| #1332 A4 alert dedup | VERIFIED-CODE (`ops_health_check.py:551-568` + `ops_health_service.py:802-858`, 4-part identity incl. `failure_signature`); runtime **INCONCLUSIVE** — the next 3h re-fire boundary falls in Tuesday RTH; grade: zero new HIGHs on the two old partials tomorrow |
| #1323 atomic-close guard | migration receipted 23:50Z; **DEFERRED-SAMPLE** (0 internal closes; book flat) |
| #1334 taper band [800,1000] | VERIFIED-CODE `tier_taper.py:143-146`, `ENGINE_VERSION="tier_taper.v2"`, still DARK (cycle_metadata only) |
| #1331 E19 v3 re-freeze | VERIFIED-CODE — v3 file separate (`MINIMUM_DISTINCT_SOURCE_EVENTS=8`), v2 preserved + hash-pinned |
| #1335 receipt writer + privileges | VERIFIED-CODE + APPLIED (receipts 02:56Z): writer RPC idempotent/server-id/marker-validated; **REVOKE TRUNCATE... + BEFORE TRUNCATE guard — the ledgered OBS-2 TRUNCATE residual is now CLOSED** (§4 doctrine note stale — drift list) |
| #1336 single-leg draft manifest | VERIFIED-CODE — generates UNAPPLIED seed SQL only; no registry write; still 0 opt-in/DARK |
| #1330 url.txt / #1337 OI overflow | VERIFIED-CODE (`.gitignore:92` + guard test; `quote_provenance.py:200-211/:315/:336`) |
| Fleet posture | ✅ unchanged-INACTIVE: `fleet_reconciliation_receipts` **0 rows** ⇒ scenario-5 activation remains fail-closed-unsatisfiable; no activation receipt in alerts |
| F-REDATE-0718 re-stamp | ❌ **NOT executed** — 20 v3 rows still `closed_at`=07-18; carried operator item (TOP-2) |
| Migration receipts reconcile | ✅ 7/7 named + receipted; **2 newest (#1335 Lane A/B) have NO ledger entry** — A4 finding (b) |
| Universe 78 / carried pends #3/#5 | NOT re-verified (no mover / budget) — carried |

## 4. THE ELEVEN AREAS

### A1 — PROFITS
Charter answered with the first DIRECT measurement of the binding constraint: Monday's only four
ranked survivors (META LONG_CALL_DEBIT_SPREAD ×2, QQQ LONG_PUT_DEBIT_SPREAD ×2) each died at the H7
prefilter with `roundtrip_bp` — `rt_required` **$3,656–$8,520 vs available_bp $2,067.86 (1.8–4.1×)**.
The constraint is CAPITAL vs structure size (round-trip BP), sitting beneath the known 0.5-floor ×
$15-gate composition. Bonus: the artifacts confirm F-A1-3 ordering **in data** — `ev_basis.flag=
"raw_only"`, `calibrated_ev=null` at the prefilter seam (selection IS raw; apply is downstream).
No defect; N-disqualifier held (no outcome claims at n=8). Conditions checked.

### A2 — LOSSES
No finding; no exit territory exercised (0 closes; 0 internal fires). #1323's guard hardening is
receipted but DEFERRED-SAMPLE. Carried custody state unchanged (E6 shipped; caps still 0/dark —
concurrency tripwire remains the guard). Conditions checked.

### A3 — SELF-LEARNING
No NEW finding. **F-REDATE-0718 carried un-remediated** — 20 v3 rows still dated 07-18; every
paper-performance/go-live window that opens from here reads the +$33.8k phantom cluster as fresh.
Ingest clean (errors=0; dedup correct); TCM v2 still 0/15 accruing (HONEST-EMPTY); model_review
honest-empty. [A7 rider: nothing new; 0 live closes.]

### A4 — SELF-SUSTAINING ⚠ FINDING (MEDIUM, operator+process, two parts)
**(a) The Monday 00:00 CT nightly DID NOT RUN (or died before any artifact).** No 07-20 report, no
runner ALERT in the repo. **Premise corrected this run:** I initially read the 07-20 05:00:04Z
`phase2_precheck` job_run as runner evidence — WRONG; that job is its own 6-hourly schedule (05/11/
17/23Z, running since April), so there is NO DB evidence the Monday runner started at all. This was
the anticipated power-gated miss (`SUB_SLEEP\UNATTENDSLEEP`); #1326 (wake-lock across the child)
merged AFTER it. **Tonight IS the Lane-C falsifier and from this side it is PASSing**: manifest
written 05:00:04Z, wake lock `acquired`, fresh worktree `stale:false`, this report exists — final
grade belongs to the runner's completion contract (markers/ping at the operator sink; verify in the
morning cron.log). **(b) Run-record gap:** the evening batch **#1330–#1337 merged+deployed
(02:48–03:00Z) with TWO migrations APPLIED 02:56Z** (`20260721010000_rpc_issue_fleet_reconciliation_
receipt_v1`, `20260721010500_harden_fleet_receipt_privileges`) — receipts exist, but there is **no
ledger entry and no results doc** for the run. NEVER-REAPPLY memory for those two names currently
rests on DB receipts alone, and CLAUDE.md §4's "service_role retains TRUNCATE" residual note is now
STALE (closed by `20260721010500`). HOW: one morning docs PR — ledger entry for the batch + doctrine
drift line. Also: **broker-creds gap now 2 runs old** (07-19 TOP-1, untouched). CONFIDENCE: HIGH.

### A5 — EFFICIENCY
No new class. Measured: the calendar incident cost exactly 6 HIGHs, bounded by the 3h re-fire
cadence (#1332 should make the next incident cost 1); `ops_signal_accuracy_degraded` wrote 20 rows
Monday — ledgered design (line 5701), cited not re-found. **FREE-LOOK find:** `phase2_precheck` has
no-oped `{"status":"window_expired"}` 4×/day since ~2026-04-27 (~340 runs, `hours_since_deploy`
2,139) + 2 `dead_lettered` fossils — dead schedule spend and log noise; retire/descope is
owner-gated. Budget honesty in §7.

### A6 — VIABLE-SET HANDLING
No finding; conditions checked: no tier change (scan-time BP 2,067.86); no rejection-mix regime
shift (Monday's ranked deaths all `roundtrip_bp` — the settled 1-of-84 economics, now typed); no
structurally-dead name cleared a gate. SETTLED holds.

### A7 — dormant, fills 3/10, exit-code custody: A2.

### A8 — NEGATIVE-DECISION EFFICACY
Charter answered with the lens's FIRST fully-typed natural evidence: the NOs are correct and
reconstructable to the dollar — 163 candidates honestly rejected at scan; the 4 that ranked died
`h7_dropped/roundtrip_bp` with complete cost-recon artifacts (7 bases, typed-unavailable where
unmeasured, COMPARE-never-SUM guards written into the artifact itself). No hidden-money class:
rt_required ≥1.8× available BP is arithmetic, not model error. SOFI sentinel quiet. The #1281/#1272
investment paid out on its first RTH day. No defect finding.

### A9 — ALERT & SIGNAL INTEGRITY ⚠ FINDING (LOW-MED) — A4-detector status misstatement
WHAT: the silent-failure alert's message hardcodes *"reported status=succeeded but its
result.counts.errors=1"* while the detector matches `status IN ('succeeded','partial')`. WHERE:
`packages/quantum/jobs/handlers/ops_health_check.py:580-583` (template) vs
`services/ops_health_service.py:724` (query). IMPACT: Monday's 6 HIGHs described two jobs that
honestly reported **partial** as claiming success — inverting the alert's own "silently masked"
thesis; an operator triaging from the inbox gets a false story about what the job admitted. WHY #1:
it fired 6 times on the first real incident of the `partial`-status era (post-`20260718144818`).
HOW: one-line template fix — interpolate the actual status; ride any ops PR. EVIDENCE: 6 alert rows
14:07–20:07Z + code at `ad495be3`. RISK: none (message-only). CONFIDENCE: HIGH. (For the record:
the #1332 dedup identity is 4-part incl. `failure_signature` — finer than the commit subject
claimed; correct behavior.)

### A10 — CALENDAR & CLOCK INTEGRITY
No finding. Midnight boundary crossed correctly (clock-grounded). Evening-batch sweep: no new
module-scope env reads in the verified movers (taper values are versioned constants; the OI age
knob is call-time-read with an overflow guard). Boundaries stand: Labor Day 09-07 (#1229 falsifier)
· DST 2026-11-01. The calendar-hotfix class stays owned by the ledger entry.

### A11 — SELF-EXTENSION (nightly one-liner)
No area owns **run-record completeness** — tonight's unledgered-merges gap (A4b) was caught only
because migration receipts exist; a "every deploy-batch has a ledger entry" check would make that
loud (owner-gated; the pending A10→SECURITY rotation could absorb it as provenance hygiene).

## 5. FREE LOOK
Spent 1 SQL, 0 subagents: `phase2_precheck` full history — produced the A5 dead-schedule find AND
the A4 premise correction (its 05:00Z rows are schedule, not runner evidence). Both consumed above.

## 6. TOP-3 (value / effort / risk)

1. **Operator: set the runner's read-only broker creds** (3rd night blind; carried 07-19 TOP-1
   verbatim — minutes of work, restores broker truth to the unattended loop; keep keys out of repo).
2. **Operator: execute the F-REDATE re-stamp** (2nd carry) — the +$33.8k phantom 07-18 cluster ages
   into every go-live/paper-performance window; the guarded UPDATE is already written in the 07-19
   report/packet.
3. **Morning docs PR (one PR clears three):** ledger entry for the #1330–#1337 evening batch (+ the
   two 02:56Z migration names as NEVER-REAPPLY) · CLAUDE.md §4 TRUNCATE-residual staleness line ·
   the A9 one-line message fix can ride the same or any ops PR. Morning ritual additionally: grade
   tonight's runner completion at the operator sink (markers + ping) and #1332's first quiet RTH day.

## 7. CONFLICTS · SYNERGIES · DISAGREEMENTS · SELF-CRITICISM

**Conflicts:** none — nothing proposed touches a decision path. **Synergies:** A4(b) and A11 are the
same surface; the A9 fix and the ledger PR can share a vehicle. **Four-source disagreements:** none
new (code/DB/Railway agree everywhere checked; broker column absent — blind). **Prompt-STATE drift
(5th consecutive carry, movers named):** E6 shipped · calibration STATE raw-mode-era · runner STATE
should record wrapper + creds dependency · NEW: §4 TRUNCATE residual stale · STATE "capture tables
0-row" long stale. **Self-criticism (budget honesty):** (1) one phantom-column failure (`type` vs
`alert_type`) — I introspected only AFTER failing, violating the adopted introspect-first rule;
(2) the free-look query paged ~200 raw `phase2_precheck` rows with no aggregate/LIMIT — exactly the
"never page raw rows" class; the information gained justified 6 lines, not 200 rows. Both are
process regressions vs the 07-19 run's clean sheet.

## 8. RETIREMENT COUNTERS

| Area | Tonight | Consecutive no-finding |
|---|---|---|
| A1 | no finding; constraint directly measured | **10** |
| A2 | no finding; conditions checked | 3 |
| A3 | no NEW finding; F-REDATE carried (operator) | 1 |
| A4 | **FINDING (MED) — Monday nightly miss + unledgered evening batch** | 0 |
| A5 | no new class; free-look dead-schedule note | 8 |
| A6 | no finding; conditions checked | **10** |
| A7 | dormant (fills 3/10) | — |
| A8 | no defect; first typed evidence landed | 3 |
| A9 | **FINDING (LOW-MED) — A4-detector status misstatement** | 0 |
| A10 | no finding; sweep clean | **10** |
| A11 | one-liner delivered | — (permanent) |

*A1/A6/A10 at 10 — still the quiet-regime artifact; A1's counter measures market inactivity while
its charter keeps producing structural answers (tonight's is the strongest yet). No retirement
proposal. A10's rotation remains proposed via A11, owner-gated.*

STOP. Nothing implemented. READ-ONLY contract held — writes: this report + one ledger entry only;
no ALERT file (§1); SELECT-only SQL (12); no flag, order, deploy, migration, env, fleet, or data
value touched.
