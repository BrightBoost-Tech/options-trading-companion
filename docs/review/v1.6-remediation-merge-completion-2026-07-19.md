# v1.6 Remediation — Merge Completion — 2026-07-19

Continuation of `docs/review/v1.6-remediation-results-2026-07-19.md` (which recorded the audit
merge, the runner HIGH fix, and the landing blocker). This run resolved the landing blocker and
merged all six lanes. Orchestrator: Fable; reviews: Opus (`model=opus`). **Zero migration /
production-DB write / broker write / fleet action / env-or-control change / manual nightly-or-
trading job across the entire run.**

## Local runner landing — RESOLVED (was BLOCKED_LOCAL_RUNNER_PULL)

- Archive first: timestamped dir (`%TEMP%\otc-friday-post-close-2026-07-17\v16-remediation\
  pull-resolution-20260719T1925Z\`) with status, HEAD/worktrees, full local ledger, binary diff,
  the local nightly report, origin/main versions, SHA-256 hashes.
- Three-way compare: local `audit/reports/2026-07-19.md` **byte-identical** to origin/main
  (`fdea8e8e…` both); local `audit/ledger.md` = detached-HEAD version + 43 nightly-added lines,
  and **all 43 non-blank lines exist verbatim in origin/main** (the #1311 reproduction) ⇒
  `LOCAL_UNIQUE_CONTENT=0`.
- Clean: ledger restored to its HEAD version; the duplicate report MOVED to the archive (not
  deleted); no other file changed. `git switch main && git pull --ff-only` →
  HEAD == origin/main == `353b930c` (then advanced by this run's own reviewed merges).
- Landing verification: merged runner code present in the checkout; scheduled task Ready →
  documented `audit\run-nightly.cmd`, workdir pinned, next run 07-20 00:00 CT — **tonight runs
  the FIXED runner**; 55/55 safe self-tests passed IN the checkout; inventory hash unchanged by
  the tests (`5c6ae8bf…`); the real nightly was NOT run manually.

## Six serialized merges (adversarial opus review each → update to current main → fresh
current-head CI → squash → all-4-services deploy SUCCESS → broker flat + 0 crit/high +
entries_paused/fleet unchanged)

| lane | PR | review | merge SHA | deploy (4/4 SUCCESS) |
|---|---|---|---|---|
| A risk-basis arm evidence (enforcement dark) | #1306 | PASS after 1 repair cycle — CI's strict H9 gate caught a `silent_swallow` in `persist_arm_evidence`; repaired loud-partial (logger.error + durable `risk_basis_arm_evidence_persist_failed` warning alert + preserved `counts.errors`→partial fold), head `41ceb8ba` | `362bd3da` | 19:45:44Z |
| B HMAC canonical prod detection + fail-closed replay tests | #1307 | PASS (fail-open structurally unreachable under production; every unskip repair validated against current code; 232 tests) | `aced5eaf` | 19:51:54Z |
| C holiday-aware market sessions (entry tightening only) | #1304 | PASS (exit paths structurally immune to calendar outage; tightened edges reach only the human-approval endpoint) | `54fd978a` | 19:58:00Z |
| D candidate lifecycle milestones (observe-only) | #1310 | PASS (one-final invariant + h7 CHECK un-violable; `broker_submitted` live-only; `filled` on broker evidence only) | `0feb6cec` | 20:04:01Z |
| E OI known-at provenance (no OI gate) | #1309 | PASS (no fabrication path — retrieval time never becomes an observation date; 1 MINOR follow-up: `OverflowError` on an `inf` env value, upstream-caught) | `0be131f6` | 20:10:08Z |
| F greek divisibility guard (caps dormant) | #1308 | PASS (195-combo empirical byte-identity sweep: 0 count mutations; all divergences = intended non-divisible→typed-None) | `d4c083ea` | (this run's final deploy) |

Special checks all held: #1306 enforcement dark + Monday falsifier untriggered · #1307
production nonce outage unconditionally fail-closed (typed 503) · #1304 exits available on
outage, entries fail closed · #1310 blocked candidates never advance; live `filled` only after
broker evidence · #1309 retrieval time never fabricated as observation date · #1308 valid
structures byte-identical, caps 0.

## Natural runtime falsifiers still pending (nothing triggered early)

1. **Tonight 00:00 CT** — first natural nightly under the FIXED runner: per-run-tagged markers
   in `audit/runner-markers.log`, disposable `%LOCALAPPDATA%` worktree, operator checkout
   untouched, honest ping.
2. **First natural trading cycle** — `job_runs.result.cycle_metadata.risk_basis_arm_evidence`
   payload with `would_flip` populated (Lane A; the ~1-week P0-B arm review clock starts there).
3. **First live-routed candidate chain** — `staged → broker_submitted → filled` milestones
   (Lane D).
4. **2026-09-07 Labor Day** — zero suggestion generation, zero false job-late/data_stale
   (Lane C; also the #1229 falsifier).
5. **First provider payload carrying a genuine OI date** — Lane E flips that leg from
   `provider_date_unavailable` to a dated, freshness-computed row.
6. Lane F: none (inert until greek-cap arming, a separate owner decision).

## Non-blocking follow-ups recorded (from PASS reviews; no code change this run)

- `scripts/run_signed_task.py:360-367` — second weekday-only `is_market_day()` with a false
  docstring (same F-A10-HOLIDAY class; gates only the read-only audit runner) — fix docstring or
  delegate at next touch.
- `quote_provenance.py` `oi_observation_max_age_days()` — add `OverflowError`/`isfinite` guard +
  a both-ways parser test (§3 discipline).
- `config.py` `audit_production_security()` — stale over-warning text about
  `TASK_NONCE_FAIL_CLOSED_IN_PROD` (now inert in production; text-only fix).
- Lane A polish: dedupe the `candidate_bases` double-call; document the deliberate
  drain-robustness asymmetry + thread-boundary caveat.
- Runner: defensive `assert run_tag`; future sidecar rotation.
- `.Jules/`-vs-`.jules/` tracked-path case-collision (phantom `M` in fresh Windows worktrees) —
  de-dup in a normal code PR.

## Standing gates (unchanged)

Fleet activation still requires: the repaired nightly's **natural PASS tonight** + **Monday
evidence PASS** (`monday_evidence_reader` ≥17:45Z) + a **separate explicit operator token**
(`FLEET_ACTIVATION_AUTHORIZED=1` + confirm-literal + 50-slot payload + attestation). F-REDATE-0718
(nightly MEDIUM) awaits operator adjudication. `entries_paused=false`; broker flat throughout;
0 critical/high across the run; fleet byte-unchanged (1 `pending_legacy_terminal` / 50 inactive /
0 bindings / 0 receipts).
