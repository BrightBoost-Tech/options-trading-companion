# v1.6 Remediation Orchestrator — Results — 2026-07-19

Orchestrator: Fable (claude-fable-5). Implementation/review agents: Opus (explicit `model=opus`,
cap 5). Authorization: code/test/docs merges only — **zero** migration, production-DB write,
broker write, fleet action, Railway/env mutation, manual trading/evidence job, or live-control
loosening occurred. The v1.6 audit report (`external-full-audit-v1.6-results-2026-07-19.md`)
is preserved as a historical pinned-SHA audit at `20ca312e`; nothing below rewrites its findings.

## Grounding (Phase 0)

Clocks: DB `2026-07-19 17:25:59Z` ≡ broker `13:25 ET` (agree); market CLOSED, next open Mon
07-20 09:30 ET. Broker flat (0 positions / 0 open orders). 0 critical/high 72h.
`entries_paused=false`. Fleet: 1 `pending_legacy_terminal` / 50 inactive / 0 active / 0 bindings /
0 activation receipts (no receipts table exists — none ever written) / 50 approved policies.
Start `origin/main=20ca312e` (all four services SUCCESS at it); operator checkout detached at
`17141967` with tracked `M audit/ledger.md` + expected untracked artifacts — status/diff archived
to `%TEMP%\otc-friday-post-close-2026-07-17\v16-remediation\` before any work. Operator-checkout
inventory hash at start: `ddb9e07362a1a26b`. Only open PRs touching planned files: none
(Palette FE PRs are disjoint).

## Lane table (final)

| lane | finding | PR | merge SHA | CI | deployment | runtime state | remaining falsifier |
|---|---|---|---|---|---|---|---|
| audit docs | v1.6 report + F-A4 rewording | #1303 | `d6a3174e` | VERIFIED-CI | VERIFIED-DEPLOYMENT (4/4 SUCCESS) | docs live | — |
| runner HIGH | F-RUNNER-WORKTREE-DEADFALLBACK | #1305 | `8588754d` | VERIFIED-CI | VERIFIED-DEPLOYMENT (4/4 SUCCESS) | code live; **local landing `BLOCKED_LOCAL_RUNNER_PULL`** | first natural nightly under the FIXED local runner: per-run-tagged markers in `audit/runner-markers.log`, fresh `%LOCALAPPDATA%` worktree, operator checkout untouched |
| Lane A | F-A4-RISKBASIS-SILENT (MED) | #1306 draft | — (merge-blocked) | VERIFIED-CI (branch) | NOT-PROVEN | built + adversary-grade central review PASS | first natural cycle writing `job_runs.result.cycle_metadata.risk_basis_arm_evidence` with `would_flip` populated |
| Lane B | F-A9-1 + F-A9-2 (MED) | #1307 draft | — (merge-blocked) | VERIFIED-CI (branch) | NOT-PROVEN | built + central review PASS; 8 suites unskipped | prod-marker matrix runs in CI on merge; nonce-outage 503 path is test-proven (runtime falsifier: none needed beyond CI) |
| Lane C | F-A10-HOLIDAY (MED) | #1304 draft | — (merge-blocked) | VERIFIED-CI (branch) | NOT-PROVEN | built + central review PASS | first holiday weekday (2026-09-07 Labor Day): zero suggestion generation, zero false job-late/data_stale |
| Lane D | A3-LIFECYCLE (LOW) | #1310 draft | — (merge-blocked) | VERIFIED-CI (branch) | NOT-PROVEN | schema-contract PASS (no migration); observe-only | first natural staged→broker_submitted→filled chain on a live-routed candidate |
| Lane E | A5-OI-KNOWNAT (LOW) | #1309 draft | — (merge-blocked) | VERIFIED-CI (branch) | NOT-PROVEN | built + central review PASS | first provenance row after a provider payload carries a genuine OI date (until then `provider_date_unavailable` is the truthful steady state) |
| Lane F | A4-DIVISIBILITY (LOW-INERT) | #1308 draft | — (merge-blocked) | VERIFIED-CI (branch) | NOT-PROVEN | built + central review PASS; byte-identity proven | none (caps dormant); inert until cap arming |
| docs final | this record | this PR | (merge SHA in PR) | required | required | — | — |

Branch CI note: each lane branch ran CI green at its head at build time; the serialized merge
process re-requires update-branch + current-head CI at each lane's merge slot.

## Phase 2 detail — the HIGH fix and its review cycle

Implementation (#1305) satisfied the 12-item contract; the **independent opus adversarial review
returned FAIL** with a real BLOCKER — the durable marker sidecar accumulated across nights, so a
prior run's `start`/`end` markers could satisfy the current run's completion contract (false UP
ping over lost markers — the exact class the fix targets), proven end-to-end by a reviewer probe.
Repair: per-run tag `[run=<report_date>-<pid>-<rand>]` stamped into START/END/ABORTED lines;
contract requires `marker AND run_tag` per line; + missing run-SHA now a typed fail-closed
contract reason; + the finally-block completion evaluation wrapped so an exception routes to the
`/fail` ping and always releases the lock; + `Path(env.strip())`. Re-review: **PASS** (reviewer
re-ran an independent falsifier; 55 tests; residual NOTEs only: defensive `assert run_tag`,
future sidecar rotation). Merged squash → `8588754d`; all four services SUCCESS; broker flat;
0 new critical/high.

## Local operational landing — `BLOCKED_LOCAL_RUNNER_PULL`

The operator checkout carries a tracked modification: the 00:00 CT nightly (OLD runner, cwd='.')
appended its 07-19 FULL-audit entry (43 lines, incl. F-REDATE-0718 MEDIUM) to `audit/ledger.md`,
on a detached HEAD at `17141967`. Per the landing contract, no stash/reset was performed and the
pull was NOT run. Mitigations executed instead: the diff+status were archived outside the repo,
and this docs PR **reproduces the nightly's ledger entry and sweeps `audit/reports/2026-07-19.md`
into main**, so the findings survive even if tonight's old-runner `checkout --force` discards the
local copy. Scheduled task verified read-only: `Ready`, executes the documented wrapper
`audit\run-nightly.cmd`, workdir pinned to the checkout, last run 07-19 00:00 (rc 0), next run
07-20 00:00. Safe self-test suite on merged content: **55 passed**. No manual nightly audit run.

**Operator morning/tonight checklist (before 00:00 CT):**
1. In `C:\options-trading-companion`: reconcile the local `audit/ledger.md` edit (its content is
   now already in main via this PR — `git checkout -- audit/ledger.md` after confirming
   byte-equivalence against main's reproduced entry, or commit-and-discard per preference), then
   `git checkout main && git fetch origin && git pull --ff-only`.
2. That pull lands the FIXED runner locally; tonight's 00:00 CT run then uses the disposable
   `%LOCALAPPDATA%` worktree and can no longer touch the checkout.
3. Then clear the merge gate: the six lane PRs merge in order #1306 → #1307 → #1304 → #1310 →
   #1309 → #1308 (update-branch + current-head CI + deploy-verify + broker/alert check each).

## Monday natural-evidence checklist (≥ 17:45Z, unchanged from the Sunday standing, plus new)

1. `monday_evidence_reader` (operator prompt `docs/review/monday-evidence-operator-prompt-2026-07-20.md`).
2. Fleet activation decision (READY_FOR_SEPARATE_AUTHORIZATION; packet 1 + ratification 1 +
   separate token) — only after a trustworthy nightly (fixed runner) + Monday evidence PASS.
3. New post-merge falsifiers as the lanes land: first `risk_basis_arm_evidence` payload; first
   lifecycle milestone chain; holiday falsifier waits for 2026-09-07.
4. F-REDATE-0718 (nightly MEDIUM, `status:reported`): adjudicate the 20 re-dated shadow rows'
   `closed_at` (paper-window contamination in go-live validation / autotune reads; live
   calibration excluded by `is_paper=false`).

## Explicit non-build dispositions (unchanged)

A1-G1 (needs natural post-07-16 realized overlap) · A2-ASSIGNMENT (NOT-PROVEN / DEFERRED-SAMPLE)
· UI honesty (Palette/Jules file ownership) · fleet activation (separate authorization) ·
TCM/taper/greek/OI/single-leg live activation (owner-gated) · notes N1/N2/CLAUDE-size. The Monday
evidence reader is the natural falsifier surface — not triggered early.

## Safety attestation

Broker positions/orders: flat at every checkpoint. risk_alerts: 0 new critical/high all run.
`entries_paused=false` untouched. Fleet state byte-unchanged. Migrations/DB writes/broker
writes/env mutations: **zero**. Operator worktree: start hash `ddb9e07362a1a26b`; the only change
across the run is the pre-existing nightly-authored drift (untouched by this run — end state
identical to start; re-verified before the docs merge).
