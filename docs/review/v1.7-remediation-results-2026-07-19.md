# v1.7 Remediation — Results — 2026-07-19

Orchestrator: Fable (claude-fable-5). Opus verify/build/review agents (`model=opus`, cap 5).
Authorization: code/test/docs merges + two named DDL migrations by exact name + doctrine receipts
ONLY. **Zero** production-data correction, broker write, fleet provision/activation, Railway/env
mutation, manual scan/executor/close/learning/audit job, or live-control change occurred.

## Grounding
Basis code main at start `f48c298c` (all 4 services deployed); DB ≡ broker clock, market CLOSED
(Sunday); broker flat 0/0; 0 crit/high 72h; `entries_paused=false`; fleet 1 `pending_legacy_terminal`
/ 50 inactive / 0 active / 0 bindings / 0 activation receipts; registry 50/50 approved. Operator
worktree preserved; start hash `5c6ae8bf…`.

## Findings → fixes (serialized merge order; each: adversarial review → update-branch → current-head
CI → squash → all-4-services deploy SUCCESS → broker-flat/0-crit-high checkpoint)

| # | finding | PR | merge SHA | migration (applied by exact name) | receipt |
|---|---|---|---|---|---|
| 1A | V17-1 atomic internal-close RPC foundation | #1316 | `3ec4f766` | `20260719180000_rpc_commit_internal_close_v1` | `risk_alerts 8cfd7333` fp c3fd80d0 |
| 1B | V17-1 route switch to the atomic RPC | #1317 | `2b9099d3` | — | — |
| 2 | V17-2 fleet activation txn binding | #1315 | `390bf3c7` | `20260719020000_harden_shadow_fleet_activation_rpc` | `risk_alerts 84687a20` fp 0244d24a |
| 3 | V17-5 credential-safe market-data logging | #1314 | `d7c2ebd5` | — | — |
| 4 | V17-3+V17-4 TCM evidence honesty | #1318 | `d1a7f22b` | — | — |
| docs | this record + audit results + census packet | (this PR) | — | — | — |

## Adversarial review outcomes (every code lane reviewed; DDL lanes got two reviews each)
- **Lane 1A** — two reviews (SQL/security + close-custody/economics) BOTH FAIL → repaired
  (live-order isolation guard, NaN/±Inf rejection, #1017 provenance, bidirectional enum drift-lock)
  → re-review PASS. Economic core (sign, all-or-none, CAS-race, idempotency, ledger no-swallow)
  runtime-proven byte-identical; apply-safety verified (additive nullable marker columns, zero-row
  partial index). Migration applied: 1 overload, service_role-only, **zero business-row change**
  (orders 528 / positions 86 / ledger 193 / portfolios 57 / cash_sum $550,761.17 unchanged);
  function never invoked on production.
- **Lane 1B** — one review PASS. Re-orphaning eliminated (single atomic commit, no residual writes),
  live-broker path byte-identical, RPC failure holds the position open fail-safe, the CLOSE_FILL_GAP
  structural-None claim confirmed. One CI repair converted two #1126 source-string costume tests
  (`TestFix3CloseOrderSubmittedAtWrite`) into real behavior tests (the FIX-3 `submitted_at`
  invariant relocated into the RPC's `COALESCE(submitted_at, v_now)` and verified two ways).
- **Lane 2** — two reviews (DB/security + experiment-identity) BOTH PASS with two required-before-
  apply fixes → repaired (COLLATE "C" ordering-determinism pin, bind-off-`v_derived_map`,
  drift-lock the outer wrapper) → Fable-verified on the live DB (COLLATE "C" == codepoint for the
  contention canary; server fingerprint == committed `1cd004b5…`). Migration applied: old 4-arg
  overload DROPPED, hardened 5-arg service_role-only, **fleet state byte-unchanged**.
- **Lane 3** — Fable central review PASS (0 residual key-slicing, constant message, 6 synthetic-
  secret tests). **Rotation status NOT_PROVEN.**
- **Lane 4** — Fable central review PASS (economic evidence keyed on `execution_mode`, new
  `economic_evidence_cohort` axis, broker-live headline provably == the alpaca_live sum).

## Fleet standing — INACTIVE, NOT READY FOR AUTHORIZATION
The activation transaction is now artifact/registry/receipt bound, but the fleet is unchanged (0
active / 0 bindings / 0 activation receipts). Activation still requires, all separately:
`FLEET_ACTIVATION_AUTHORIZED=1` + confirm-literal + the 50-slot payload + attestation of the
**`1cd004b5…`** fingerprint (owner-packet-1's ratified token was against the non-reproducible
`6f8d1499…` and must be RE-ISSUED against `1cd004b5…`) + the scenario-5 durable receipt-contract
prerequisite + a natural fixed-runner nightly PASS + Monday evidence. None of these is done here.

## Production anomaly census (V17-1) — CLEAN, no correction
Current-state clean (0 orphans, 0 duplicate filled-close-orders). 3 pre-guard historical incidents
(CSX `d077c93d` 2026-05-18 + `7cb40372`/`ef009864` 2026-04-03) recorded in the operator packet
`docs/review/v17-1-internal-close-anomaly-census-2026-07-19.md`. **No rows corrected**
(`PRODUCTION_DATA_CORRECTION=false`). The 22 "duplicate fill-ledger" positions are mostly
legitimate multi-leg per-leg fills, NOT anomalies.

## Migrations applied by exact name (NEVER REAPPLY)
- `20260719180000_rpc_commit_internal_close_v1` (MCP version `20260719215826`)
- `20260719020000_harden_shadow_fleet_activation_rpc` (MCP version `20260719231412`)

## Natural falsifiers still pending
First natural internal-close through the atomic RPC (Lane 1A/1B); the tonight fixed-runner nightly
(carried from v1.6); first `risk_basis_arm_evidence` payload; the TCM honesty surfaces are
observe-only (no live consumer). Fleet activation gated as above.

## Safety attestation
Broker flat at every checkpoint; 0 new critical/high across the run; `entries_paused=false`
untouched; fleet byte-unchanged and inactive. Migrations/receipts are the ONLY production DB writes;
zero business-row corrections; zero broker/fleet/env/control changes. Operator worktree: start hash
`5c6ae8bf…` (end hash recorded in the closing report).
