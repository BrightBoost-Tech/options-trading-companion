# packages/quantum/.env.example

# 1. Set Environment to 'development' or 'production' or 'sandbox'
# 'sandbox'     = Fake banks (Plaid Sandbox). Safe for testing.
# 'development' = Real Banks, Free (limited logins). Best for personal use.
# 'production'  = Real Banks, Paid (requires Plaid approval).
PLAID_ENV=sandbox

# 2. Use the matching Secret Key from your Plaid Dashboard
# GO TO: https://dashboard.plaid.com/team/keys
PLAID_CLIENT_ID=your_client_id_here
PLAID_SECRET=your_matching_secret_key_here  # Must match the ENV above

# 3. Other backend keys (placeholders)
POLYGON_API_KEY=your_polygon_key_here
SUPABASE_URL=your_supabase_url_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
ENCRYPTION_KEY=your_encryption_key_here
