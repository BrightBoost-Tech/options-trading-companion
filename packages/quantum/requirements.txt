# To install these dependencies, navigate to this directory in your terminal and run:
# cd packages/quantum
# pip install -r requirements.txt

# Core web framework
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0 # For data validation and settings management
python-multipart>=0.0.20 # For file uploads

# External Services
supabase>=1.0.0 # Supabase Python client
alpaca-py>=0.28.0 # Alpaca broker client (paper + live)
pytz>=2024.1 # Required by alpaca-py data client internals
python-dotenv>=1.0.0 # For loading .env files
requests>=2.0.0 # For HTTP requests

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# Security & Rate Limiting
cryptography>=41.0.0
pyjwt>=2.8.0
slowapi>=0.1.9
redis>=5.0.0
rq>=1.15.0,<3.0.0
apscheduler>=3.10.0,<4.0.0 # In-process cron scheduler for precise job timing
qci-client>=5.0.0; python_version < "3.14"
